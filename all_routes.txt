=== academicYearRoutes.js ===
// src/routes/academicYearRoutes.js
import express from "express";

import {
  getAcademicYears,
  getActiveAcademicYear,
  createAcademicYear,
  updateAcademicYear,
  deleteAcademicYear,
  setActiveAcademicYear,
} from "../controllers/academicYearController.js";

import authMiddleware from "../middleware/authMiddleware.js";
import  checkPermission  from "../middleware/checkPermission.js";

const router = express.Router();

/**
 * ظ…ظ„ط§ط­ط¸ط§طھ ط¹ظ† ط§ظ„طµظ„ط§ط­ظٹط§طھ:
 * - ط§ط³طھط®ط¯ظ…طھ:
 *    calendar.view_years   ظ„ط¹ط±ط¶ ط§ظ„ط³ظ†ظˆط§طھ
 *    calendar.manage_years ظ„ظ„ط¥ط¶ط§ظپط© ظˆط§ظ„طھط¹ط¯ظٹظ„ ظˆط§ظ„ط­ط°ظپ ظˆط§ظ„طھظپط¹ظٹظ„
 * - ظ„ظˆ ظ…ط§ ط¹ظ†ط¯ظƒ permission ط¨ط§ط³ظ… "calendar.manage_years" ط£ط¶ظگظپظ‡ ظپظٹ ط¬ط¯ظˆظ„ permissions
 *   ظˆط§ط±ط¨ط·ظ‡ ط¨ط¯ظˆط± ط§ظ„ظ€ admin ظپظٹ ط¬ط¯ظˆظ„ role_permissions.
 */

// ط¬ظ„ط¨ ظƒظ„ ط§ظ„ط³ظ†ظˆط§طھ ط§ظ„ط¯ط±ط§ط³ظٹط©
router.get(
  "/",
  authMiddleware,
  checkPermission("calendar.view_years"),
  getAcademicYears
);

// ط¬ظ„ط¨ ط§ظ„ط³ظ†ط© ط§ظ„ط¯ط±ط§ط³ظٹط© ط§ظ„ط­ط§ظ„ظٹط©
router.get(
  "/active",
  authMiddleware,
  checkPermission("calendar.view_years"),
  getActiveAcademicYear
);

// ط¥ظ†ط´ط§ط، ط³ظ†ط© ط¯ط±ط§ط³ظٹط© ط¬ط¯ظٹط¯ط©
router.post(
  "/",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  createAcademicYear
);

// طھط¹ط¯ظٹظ„ ط³ظ†ط© ط¯ط±ط§ط³ظٹط©
router.put(
  "/:id",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  updateAcademicYear
);

// ط­ط°ظپ ط³ظ†ط© ط¯ط±ط§ط³ظٹط©
router.delete(
  "/:id",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  deleteAcademicYear
);

// طھط¹ظٹظٹظ† ط³ظ†ط© ظƒظ€ ط³ظ†ط© ط­ط§ظ„ظٹط©
router.patch(
  "/:id/activate",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  setActiveAcademicYear
);

export default router;

=== activityRoutes.js ===
import express from 'express';
import { getRecentActivities } from '../controllers/activityLogController.js';

// âœ… ط§ظ„ط§ط³طھط¯ط¹ط§ط، ط§ظ„طµط­ظٹط­ ط¨ظ†ط§ط،ظ‹ ط¹ظ„ظ‰ ظ†ط¸ط§ظ…ظƒ
import authMiddleware from '../middleware/authMiddleware.js'; 

const router = express.Router();

// ظ…ط³ط§ط± ظ…ط­ظ…ظٹ ظٹط¬ظ„ط¨ ط£ط­ط¯ط« ط§ظ„ظ†ط´ط§ط·ط§طھ ظ„ظ„ظ…ط¯ط±ط³ط©
router.get('/activities/recent', authMiddleware, getRecentActivities);

export default router;

=== adminAssessmentsRoutes.js ===
import express from 'express';
import authMiddleware from "../middleware/authMiddleware.js";
import { autoActivityLogger } from '../middleware/activityLogger.js';
import { 
    getAdminAssessments, 
    getAssessmentGrades, 
    bulkOverrideGrades ,
    getSubjectsByGradeId
} from '../controllers/adminAssessmentsController.js';

const router = express.Router();

// ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ‡ظ†ط§ طھط­طھط§ط¬ ط¥ظ„ظ‰ طھط³ط¬ظٹظ„ ط¯ط®ظˆظ„
router.use(authMiddleware);

// 1. ط¬ظ„ط¨ ط§ظ„طھظ‚ظٹظٹظ…ط§طھ ط¨ظ†ط§ط،ظ‹ ط¹ظ„ظ‰ ط§ظ„ط´ط¹ط¨ط© ظˆط§ظ„ظ…ط§ط¯ط© (ظ„ظ„ظ‚ط§ط¦ظ…ط© ط§ظ„ظ…ظ†ط³ط¯ظ„ط©)
router.get('/', getAdminAssessments);
router.get('/subjects-by-grade', getSubjectsByGradeId);
// 2. ط¬ظ„ط¨ ط¯ط±ط¬ط§طھ طھظ‚ظٹظٹظ… ظ…ط¹ظٹظ† ظ„ط·ظ„ط§ط¨ ط´ط¹ط¨ط© ظ…ط¹ظٹظ†ط© (ظ„ط±ط³ظ… ط§ظ„ط¬ط¯ظˆظ„)
router.get('/:assessment_id/grades', getAssessmentGrades);

// 3. ط§ط¹طھظ…ط§ط¯ ظˆطھط¹ط¯ظٹظ„ ط§ظ„ط¯ط±ط¬ط§طھ ظ…ظ† ظ‚ط¨ظ„ ط§ظ„ط¥ط¯ط§ط±ط© (ظ…ط¹ ط§ظ„طھط³ط¬ظٹظ„ ط§ظ„ط¢ظ„ظٹ ظپظٹ ط³ط¬ظ„ ط§ظ„طھط¯ظ‚ظٹظ‚)
router.post('/:assessment_id/bulk-override', autoActivityLogger, bulkOverrideGrades);

export default router;

=== adminPermissionsRoutes.js ===
// backend/src/routes/adminPermissionsRoutes.js
import { Router } from "express";
import { listPermissions, decidePermission, overridePermission } from "../controllers/adminPermissionsController.js";

const router = Router();

router.get("/permissions", listPermissions);
router.post("/permissions/:id/decide", decidePermission);
router.post("/permissions/:id/override", overridePermission);

export default router;

=== adminTeacherAttendanceRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { AdminTeacherAttendanceController } from "../controllers/adminTeacherAttendanceController.js";

const router = express.Router();
router.use(authMiddleware);



// ===== Day Summary + Open/Lock =====
router.get("/day", AdminTeacherAttendanceController.daySummary); // ?date=YYYY-MM-DD
router.post("/day/open", AdminTeacherAttendanceController.openDay); // {date}
router.patch("/day/:id/lock", AdminTeacherAttendanceController.lockDay);
router.patch("/day/:id/unlock", AdminTeacherAttendanceController.unlockDay);

// ===== Scan + Manual =====
router.post("/scan", AdminTeacherAttendanceController.scan); // {date, code}
router.post("/entries", AdminTeacherAttendanceController.createEntry); // {date, teacher_id, status, method}
router.patch("/entries/:id", AdminTeacherAttendanceController.updateEntry); // {status, method, reason?}

export default router;

=== adminTeacherPermitsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { AdminTeacherPermitsController } from "../controllers/adminTeacherPermitsController.js";

const router = express.Router();
router.use(authMiddleware);


// list + count
// ظ†ط¸ط§ظ… ط¹ظٹظ† ط§ظ„طµظ‚ط± ظ„ظ„ط­طµطµ ط§ظ„ظ…ط±ظپظˆط¶ط©
router.get("/alerts/rejected-subs", AdminTeacherPermitsController.getRejectedSubsAlerts);
router.get("/", AdminTeacherPermitsController.list); // ?status=&from=&to=&q=&count=1
router.get("/:id", AdminTeacherPermitsController.getOne);
router.patch("/:id/decision", AdminTeacherPermitsController.decide); // {status, decision_note}

export default router;

=== adminTermResultsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getTermResults,
  calculateTermResults,
  approveTermResults,
  publishTermResults,
  unpublishTermResults,
} from "../controllers/adminTermResultsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getTermResults);
router.post("/calculate", calculateTermResults);
router.post("/approve", approveTermResults);
router.post("/publish", publishTermResults);
router.post("/unpublish", unpublishTermResults);

export default router;

=== adminTermWorksRoutes.js ===
import express from "express";
import {
  getTermWorks,
  approveTermWorks,
  returnTermWorks,
} from "../controllers/adminTermWorksController.js";
import {
  listMonthlyAssessments,
  getMonthlyWorks,
  approveMonthlyWorks,
  returnMonthlyWorks,
} from "../controllers/adminMonthlyWorksController.js";
const router = express.Router();

router.get("/monthly-works/assessments", listMonthlyAssessments);
router.get("/monthly-works", getMonthlyWorks);
router.post("/monthly-works/approve", approveMonthlyWorks);
router.post("/monthly-works/return", returnMonthlyWorks);

router.get("/term-works", getTermWorks);
router.post("/term-works/approve", approveTermWorks);
router.post("/term-works/return", returnTermWorks);

export default router;

=== aiChatRoutes.js ===
// import express from "express";
// import { aiChatController } from "../controllers/aiChatController.js";

// const router = express.Router();

// router.post("/", aiChatController.chat); // POST /api/ai-chat

// export default router;

=== assessmentReopenRequestsRoutes.js ===
// src/routes/assessmentReopenRequestsRoutes.js
import { Router } from "express";
import { AssessmentReopenRequestsController } from "../controllers/assessmentReopenRequestsController.js";

const router = Router();

router.get("/", AssessmentReopenRequestsController.list);

router.post("/:id/approve", AssessmentReopenRequestsController.approve);

router.post("/:id/reject", AssessmentReopenRequestsController.reject);

export default router;

=== assignTeachersRoutes.js ===
// src/routes/assignTeachersRoutes.js
import { Router } from "express";
import { meta, sectionView, saveSection } from "../controllers/assignTeachersController.js";

// ط¥ط°ط§ ط¹ظ†ط¯ظƒ authMiddleware/checkPermission ط§ط³طھط®ط¯ظ…ظ‡ظ… ظ‡ظ†ط§
import  authMiddleware  from "../middleware/authMiddleware.js";
// import { checkPermission } from "../middleware/checkPermission.js";

const router = Router();

router.use(authMiddleware);
// router.use(checkPermission("timetables.assign_teachers"));

router.get("/meta", meta);
router.get("/section", sectionView);
router.post("/section", saveSection);

export default router;

=== attendanceReportsRoutes.js ===
// routes/attendanceReportsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط§ط³طھط¯ط¹ط§ط، ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط±
import attendanceReportsController from "../controllers/attendanceReportsController.js";

const router = express.Router();

// âœ… طھط·ط¨ظٹظ‚ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ظ…ط³ط§ط±ط§طھ ط§ظ„طھظ‚ط§ط±ظٹط± ظ„طھظˆظپظٹط± ط¨ظٹط§ظ†ط§طھ ط§ظ„ظ…ط¯ط±ط³ط© (school_id)
router.use(authMiddleware);

// Students
router.get("/attendance/students", attendanceReportsController.getStudentsAttendanceReport);
router.get("/attendance/students/:id/details", attendanceReportsController.getStudentAttendanceDetails);

// Teachers
router.get("/attendance/teachers", attendanceReportsController.getTeachersAttendanceReport);
router.get("/attendance/teachers/:id/details", attendanceReportsController.getTeacherAttendanceDetails);

export default router;

=== authRoutes.js ===
// src/routes/authRoutes.js
import { Router } from "express";
import { AuthController } from "../controllers/authController.js";
import { rateLimiter } from "../middleware/rateLimiter.js";
import jwt from "jsonwebtoken";
import { logAudit } from "../utils/auditLogger.js";

const router = Router();

const loginLimiter = rateLimiter({
  windowMs: 15 * 60 * 1000, // 15 ط¯ظ‚ظٹظ‚ط©
  max: 10, // ط§ظ„ط­ط¯ ط§ظ„ط£ظ‚طµظ‰ 10 ظ…ط­ط§ظˆظ„ط§طھ
  message: "ظ„ظ‚ط¯ ظ‚ظ…طھ ط¨ظ…ط­ط§ظˆظ„ط§طھ طھط³ط¬ظٹظ„ ط¯ط®ظˆظ„ ظƒط«ظٹط±ط© ط¬ط¯ط§ظ‹. ظٹط±ط¬ظ‰ ط§ظ„ظ…ط­ط§ظˆظ„ط© ط¨ط¹ط¯ 15 ط¯ظ‚ظٹظ‚ط©."
});

router.post("/login", loginLimiter, AuthController.login);

router.post("/logout", async (req, res) => {
  try {
    let token = null;
    if (req.headers.cookie) {
      const cookies = {};
      req.headers.cookie.split(";").forEach((cookie) => {
        const parts = cookie.split("=");
        if (parts.length >= 2) {
          cookies[parts[0].trim()] = decodeURIComponent(parts.slice(1).join("="));
        }
      });
      token = cookies.token;
    }
    if (!token) {
      const authHeader = req.headers.authorization || "";
      const match = authHeader.match(/^Bearer\s+(.+)$/i);
      token = match ? match[1]?.trim() : null;
    }
    if (token && process.env.JWT_SECRET) {
      const decoded = jwt.verify(token, process.env.JWT_SECRET);
      if (decoded && decoded.id) {
        await logAudit({
          action: "LOGOUT",
          actionLabel: "طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬",
          module: "Security",
          tableName: "users",
          recordId: decoded.id,
          description: `ظ‚ط§ظ… ط§ظ„ظ…ط³طھط®ط¯ظ… ط¨طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬ ظ…ظ† ط§ظ„ظ†ط¸ط§ظ…`,
          schoolIdFallback: decoded.school_id,
          userIdFallback: decoded.id,
          userNameFallback: decoded.role || "ظ…ط³طھط®ط¯ظ…",
          userRoleFallback: decoded.role || "system"
        });
      }
    }
  } catch (err) {
    // Ignore error, we still want to log out
  }
  res.clearCookie("token");
  return res.json({ success: true, message: "طھظ… طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬ ط¨ظ†ط¬ط§ط­" });
});

export default router;

=== backupRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";
import multer from "multer";
import {
  getSettings,
  updateSettings,
  getLogs,
  runManualBackup,
  downloadBackup,
  deleteBackup,
  browseDirectories,
  restoreBackup,
  getGoogleDriveStatus,      // ًںں¢ ط¥ط¶ط§ظپط© ط§ط³طھظٹط±ط§ط¯ ط¯ط§ظ„ط© ط§ظ„ط­ط§ظ„ط©
  disconnectGoogleDrive      // ًںں¢ ط¥ط¶ط§ظپط© ط§ط³طھظٹط±ط§ط¯ ط¯ط§ظ„ط© ظپطµظ„ ط§ظ„ط­ط³ط§ط¨
} from "../controllers/backupController.js";
const upload = multer({ dest: "uploads/temp/" });
const router = Router();

// طھط£ظ…ظٹظ† ظƒط§ظپط© ظ…ط³ط§ط±ط§طھ ط§ظ„ظ†ط³ط® ط§ظ„ط§ط­طھظٹط§ط·ظٹ ط¨طµظ„ط§ط­ظٹط§طھ ط¥ط¯ط§ط±ظٹط© ظƒط§ظ…ظ„ط© ظ„ظ„ظ…ط¯ط±ط³ط©
router.use(authMiddleware);

router.get("/settings", getSettings);
router.post("/settings", updateSettings);
// ًںں¢ ظ…ط³ط§ط±ط§طھ ط¥ط¯ط§ط±ط© ط§ظ„طھط²ط§ظ…ظ† ط§ظ„ط³ط­ط§ط¨ظٹ ظ…ط¹ ظ‚ظˆظ‚ظ„ ط¯ط±ط§ظٹظپ (BYOD)
router.get("/google-drive-status", getGoogleDriveStatus);
router.post("/google-drive-disconnect", disconnectGoogleDrive);
router.get("/logs", getLogs);
router.post("/run-manual", runManualBackup);
router.get("/download/:id", downloadBackup);
router.get("/browse-directories", browseDirectories);
router.post("/restore", upload.single("backupFile"), restoreBackup);
router.delete("/:id", deleteBackup);

export default router;


=== certificatesRoutes.js ===
// backend/src/routes/certificatesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { CertificatesController } from "../controllers/certificatesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/meta", CertificatesController.meta);

router.get("/:type/students", CertificatesController.students);
router.get("/:type", CertificatesController.list);

router.post("/:type", CertificatesController.create);
router.post("/:type/:id/printed", CertificatesController.markPrinted);

router.delete("/:type/:id", CertificatesController.remove);

export default router;

=== continuingRoutes.js ===
import express from "express";
import {
  getContinuingMeta,
  listContinuingStudents,
  registerContinuingStudents,
} from "../controllers/continuingStudentsController.js";

const router = express.Router();

router.get("/continuing-students/meta", getContinuingMeta);
router.get("/continuing-students/students", listContinuingStudents);
router.post("/continuing-students/register", registerContinuingStudents);

export default router;

=== dashboardRoutes.js ===
// src/routes/dashboardRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";

import {
  getDashboardStats,
  getAdminHomeDashboard,
} from "../controllers/dashboardController.js";

const router = Router();

// ط§ظ„ط¥ط­طµط§ط¦ظٹط§طھ ط§ظ„ظ‚ط¯ظٹظ…ط© ط§ظ„ظ…ظˆط¬ظˆط¯ط© ط­ط§ظ„ظٹظ‹ط§
router.get("/stats", authMiddleware, getDashboardStats);

// ط§ظ„ط¯ط§ط´ط¨ظˆط±ط¯ ط§ظ„ط¬ط¯ظٹط¯ط© ط§ظ„ظƒط§ظ…ظ„ط©
router.get("/admin-home", authMiddleware, getAdminHomeDashboard);

export default router;

=== employeesRoutes.js ===
// src/routes/employeesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  employeesMeta,
  employeesList,
  employeeGet,
  employeeCreate,
  employeeUpdate,
  employeeDelete,
  employeeSetActive,
} from "../controllers/employeesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/meta", employeesMeta);
router.get("/", employeesList);
router.get("/:id", employeeGet);
router.post("/", employeeCreate);
router.put("/:id", employeeUpdate);
router.patch("/:id/active", employeeSetActive);
router.delete("/:id", employeeDelete);

export default router;

=== examTimetablesRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import ExamTimetablesController from "../controllers/examTimetablesController.js";

const router = Router();

// âœ… Meta (dropdowns)
router.get("/meta", authMiddleware, ExamTimetablesController.meta);

// âœ… List (ظ„ط§ط²ظ… ظ‚ط¨ظ„ /:id)
router.get("/list", authMiddleware, ExamTimetablesController.list);

// Get or create timetable header
router.post("/get-or-create", authMiddleware, ExamTimetablesController.getOrCreate);

// Copy from another exam timetable
router.post("/:id/copy-from", authMiddleware, ExamTimetablesController.copyFrom);

// Publish / Unpublish
router.put("/:id/publish", authMiddleware, ExamTimetablesController.publish);
router.put("/:id/unpublish", authMiddleware, ExamTimetablesController.unpublish);

// Save draft entries (bulk replace)
router.put("/:id/entries", authMiddleware, ExamTimetablesController.saveEntries);

// Clear entries
router.delete("/:id/entries", authMiddleware, ExamTimetablesController.clearEntries);

// Delete timetable (draft only)
router.delete("/:id", authMiddleware, ExamTimetablesController.remove);

// Get timetable + entries
router.get("/:id", authMiddleware, ExamTimetablesController.getById);

export default router;

=== feeAdjustmentRequestsRoutes.js ===
// src/routes/feeAdjustmentRequestsRoutes.js
import { Router } from "express";
import { FeeAdjustmentRequestsController } from "../controllers/feeAdjustmentRequestsController.js";

const router = Router();

router.get("/", FeeAdjustmentRequestsController.list);

router.post("/", FeeAdjustmentRequestsController.create);

router.post("/:id/approve", FeeAdjustmentRequestsController.approve);

router.post("/:id/reject", FeeAdjustmentRequestsController.reject);

export default router;

=== feeRulesRoutes.js ===
// src/routes/feeRulesRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";

import {
  listFeeRules,
  createFeeRule,
  updateFeeRule,
  deleteFeeRule,
  listStudentsFeesView,
} from "../controllers/feeRulesController.js";

const router = Router();

// âڑ ï¸ڈ ط¹ط¯ظ‘ظ„ ط§ط³ظ… ط§ظ„طµظ„ط§ط­ظٹط© ط­ط³ط¨ ط¬ط¯ظˆظ„ permissions ط¹ظ†ط¯ظƒ

router.get(
  "/",
  authMiddleware,
  listFeeRules
);

router.get(
  "/students",
  authMiddleware,
  listStudentsFeesView
);

router.post(
  "/",
  authMiddleware,
  createFeeRule
);

router.put(
  "/:id",
  authMiddleware,
  updateFeeRule
);

router.delete(
  "/:id",
  authMiddleware,
  deleteFeeRule
);

export default router;

=== feesRequestsRoutes.js ===
// import express from "express";
// import authMiddleware from "../middleware/authMiddleware.js";
// import { uploadFeesRequestImages } from "../middleware/uploadFeesRequests.js";
// import { FeesRequestsController } from "../controllers/feesRequestsController.js";

// const router = express.Router();

// // ط¥ظ†ط´ط§ط، ط·ظ„ط¨ ط¯ظپط¹ (multipart/form-data) + طµظˆط± ظ…طھط¹ط¯ط¯ط©
// router.post("/fees/requests", authMiddleware, (req, res, next) => {
//   uploadFeesRequestImages(req, res, (err) => {
//     if (err) return res.status(400).json({ message: err.message });
//     next();
//   });
// }, FeesRequestsController.create);

// router.get("/fees/requests", authMiddleware, FeesRequestsController.list);
// router.get("/fees/requests/:id", authMiddleware, FeesRequestsController.read);

// router.post("/fees/requests/:id/approve", authMiddleware, FeesRequestsController.approve);
// router.post("/fees/requests/:id/reject", authMiddleware, FeesRequestsController.reject);

// export default router;

=== feesRoutes.js ===
import { Router } from "express";
import uploadFeesRequests from "../middleware/uploadFeesRequests.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط£ط¶ظپظ†ط§ ظ‡ط°ط§

import {
  getContract,
  createContract,
  updateContract,     // âœ… ط§ط³طھط¯ط¹ط§ط، ط¯ط§ظ„ط© ط§ظ„طھط¹ط¯ظٹظ„ ط§ظ„ط¬ط¯ظٹط¯ط©
  getInstallments,
  getPayments,
  createPayment,
  reportCollections,
  reportOutstanding,
  confirmPayment, 
} from "../controllers/feesController.js";

const router = Router();

// ==========================================
// ًں”’ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ…ط­ظ…ظٹط© ط¨ظ€ authMiddleware
// ==========================================

// ط¥ط¯ط§ط±ط© ط§ظ„ط¹ظ‚ظˆط¯ ظˆط§ظ„ط£ظ‚ط³ط§ط·
router.get("/fees/contract", authMiddleware, getContract);
router.post("/fees/contracts", authMiddleware, createContract);
router.put("/fees/contracts/:id", authMiddleware, updateContract); // âœ… ظ…ط³ط§ط± ط§ظ„طھط¹ط¯ظٹظ„ ظˆط¥ط¹ط§ط¯ط© ط§ظ„ط¬ط¯ظˆظ„ط©

router.get("/fees/installments", authMiddleware, getInstallments);

// ط¥ط¯ط§ط±ط© ط§ظ„ط¯ظپط¹ط§طھ
router.get("/fees/payments", authMiddleware, getPayments);
router.post("/fees/payments", authMiddleware, uploadFeesRequests, createPayment);

// ط§ط¹طھظ…ط§ط¯ ط¯ظپط¹ط© (ظ‚ظٹط¯ ط§ظ„ظ…ط±ط§ط¬ط¹ط© -> ظ…ط¤ظƒط¯)
router.patch("/fees/payments/:id/confirm", authMiddleware, confirmPayment);

// ط§ظ„طھظ‚ط§ط±ظٹط±
router.get("/fees/reports/collections", authMiddleware, reportCollections);
router.get("/fees/reports/outstanding", authMiddleware, reportOutstanding);

export default router;

=== googleAuthRouter.js ===
import { Router } from 'express';
import { google } from 'googleapis';
import jwt from 'jsonwebtoken';
import { pool } from '../config/db.js';

const router = Router();

// ط¯ط§ظ„ط© ظ…ط³ط§ط¹ط¯ط© ظ„طھظ‡ظٹط¦ط© ط§ظ„ط¹ظ…ظٹظ„
function getOAuth2Client() {
  return new google.auth.OAuth2(
    process.env.GOOGLE_CLIENT_ID,
    process.env.GOOGLE_CLIENT_SECRET,
    process.env.GOOGLE_REDIRECT_URI
  );
}

// ًںں¢ 1. ظ…ط³ط§ط± ط§ظ„طھظˆط¬ظٹظ‡ ط¥ظ„ظ‰ طµظپط­ط© ط¬ظˆط¬ظ„ ط§ظ„ط±ط³ظ…ظٹط© ظ„ظ…ظ†ط­ ط§ظ„طµظ„ط§ط­ظٹط§طھ
router.get('/google', async (req, res) => {
  try {
    const token = req.query.token;
    if (!token) return res.status(401).send('ط؛ظٹط± ظ…طµط±ط­: ط§ظ„طھظˆظƒظ† ظ…ظپظ‚ظˆط¯');

    // ظپظƒ طھط´ظپظٹط± ط§ظ„طھظˆظƒظ† ظ„ظ…ط¹ط±ظپط© ظ…ظ† ظ‡ظٹ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„طھظٹ طھط·ظ„ط¨ ط§ظ„ط±ط¨ط· ط§ظ„ط¢ظ†
    // (ط§ط³طھط¨ط¯ظ„ JWT_SECRET ط¨ظ…طھط؛ظٹط± ط§ظ„ط¨ظٹط¦ط© ط§ظ„ط®ط§طµ ط¨ظƒ ظپظٹ ظ…ط´ط±ظˆط¹ظƒ)
    const decoded = jwt.verify(token, process.env.JWT_SECRET || 'secret');
    const schoolId = decoded.school_id; 

    const oauth2Client = getOAuth2Client();

    // طھظˆظ„ظٹط¯ ط±ط§ط¨ط· ط§ظ„طھط­ظ‚ظ‚ ظ…ط¹ ط·ظ„ط¨ طµظ„ط§ط­ظٹط© ط§ظ„ظˆطµظˆظ„ ظ„ظ„ظ…ظ„ظپط§طھ ظˆط·ظ„ط¨ ط§ظ„طھظˆظƒظ† ط§ظ„ط¯ط§ط¦ظ… (offline)
    const authUrl = oauth2Client.generateAuthUrl({
      access_type: 'offline', // ط¥ظ„ط²ط§ظ…ظٹ ظ„ظ„ط­طµظˆظ„ ط¹ظ„ظ‰ ط§ظ„ظ€ Refresh Token ط§ظ„ط«ط§ط¨طھ ظ„ظ„ط±ظپط¹ ط§ظ„طھظ„ظ‚ط§ط¦ظٹ
      prompt: 'consent',     // ط¥ط¬ط¨ط§ط± ظ‚ظˆظ‚ظ„ ط¹ظ„ظ‰ ط¥ط¸ظ‡ط§ط± ط´ط§ط´ط© ط§ظ„ظ…ظˆط§ظپظ‚ط© ظ„طھظˆظ„ظٹط¯ ط§ظ„طھظˆظƒظ†
      scope: ['https://www.googleapis.com/auth/drive.file'], // ط§ظ„ظˆطµظˆظ„ ظپظ‚ط· ظ„ظ„ظ…ظ„ظپط§طھ ط§ظ„طھظٹ ظٹظ†ط´ط¦ظ‡ط§ ط³ظٹط³طھظ…ظƒ ظ„ط­ظ…ط§ظٹط© ط®طµظˆطµظٹطھظ‡ظ…
      state: String(schoolId) // طھظ…ط±ظٹط± ط±ظ‚ظ… ط§ظ„ظ…ط¯ط±ط³ط© ظƒظ…ط¹ط§ظ…ظ„ ط£ظ…ط§ظ† ظ„ظژظ‚ط· ط§ظ„ط­ط§ظ„ط© ط¹ظ†ط¯ ط§ظ„ط¹ظˆط¯ط©
    });

    return res.redirect(authUrl);
  } catch (err) {
    console.error('Google Auth Init Error:', err.message);
    return res.status(500).send('ط­ط¯ط« ط®ط·ط£ ط£ط«ظ†ط§ط، ط¨ط¯ط، ط§ظ„ط§طھطµط§ظ„ ط¨ط³ظٹط±ظپط±ط§طھ ط¬ظˆط¬ظ„');
  }
});

// ًںں¢ 2. ظ…ط³ط§ط± ط§ظ„ط§ط³طھظ‚ط¨ط§ظ„ (Callback URL) ط§ظ„ط°ظٹ ظٹط¹ظˆط¯ ط¥ظ„ظٹظ‡ ط§ظ„ظ…طھطµظپط­ ط¨ط¹ط¯ ظ…ظˆط§ظپظ‚ط© ط§ظ„ط¹ظ…ظٹظ„
router.get('/google/callback', async (req, res) => {
  const { code, state } = req.query; // code = ط´ظپط±ط© ط¬ظˆط¬ظ„طŒ state = ط±ظ‚ظ… ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ظ…ظ…ط±ط± ط³ط§ط¨ظ‚ط§ظ‹
  const schoolId = parseInt(state, 10);

  if (!code || !schoolId) {
    return res.status(400).send('ط¨ظٹط§ظ†ط§طھ ط§ظ„طھط­ظ‚ظ‚ ظ…ظ† ط¬ظˆط¬ظ„ ط؛ظٹط± ظ…ظƒطھظ…ظ„ط©');
  }

  try {
    const oauth2Client = getOAuth2Client();
    
    // طھط¨ط§ط¯ظ„ ط§ظ„ط´ظپط±ط© ط§ظ„ظ…ط¤ظ‚طھط© ط¨ط§ظ„طھظˆظƒظ†ط§طھ ط§ظ„ط¯ط§ط¦ظ…ط© ظ…ظ† ط¬ظˆط¬ظ„
    const { tokens } = await oauth2Client.getToken(code);
    
    if (!tokens.refresh_token) {
      throw new Error('ظ„ظ… ظٹطھظ… ط¥ط±ط¬ط§ط¹ Refresh Token. ظٹط±ط¬ظ‰ ط¥ظ„ط؛ط§ط، ط§ظ„ط±ط¨ط· ظˆط¥ط¹ط§ط¯ط© ط§ظ„ظ…ط­ط§ظˆظ„ط© ظ…ط¬ط¯ط¯ط§ظ‹.');
    }

    // طھظ‡ظٹط¦ط© ط¹ظ…ظٹظ„ ط§ظ„ظ€ Drive ظ„ط¥ظ†ط´ط§ط، ظ…ط¬ظ„ط¯ ظ…ط¹ط²ظˆظ„ ظ„ظ„ظ…ط¯ط±ط³ط© ظپظˆط±ط§ظ‹
    oauth2Client.setCredentials(tokens);
    const drive = google.drive({ version: 'v3', auth: oauth2Client });

    // ط¬ظ„ط¨ ط§ط³ظ… ط§ظ„ظ…ط¯ط±ط³ط© ظ…ظ† ط§ظ„ط¯ط§طھط§ط¨ظٹط² ظ„طھط³ظ…ظٹط© ط§ظ„ظ…ط¬ظ„ط¯ ط¨ط§ط³ظ… ظ…ظ†ط´ط£طھظ‡ظ… ط¨ط´ظƒظ„ ط§ط­طھط±ط§ظپظٹ
    const schoolRes = await pool.query('SELECT name_ar FROM schools WHERE id = $1', [schoolId]);
    const schoolName = schoolRes.rows[0]?.name_ar || `School_${schoolId}`;

    // ط¥ط¹ط¯ط§ط¯ ط¨ظٹط§ظ†ط§طھ ط§ظ„ظ…ط¬ظ„ط¯ ط§ظ„ظپط±ظٹط¯ ظ„ظ„ظ…ط³طھط£ط¬ط±
    const folderMetadata = {
      name: `ظ…ظ†طµطھظٹ ط§ظ„طھط¹ظ„ظٹظ…ظٹط© - ظ†ط³ط® ط§ط­طھظٹط§ط·ظٹ (${schoolName})`,
      mimeType: 'application/vnd.google-apps.folder'
    };

    const folder = await drive.files.create({
      requestBody: folderMetadata,
      fields: 'id'
    });

    const folderId = folder.data.id; // ظ…ط¹ط±ظ‘ظپ ط§ظ„ظ…ط¬ظ„ط¯ ط§ظ„ظپط±ظٹط¯ ط§ظ„ظ…ط´ظپط± ظ…ظ† ط¬ظˆط¬ظ„

    // ط­ظپط¸ ط§ظ„طھظˆظƒظ† ظˆظ…ط¹ط±ظ‘ظپ ط§ظ„ظ…ط¬ظ„ط¯ ظپظٹ ط¬ط¯ظˆظ„ ط§ظ„ط¥ط¹ط¯ط§ط¯ط§طھ ط§ظ„ط®ط§طµ ط¨ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط©
    await pool.query(
      `INSERT INTO backup_settings (school_id, google_drive_refresh_token, google_drive_folder_id, updated_at)
       VALUES ($1, $2, $3, NOW())
       ON CONFLICT (school_id) DO UPDATE 
       SET google_drive_refresh_token = EXCLUDED.google_drive_refresh_token,
           google_drive_folder_id = EXCLUDED.google_drive_folder_id,
           updated_at = NOW()`,
      [schoolId, tokens.refresh_token, folderId]
    );

    // ًںژ¯ ط¥ط¹ط§ط¯ط© طھظˆط¬ظٹظ‡ ط§ظ„ظ…ط³طھط®ط¯ظ… ظ„طµظپط­ط© ط§ظ„ط¥ط¹ط¯ط§ط¯ط§طھ ظ„ظˆط­ط© ط§ظ„طھط­ظƒظ… ظپظٹ ط§ظ„ظپط±ظˆظ†طھ ط¥ظ†ط¯ ط¨ظ†ط¬ط§ط­
    // (ظ‚ظ… ط¨طھط­ط¯ظٹط« ط§ظ„ظ…ط³ط§ط± ط§ظ„ظ†طµظٹ ظ„ظٹطھط·ط§ط¨ظ‚ ظ…ط¹ ظ…ط¬ظ„ط¯ط§طھ ط§ظ„ط¹ط±ط¶ ظ„ط¯ظٹظƒ)
    return res.redirect('/frontend/settings/settings.html?tab=backups&status=success');

  } catch (err) {
    console.error('Google OAuth Callback Error:', err.message);
    return res.redirect(`/frontend/settings/settings.html?tab=backups&status=error&msg=${encodeURIComponent(err.message)}`);
  }
});

export default router;

=== gradeRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import loadPermissions from "../middleware/loadPermissions.js";
import checkPermission from "../middleware/checkPermission.js";
import { getGrades } from "../controllers/gradeController.js";

const router = Router();

router.get(
  "/",
  authMiddleware,
  loadPermissions,
  // checkPermission("grades.view"),
  getGrades
);

export default router;

=== metaRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getAcademicYears,
  getGrades,
  getClasses,
  searchStudents,
} from "../controllers/metaController.js";

const router = Router();

router.use(authMiddleware);

router.get("/academic-years", getAcademicYears);
router.get("/grades", getGrades);
router.get("/classes", getClasses);
router.get("/students/search", searchStudents);

export default router;

=== moduleRoutes.js ===
import express from "express";
import {
  addModule,
  fetchModules,
  fetchModuleById,
  editModule,
  removeModule,
} from "../controllers/moduleController.js";
import authMiddleware from "../middleware/authMiddleware.js";
import loadPermissions from "../middleware/loadPermissions.js";
import checkPermission from "../middleware/checkPermission.js";

const router = express.Router();

/**
 * ظ…ظ„ط§ط­ط¸ط© ظ…ظ‡ظ…ط©:
 * طµظپط­ط© "ط§ظ„ط£ط¯ظˆط§ط±" ظˆ "ط§ظ„طµظ„ط§ط­ظٹط§طھ" طھط­طھط§ط¬ ظپظ‚ط· ط£ظ† طھظ‚ط±ط£ ظ‚ط§ط¦ظ…ط© ط§ظ„ظˆط­ط¯ط§طھطŒ
 * ط¹ط´ط§ظ† طھط¹ط¨ظٹ ط§ظ„ظ€ select ظپظٹ ط§ظ„ظˆط§ط¬ظ‡ط©طŒ ظ…ظˆ ط¨ط§ظ„ط¶ط±ظˆط±ط© ظٹط¹ط¯ظ‘ظ„ ط§ظ„ظˆط­ط¯ط§طھ.
 *
 * ظ„ط°ظ„ظƒ:
 *  - GET /modules: ظ†ط³ظ…ط­ ط¨ظ‡ ظ„ط£ظٹ ط¯ظˆط± ط¹ظ†ط¯ظ‡ ظˆط§ط­ط¯ط© ظ…ظ† طµظ„ط§ط­ظٹط§طھ RBAC.
 *  - POST/PUT/DELETE: ظ†ط±ط¨ط·ظ‡ط§ ط¨طµظ„ط§ط­ظٹط© rbac.manage_modules ظپظ‚ط·.
 */

// ظ…ط³ط§ط¹ط¯ط©: ط¯ط§ظ„ط© ظ„ظپط­طµ ط£ظٹ طµظ„ط§ط­ظٹط© ظ…ظ† ظ…ط¬ظ…ظˆط¹ط©
import PermissionRoleModel from "../modules/permissionRoleModel.js";

function checkAnyPermission(codes = []) {
  return async function (req, res, next) {
    try {
      const user = req.user;
      if (!user) {
        return res.status(401).json({ message: "ط؛ظٹط± ظ…طµط±ط­" });
      }

      const roleName = String(user.role || user.role_name || "").toLowerCase();

      // ط§ظ„ط£ط¯ظ…ظ€ظ† ط§ظ„ظƒط§ظ…ظ„ ظٹطھط®ط·ظ‰ ط§ظ„ظپط­طµ
      if (
        roleName === "admin" ||
        roleName === "superadmin"
      ) {
        return next();
      }

      // ظ„ظˆ ط¹ظ†ط¯ظ‡ user.permissions ط¬ط±ط¨ ظ†ط¨ط­ط« ظپظٹظ‡ط§
      if (Array.isArray(user.permissions)) {
        const hasFromArray = codes.some((c) =>
          user.permissions.includes(c)
        );
        if (hasFromArray) return next();
      }

      // ظپط­طµ ظ…ظ† ظ‚ط§ط¹ط¯ط© ط§ظ„ط¨ظٹط§ظ†ط§طھ: ظ‡ظ„ ط§ظ„ط¯ظˆط± ظٹظ…ظ„ظƒ ط£ظٹ ظ…ظ† ظ‡ط°ظ‡ ط§ظ„طµظ„ط§ط­ظٹط§طھطں
      const hasAny = await PermissionRoleModel.roleHasAnyPermission(
        user.role_id,
        codes
      );
      if (!hasAny) {
        return res
          .status(403)
          .json({ message: "ظ„ظٹط³ ظ„ط¯ظٹظƒ طµظ„ط§ط­ظٹط© ظ„طھظ†ظپظٹط° ظ‡ط°ط§ ط§ظ„ط¥ط¬ط±ط§ط،" });
      }

      return next();
    } catch (err) {
      console.error("checkAnyPermission error:", err);
      return res
        .status(500)
        .json({ message: "ط®ط·ط£ ظپظٹ ط§ظ„طھط­ظ‚ظ‚ ظ…ظ† طµظ„ط§ط­ظٹط§طھ ط§ظ„ظ…ط³طھط®ط¯ظ…" });
    }
  };
}

// ======================= ط§ظ„ط±ط§ظˆطھط§طھ =======================

// ط¹ط±ط¶ ظƒظ„ ط§ظ„ظˆط­ط¯ط§طھ (ظ‚ط±ط§ط،ط© ظپظ‚ط·)
// ظٹظƒظپظٹ ط£ظ† ظٹظƒظˆظ† ط¹ظ†ط¯ظ‡ ط£ظٹ طµظ„ط§ط­ظٹط© ظ…ظ† طµظ„ط§ط­ظٹط§طھ RBAC
router.get(
  "/",
  authMiddleware,
  loadPermissions,
  checkAnyPermission([
    "rbac.manage_modules",
    "rbac.manage_permissions",
    "rbac.manage_roles",
    "rbac.manage_users",
  ]),
  fetchModules
);

// ط¥ظ†ط´ط§ط، ظˆط­ط¯ط©
router.post(
  "/",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  addModule
);

// ط¬ظ„ط¨ ظˆط­ط¯ط© ظˆط§ط­ط¯ط©
router.get(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  fetchModuleById
);

// طھط¹ط¯ظٹظ„ ظˆط­ط¯ط©
router.put(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  editModule
);

// ط­ط°ظپ ظˆط­ط¯ط©
router.delete(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  removeModule
);

export default router;

=== monthlyCertificatesRoutes.js ===
// backend/src/routes/monthlyCertificatesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { MonthlyCertificatesController } from "../controllers/monthlyCertificatesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/meta", MonthlyCertificatesController.meta);
router.get("/students", MonthlyCertificatesController.students);
router.get("/", MonthlyCertificatesController.list);

router.post("/", MonthlyCertificatesController.create);
router.post("/:id/printed", MonthlyCertificatesController.markPrinted);

router.delete("/:id", MonthlyCertificatesController.remove);

export default router;

=== notificationsAdminRoutes.js ===
// backend/src/routes/notificationsAdminRoutes.js
import express from "express";
import {
  previewRecipients,
  sendManual,
  listSentLog,
  sentLogDetails,
  lookupStagesHandler,
  lookupGradesHandler,
  lookupSectionsHandler,
  lookupStudentsHandler,
  lookupTeachersHandler,
  lookupGuardiansHandler,
} from "../controllers/notificationsAdminController.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط¶ط±ظˆط±ظٹ ط¬ط¯ط§ظ‹ ظ„ط§ط³طھط®ط±ط§ط¬ school_id
import checkPermission from "../middleware/checkPermission.js"; // âœ… ط§ط®طھظٹط§ط±ظٹ ط­ط³ط¨ ظ†ط¸ط§ظ…ظƒ
import multer from "multer";
import fs from "fs";
import path from "path";
import crypto from "crypto";

const router = express.Router();

// --- ط¥ط¹ط¯ط§ط¯ط§طھ ط±ظپط¹ ط§ظ„ظ…ظ„ظپط§طھ (ط§ظ„ظ…ط±ظپظ‚ط§طھ) ---
const UPLOAD_DIR = path.join(process.cwd(), "uploads", "notifications");
if (!fs.existsSync(UPLOAD_DIR)) {
  fs.mkdirSync(UPLOAD_DIR, { recursive: true });
}

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => cb(null, UPLOAD_DIR),
  filename: (_req, file, cb) => {
    const ext = path.extname(file.originalname || "");
    // طھط³ظ…ظٹط© ظپط±ظٹط¯ط© ظ„ظ…ظ†ط¹ طھط¯ط§ط®ظ„ ط§ظ„ظ…ظ„ظپط§طھ
    cb(null, `${Date.now()}-${crypto.randomBytes(6).toString("hex")}${ext}`);
  },
});

const upload = multer({
  storage,
  limits: { files: 10, fileSize: 25 * 1024 * 1024 }, // ط­ط¯ ط£ظ‚طµظ‰ 25 ظ…ظٹط¬ط§
});

/**
 * ًں”’ ط­ظ…ط§ظٹط© ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ:
 * ط¬ظ…ظٹط¹ ظ‡ط°ظ‡ ط§ظ„ط¹ظ…ظ„ظٹط§طھ طھطھط·ظ„ط¨ ط£ظ† ظٹظƒظˆظ† ط§ظ„ظ…ط³طھط®ط¯ظ… ظ…ط³ط¬ظ„ ط¯ط®ظˆظ„
 * ظ„ظƒظٹ ظٹطھظ…ظƒظ† ط§ظ„ظ†ط¸ط§ظ… ظ…ظ† ظ…ط¹ط±ظپط© ظ…ط¯ط±ط³ط© ط§ظ„ظ…ط³طھط®ط¯ظ… (req.user.school_id)
 */

// ===== Lookups ظ„ظ„ظˆط§ط¬ظ‡ط© (ط§ظ„ط¨ط­ط« ط§ظ„ظ…ط®طµطµ ظ„ظ„ظ…ط¯ط±ط³ط©) =====
router.get("/lookups/stages", authMiddleware, lookupStagesHandler);
router.get("/lookups/grades", authMiddleware, lookupGradesHandler);
router.get("/lookups/sections", authMiddleware, lookupSectionsHandler);
router.get("/lookups/students", authMiddleware, lookupStudentsHandler);
router.get("/lookups/teachers", authMiddleware, lookupTeachersHandler);
router.get("/lookups/guardians", authMiddleware, lookupGuardiansHandler);

// ===== ط¹ظ…ظ„ظٹط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ ظˆط§ظ„ظ…ط¹ط§ظٹظ†ط© =====

// ظ…ط¹ط§ظٹظ†ط© ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† (ظٹطھط·ظ„ط¨ طµظ„ط§ط­ظٹط© ط§ظ„ط¥ط±ط³ط§ظ„)
router.post(
  "/preview-recipients", 
  authMiddleware, 
  // checkPermission?.("notifications.send"), // ط§ط®طھظٹط§ط±ظٹ: طھط£ظƒط¯ ط£ظ† ط§ظ„ط¯ط§ظ„ط© ظ…ط¯ط¹ظˆظ…ط© ط¹ظ†ط¯ظƒ
  previewRecipients
);

// ط¥ط±ط³ط§ظ„ ط¥ط´ط¹ط§ط± ظٹط¯ظˆظٹ ظ…ط¹ ط§ظ„ظ…ط±ظپظ‚ط§طھ
router.post(
  "/send", 
  authMiddleware, 
  // checkPermission?.("notifications.send"), 
  upload.array("files", 10), // ط§ط³طھظ‚ط¨ط§ظ„ ط­طھظ‰ 10 ظ…ظ„ظپط§طھ
  sendManual
);

// ===== ط³ط¬ظ„ط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ (Sent Log) =====

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط±ط³ظ„ط© ظ…ظ† ظ‚ط¨ظ„ ط§ظ„ظ…ط¯ط±ط³ط©
router.get(
  "/sent-log", 
  authMiddleware, 
  // checkPermission?.("notifications.sent_log.view"), 
  listSentLog
);

// طھظپط§طµظٹظ„ ط¥ط´ط¹ط§ط± ظ…ط±ط³ظ„ ظ…ط­ط¯ط¯ ظ…ط¹ ط­ط§ظ„ط© ط§ظ„ظ‚ط±ط§ط،ط©
router.get(
  "/sent-log/:id", 
  authMiddleware, 
  // checkPermission?.("notifications.sent_log.view"), 
  sentLogDetails
);

export default router;

=== notificationsInboxRoutes.js ===
// backend/src/routes/notificationsInboxRoutes.js
import { Router } from "express";
import {
  listInbox,
  unreadCount,
  markRead,
  markAllRead,
} from "../controllers/notificationsInboxController.js";

const router = Router();

/**
 * ًں”’ طµظ†ط¯ظˆظ‚ ط§ظ„ظˆط§ط±ط¯ ظ„ظ„ظ…ط³طھط®ط¯ظ…ظٹظ† (Inbox)
 * ظ…ظ„ط§ط­ط¸ط©: authMiddleware ظ…ط¶ط§ظپ ظپظٹ app.js ط¹ظ†ط¯ ط§ظ„ط±ط¨ط·طŒ 
 * ظˆظ‡ظˆ ط§ظ„ظ…ط³ط¤ظˆظ„ ط¹ظ† ط­ظ‚ظ† req.user.school_id ظ„ط¶ظ…ط§ظ† ط¹ط²ظ„ ط§ظ„ط¨ظٹط§ظ†ط§طھ ط¨ظٹظ† ط§ظ„ظ…ط¯ط§ط±ط³.
 */

// 1ï¸ڈâƒ£ ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ (ظ…ط¹ ط§ظ„ظپظ„طھط±ط© ظˆط§ظ„ط¨ط­ط« ظˆط§ظ„ظ…ط±ظپظ‚ط§طھ)
// GET /api/notifications/inbox?filter=unread&q=search_term
router.get("/inbox", listInbox);

// 2ï¸ڈâƒ£ ط¬ظ„ط¨ ط¹ط¯ط¯ ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط،ط© ظپظ‚ط· (ظ„ط¥ط¸ظ‡ط§ط± ط§ظ„طھظ†ط¨ظٹظ‡ط§طھ ظپظٹ ط§ظ„ظˆط§ط¬ظ‡ط©)
// GET /api/notifications/inbox/unread-count
router.get("/inbox/unread-count", unreadCount);

// 3ï¸ڈâƒ£ طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظ…ط­ط¯ط¯ ظƒظ…ظ‚ط±ظˆط، (ط¨ط§ط³طھط®ط¯ط§ظ… ظ…ط¹ط±ظپ ط³ط¬ظ„ ط§ظ„ط§ط³طھظ„ط§ظ…)
// PATCH /api/notifications/inbox/123/read
router.patch("/inbox/:recipientRowId/read", markRead);

// 4ï¸ڈâƒ£ طھط¹ظ„ظٹظ… ط¬ظ…ظٹط¹ ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط³طھط®ط¯ظ… ظپظٹ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ط­ط§ظ„ظٹط© ظƒظ…ظ‚ط±ظˆط،ط©
// PATCH /api/notifications/inbox/read-all
router.patch("/inbox/read-all", markAllRead);

export default router;

=== parentAttendanceRoutes.js ===
// backend/src/routes/parentAttendanceRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { ParentAttendanceController } from "../controllers/parentAttendanceController.js";

const router = express.Router();
router.use(authMiddleware);

// /api/parent/attendance/today?studentId=..&date=YYYY-MM-DD&term=1&yearId=1
router.get("/today", ParentAttendanceController.today);

// /api/parent/attendance/week?studentId=..&end=YYYY-MM-DD&term=1&yearId=1
router.get("/week", ParentAttendanceController.week);
router.get("/range", ParentAttendanceController.range);

export default router;

=== parentFeesPortalRoutes.js ===
import { Router } from "express";
import uploadFeesRequests from "../middleware/uploadFeesRequests.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط§ط³طھط¯ط¹ط§ط، ط§ظ„ظ…ظٹط¯ظ„ظˆظٹط±
import { parentFeesOverview, parentPaymentRequest } from "../controllers/parentFeesPortalController.js";

const router = Router();

// âœ… طھط·ط¨ظٹظ‚ ط§ظ„ط­ظ…ط§ظٹط© (authMiddleware) ط¹ظ„ظ‰ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ„ط¶ظ…ط§ظ† ظˆط¬ظˆط¯ ظ…ط¹ط±ظپ ط§ظ„ظ…ط³طھط®ط¯ظ… ظˆط§ظ„ظ…ط¯ط±ط³ط©
router.get("/parent/fees/overview", authMiddleware, parentFeesOverview);
router.post("/parent/fees/payment-request", authMiddleware, uploadFeesRequests, parentPaymentRequest);

export default router;

=== parentGradesRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { getParentChildAssessmentGrades } from "../controllers/parentGradesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getParentChildAssessmentGrades);

export default router;

=== parentNotificationsRoutes.js ===
// backend/src/routes/parentNotificationsRoutes.js
import express from "express";
import {
  listChildren,
  childTeachers,
  getUnreadCount,
  listInbox,
  markOneRead,
  markAllRead,
  listOutbox,
  outboxRecipients,
  sendAdmins,
  sendChildren,
  sendTeachers,
} from "../controllers/parentNotificationsController.js";

// âœ… ط§ط³طھظٹط±ط§ط¯ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ط§ظ„ط®ط§طµ ط¨ط§ظ„طھظˆط«ظٹظ‚ ظ„ط¶ظ…ط§ظ† ظˆط¬ظˆط¯ req.user.school_id
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

/**
 * ًں”’ ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ
 * طھط·ط¨ظٹظ‚ authMiddleware ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„طھط§ظ„ظٹط© ظ„ط¶ظ…ط§ظ† ط¹ط²ظ„ ط§ظ„ط¨ظٹط§ظ†ط§طھ
 * ط¨ظ†ط§ط،ظ‹ ط¹ظ„ظ‰ ط±ظ‚ظ… ط§ظ„ظ…ط¯ط±ط³ط© (school_id) ط§ظ„ظ…ظˆط¬ظˆط¯ ظپظٹ ط§ظ„طھظˆظƒظ†.
 */
router.use(authMiddleware);

// ==========================================
// ًں”چ ظ…ط³ط§ط¹ط¯ط§طھ ط§ظ„ظˆط§ط¬ظ‡ط© (Lookups)
// ==========================================

// ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ط§ظ„ط£ط¨ظ†ط§ط، ط§ظ„ظ…ط±طھط¨ط·ظٹظ† ط¨ظˆظ„ظٹ ط§ظ„ط£ظ…ط± (ظپظٹ ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط© ظپظ‚ط·)
router.get("/children", listChildren);

// ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ظ…ط¹ظ„ظ…ظٹظ† ط§ط¨ظ† ظ…ط­ط¯ط¯ (ظ„ظ„ط³ظ†ط© ط§ظ„ط¯ط±ط§ط³ظٹط© ط§ظ„ط­ط§ظ„ظٹط© ظپظٹ ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط©)
router.get("/children/:studentId/teachers", childTeachers);

// ==========================================
// ًں“¥ طµظ†ط¯ظˆظ‚ ط§ظ„ظˆط§ط±ط¯ (Inbox)
// ==========================================

// ط¹ط¯ط¯ ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط،ط© ظ„ظˆظ„ظٹ ط§ظ„ط£ظ…ط±
router.get("/unread-count", getUnreadCount);

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط³طھظ„ظ…ط© (ظ…ط¹ ط¯ط¹ظ… ط§ظ„ظپظ„طھط±ط© ظˆط§ظ„ط¨ط­ط«)
router.get("/inbox", listInbox);

// طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظˆط§ط­ط¯ ظƒظ…ظ‚ط±ظˆط،
router.patch("/inbox/:id/read", markOneRead);

// طھط¹ظ„ظٹظ… ط¬ظ…ظٹط¹ ط¥ط´ط¹ط§ط±ط§طھ ظ…ط¯ط±ط³ط© ظˆظ„ظٹ ط§ظ„ط£ظ…ط± ظƒظ…ظ‚ط±ظˆط،ط©
router.patch("/inbox/read-all", markAllRead);

// ==========================================
// ًں“¤ طµظ†ط¯ظˆظ‚ ط§ظ„طµط§ط¯ط± (Outbox)
// ==========================================

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„طھظٹ ط£ط±ط³ظ„ظ‡ط§ ظˆظ„ظٹ ط§ظ„ط£ظ…ط±
router.get("/outbox", listOutbox);

// طھظپط§طµظٹظ„ ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† ظ„ط¥ط´ط¹ط§ط± ظ…ط±ط³ظ„ (ظ„ظ…ط¹ط±ظپط© ظ…ظ† ظ‚ط±ط£ ط§ظ„ط±ط³ط§ظ„ط©)
router.get("/outbox/:id/recipients", outboxRecipients);

// ==========================================
// âœ‰ï¸ڈ ط¹ظ…ظ„ظٹط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ (Send)
// ==========================================

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ط¥ط¯ط§ط±ط© ط§ظ„ظ…ط¯ط±ط³ط©
router.post("/send/admins", sendAdmins);

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ط§ظ„ط£ط¨ظ†ط§ط، (ظ„ط­ط³ط§ط¨ط§طھ ط§ظ„ط·ظ„ط§ط¨)
router.post("/send/children", sendChildren);

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ظ…ط¹ظ„ظ…ظٹ ط§ظ„ط£ط¨ظ†ط§ط،
router.post("/send/teachers", sendTeachers);

export default router;

=== parentPermissionsRoutes.js ===
// backend/src/routes/parentPermissionsRoutes.js
import { Router } from "express";
import {
  createPermission,
  getPermissionForDay,
} from "../controllers/parentPermissionsController.js";

const router = Router();

// /api/parent/permissions
router.get("/permissions", getPermissionForDay);
router.post("/permissions", createPermission);

export default router;

=== parentPortalRoutes.js ===
// src/routes/parentPortalRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getParentMe,
  getParentMeta,
  getChildTimetable,
  getParentExamsMeta,
  getChildExams,
} from "../controllers/parentPortalController.js";

const router = express.Router();

router.get("/me", authMiddleware, getParentMe);
router.get("/meta", authMiddleware, getParentMeta);
router.get("/timetable", authMiddleware, getChildTimetable);

// âœ… Exams for parent
router.get("/exams/meta", authMiddleware, getParentExamsMeta);
router.get("/exams", authMiddleware, getChildExams);

export default router;

=== parentRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { ParentsController } from "../controllers/parentsController.js";
import { getChildActivities } from "../controllers/parentLearningController.js"; 

const router = express.Router();

// ًں”’ ط­ظ…ط§ظٹط© ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ (ظٹط¬ط¨ ط£ظ† ظٹظƒظˆظ† ط§ظ„ظ…ط³طھط®ط¯ظ… ظ…ط³ط¬ظ„ط§ظ‹)
router.use(authMiddleware);

// ==========================================
// âœ… ظ…ط³ط§ط±ط§طھ ط§ظ„ط¥ط¯ط§ط±ط© (ظ„ظ„ط¨ط­ط« ط¹ظ† ط£ظˆظ„ظٹط§ط، ط§ظ„ط£ظ…ظˆط±)
// ==========================================
router.get("/search", ParentsController.search);
router.get("/", ParentsController.list);
router.get("/:id", ParentsController.getById);

// ==========================================
// âœ… ظ…ط³ط§ط±ط§طھ ط¨ظˆط§ط¨ط© ظˆظ„ظٹ ط§ظ„ط£ظ…ط± (Parent Portal)
// ==========================================
// ط§ظ„ظ…ط³ط§ط± ط§ظ„ط®ط§طµ ط¨ظ…ظ‡ط§ظ… ظˆط£ظ†ط´ط·ط© ط§ظ„ط§ط¨ظ†
router.get("/children/:childId/activities", getChildActivities);

export default router;

=== parentsRoutes.js ===
// src/routes/parentsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import ParentsController from "../controllers/parentsController.js";

const router = express.Router();

// âœ… endpoints ظˆط§ط¶ط­ط©
router.get("/search", authMiddleware, ParentsController.search);
router.get("/", authMiddleware, ParentsController.list);

// âœ… Route ط°ظƒظٹ: ظ„ظˆ ط±ظ‚ظ… => getById طŒ ظ„ظˆ ظ†طµ => search ط¨ط¯ظ„ ط®ط·ط£ Invalid id
router.get("/:id", authMiddleware, (req, res) => {
  const raw = String(req.params.id || "").trim();

  // ط±ظ‚ظ…طں ط±ظˆط­ getById
  if (/^\d+$/.test(raw)) {
    return ParentsController.getById(req, res);
  }

  // ظ†طµطں ط§ط¹طھط¨ط±ظ‡ ط¨ط­ط« (ط­طھظ‰ ظ„ظˆ ط¬ط§ط، ط¨ط§ظ„ط؛ظ„ط· ظ…ظ† ط£ظٹ ط³ظƒط±ط¨طھ)
  req.query.q = raw; // ظٹط¯ط¹ظ… search() ظ…ط¨ط§ط´ط±ط©
  return ParentsController.search(req, res);
});

export default router;

=== parentTermResultsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { getParentChildTermResults } from "../controllers/parentTermResultsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getParentChildTermResults);

export default router;

=== periodsRoutes.js ===
// src/routes/periodsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { listPeriods, createPeriod, updatePeriod, deletePeriod } from "../controllers/periodsController.js";

const router = express.Router();

router.get("/", authMiddleware, listPeriods);
router.post("/", authMiddleware, createPeriod);
router.put("/:id", authMiddleware, updatePeriod);
router.delete("/:id", authMiddleware, deletePeriod);

export default router;

=== permissionRoleRoutes.js ===
import { Router } from "express";
import { PermissionRoleController } from "../controllers/permissionRoleController.js";
import adminOnly from "../middleware/adminOnly.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط£ط¶ظپظ†ط§ ط§ط³طھظٹط±ط§ط¯ ظ…طµط§ط¯ظ‚ط© ط§ظ„ط¯ط®ظˆظ„

const router = Router();

// ط§ط³طھط®ط¯ط§ظ… authMiddleware ظ„ظ„طھط£ظƒط¯ ظ…ظ† ظ‡ظˆظٹط© ط§ظ„ظ…ط¯ط±ط³ط©
router.use(authMiddleware);

// ط¥ظ†ط´ط§ط، ط¹ظ„ط§ظ‚ط©
router.post("/assign", PermissionRoleController.assign);

// ظƒظ„ ط§ظ„ط¹ظ„ط§ظ‚ط§طھ
router.get("/", PermissionRoleController.getAll);

// ط­ط³ط¨ ط§ظ„ط¯ظˆط±
router.get("/:role_id", PermissionRoleController.getByRole);

// ط­ط°ظپ ط¹ظ„ط§ظ‚ط©
router.delete("/:id", PermissionRoleController.delete);

export default router;

=== permissionRoutes.js ===
import { Router } from "express";
import { PermissionController } from "../controllers/permissionController.js";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";

const router = Router();

// ًں”’ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظٹط¬ط¨ ط£ظ† طھظƒظˆظ† ظ…ط­ظ…ظٹط© ط¨طھط³ط¬ظٹظ„ ط§ظ„ط¯ط®ظˆظ„
router.use(authMiddleware);

// ًں‘پï¸ڈ ط¹ط±ط¶ ط§ظ„طµظ„ط§ط­ظٹط§طھ (ظ…ط³ظ…ظˆط­ ظ„ط£ظٹ ط´ط®طµ ظٹظ…ظ„ظƒ طµظ„ط§ط­ظٹط© ط¥ط¯ط§ط±ط© ط§ظ„ط£ط¯ظˆط§ط± ظ„ظٹطھظ…ظƒظ† ظ…ظ† ط±ط¤ظٹط© ط§ظ„ظ‚ط§ط¦ظ…ط©)
router.get("/", PermissionController.getPermissions);
router.get("/:id", PermissionController.getPermission);

// âڑ ï¸ڈ ط¥ظ†ط´ط§ط،طŒ طھط¹ط¯ظٹظ„طŒ ظˆط­ط°ظپ طµظ„ط§ط­ظٹط© (ط¹ظ…ظ„ظٹط§طھ ط­ط³ط§ط³ط© ظˆظ…ط­ظƒظˆظ…ط© ط¨طµظ„ط§ط­ظٹط© ط¥ط¯ط§ط±ط© ط§ظ„طµظ„ط§ط­ظٹط§طھ)
router.post("/", checkPermission("rbac.manage_permissions"), PermissionController.createPermission);
router.put("/:id", checkPermission("rbac.manage_permissions"), PermissionController.updatePermission);
router.delete("/:id", checkPermission("rbac.manage_permissions"), PermissionController.deletePermission);

export default router;

=== platformAuthRoutes.js ===
import express from "express";
import {
  loginPlatformAdmin,
  getPlatformMe,
} from "../controllers/platformAuthController.js";
import platformAuthMiddleware from "../middleware/platformAuthMiddleware.js";
import { rateLimiter } from "../middleware/rateLimiter.js";

const router = express.Router();

const platformLoginLimiter = rateLimiter({
  windowMs: 15 * 60 * 1000, // 15 ط¯ظ‚ظٹظ‚ط©
  max: 10, // ط§ظ„ط­ط¯ ط§ظ„ط£ظ‚طµظ‰ 10 ظ…ط­ط§ظˆظ„ط§طھ
  message: "ظ„ظ‚ط¯ ظ‚ظ…طھ ط¨ظ…ط­ط§ظˆظ„ط§طھ طھط³ط¬ظٹظ„ ط¯ط®ظˆظ„ ظƒط«ظٹط±ط© ط¬ط¯ط§ظ‹. ظٹط±ط¬ظ‰ ط§ظ„ظ…ط­ط§ظˆظ„ط© ط¨ط¹ط¯ 15 ط¯ظ‚ظٹظ‚ط©."
});

router.post("/login", platformLoginLimiter, loginPlatformAdmin);
router.get("/me", platformAuthMiddleware, getPlatformMe);

router.post("/logout", (req, res) => {
  res.clearCookie("token");
  return res.json({ success: true, message: "طھظ… طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬ ط¨ظ†ط¬ط§ط­" });
});

export default router;

=== platformSchoolsRoutes.js ===
import express from "express";
import {
  getPlatformDashboard,
  listSchools,
  getSchoolDetails,
  activateTrial,
  activateSubscription,
  suspendSchool,
  impersonateSchoolAdmin,
  reactivateSchool,
  getSchoolFullDetails,
listPlatformActivityLogs,
getSubscriptionAlerts,
} from "../controllers/platformSchoolsController.js";

const router = express.Router();
router.get("/dashboard", getPlatformDashboard);
router.get("/activity-logs", listPlatformActivityLogs);
router.get("/subscription-alerts", getSubscriptionAlerts);

router.get("/schools", listSchools);
router.get("/schools/:id/full-details", getSchoolFullDetails);
router.get("/schools/:id", getSchoolDetails);

router.post("/schools/:id/trial", activateTrial);
router.post("/schools/:id/subscription", activateSubscription);
router.post("/schools/:id/suspend", suspendSchool);
router.post("/schools/:id/reactivate", reactivateSchool);
router.post("/schools/:id/impersonate", impersonateSchoolAdmin);

export default router;

=== profileRoutes.js ===
// src/routes/profileRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getMyProfile,
  changePassword,
  changeEmail,
} from "../controllers/profileController.js";

const router = Router();

router.use(authMiddleware);

// GET /api/profile/me
router.get("/me", getMyProfile);

// PUT /api/profile/password
router.put("/password", changePassword);

// PUT /api/profile/email
router.put("/email", changeEmail);

export default router;

=== public.routes.js ===
import { Router } from 'express';
import multer from 'multer';
import path from 'path';
import { registerSchool } from '../controllers/public.controller.js';

const router = Router();

// ط¥ط¹ط¯ط§ط¯ ظ…ظƒط§ظ† ظˆط§ط³ظ… ط­ظپط¸ ط§ظ„ط´ط¹ط§ط± (ط§ظ„طµظˆط±)
const storage = multer.diskStorage({
  destination: function (req, file, cb) {
    // âڑ ï¸ڈ طھط£ظƒط¯ ط£ظ† ظ„ط¯ظٹظƒ ظ…ط¬ظ„ط¯ ط¨ط§ط³ظ… "uploads" ظپظٹ ط§ظ„ظ…ط³ط§ط± ط§ظ„ط±ط¦ظٹط³ظٹ ظ„ظ…ط´ط±ظˆط¹ظƒ (ط¨ط¬ط§ظ†ط¨ src)
    cb(null, 'uploads/'); 
  },
  filename: function (req, file, cb) {
    // ط¥ط¹ط·ط§ط، ط§ظ„طµظˆط±ط© ط§ط³ظ…ط§ظ‹ ظپط±ظٹط¯ط§ظ‹: logo-16987654321-random.jpg
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
    cb(null, 'logo-' + uniqueSuffix + path.extname(file.originalname));
  }
});

const upload = multer({ storage: storage });

// âœ… ط¥ط¶ط§ظپط© upload.single('logo') ظ„ظƒظٹ ظٹطھظ…ظƒظ† ط§ظ„ط³ظٹط±ظپط± ظ…ظ† ظ‚ط±ط§ط،ط© ط§ظ„طµظˆط±ط©
// ظƒظ„ظ…ط© 'logo' ظٹط¬ط¨ ط£ظ† طھط·ط§ط¨ظ‚ ظ†ظپط³ ط§ظ„ط§ط³ظ… ط§ظ„ط°ظٹ ط£ط±ط³ظ„ظ†ط§ظ‡ ظ…ظ† ط§ظ„ظ€ FormData ظپظٹ ط§ظ„ظ…طھطµظپط­
router.post('/register-school', upload.single('logo'), registerSchool);

export default router;

=== roleRoutes.js ===
// src/routes/roleRoutes.js
import express from "express";
import {
  createRole,
  getRoles,
  getRole,
  updateRole,
  deleteRole,
  getRolePermissions,
  updateRolePermissions,
  grantAllPermissions, // âœ… ط§ظ„ط¥ط¶ط§ظپط© ط§ظ„ط¬ط¯ظٹط¯ط©
} from "../controllers/roleController.js";

import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";

const router = express.Router();

/* ======================
   ًں”’ ط¥ط¯ط§ط±ط© ط§ظ„ط£ط¯ظˆط§ط±
====================== */

// ط¥ظ†ط´ط§ط، ط¯ظˆط± ط¬ط¯ظٹط¯
router.post(
  "/",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  createRole
);

// ط¬ظ„ط¨ ظƒظ„ ط§ظ„ط£ط¯ظˆط§ط±
router.get(
  "/",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  getRoles
);

// ط¬ظ„ط¨ ط¯ظˆط± ظˆط§ط­ط¯
router.get(
  "/:id",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  getRole
);

// طھط­ط¯ظٹط« ط¯ظˆط±
router.put(
  "/:id",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  updateRole
);

// ط­ط°ظپ ط¯ظˆط±
router.delete(
  "/:id",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  deleteRole
);

/* ======================
   ًں”‘ طµظ„ط§ط­ظٹط§طھ ط§ظ„ط¯ظˆط±
====================== */

// ط¬ظ„ط¨ طµظ„ط§ط­ظٹط§طھ ط¯ظˆط± ظ…ط¹ظٹظ†
router.get(
  "/:id/permissions",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  getRolePermissions
);

// طھط­ط¯ظٹط« طµظ„ط§ط­ظٹط§طھ ط¯ظˆط± (checkboxes)
router.post(
  "/:id/permissions",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  updateRolePermissions
);

/* ======================
   âœ… ظ…ظٹط²ط© ط§ط­طھط±ط§ظپظٹط©
   ًں”گ ظ…ظ†ط­ ظƒظ„ ط§ظ„طµظ„ط§ط­ظٹط§طھ
====================== */

// router.post(
//   "/:id/grant-all-permissions",
//   authMiddleware,
//   checkPermission("rbac.manage_roles"),
//   grantAllPermissions
// );

export default router;

=== schoolSettingsRoutes.js ===
import { Router } from "express";
import multer from "multer";
import path from "path";
import authMiddleware from "../middleware/authMiddleware.js";

import {
  meta,
  profileUpdate,
  academicGet, // âœ… ط£ط¶ظپ ظ‡ط°ط§ ط§ظ„ط§ط³طھظٹط±ط§ط¯
  academicUpdate, // âœ… ط£ط¶ظپ ظ‡ط°ط§ ط§ظ„ط§ط³طھظٹط±ط§ط¯
  financeUpdate,
  portalsGet,
  portalsUpdate,
  yearsCreate,
  yearsUpdate,
  yearsToggle,
  stagesCreate,
  stagesUpdate,
  stagesToggle,
  gradesCreate,
  gradesUpdate,
  gradesToggle,
  sectionsCreate,
  sectionsUpdate,
  sectionsToggle,
  subjectsCreate,
  subjectsUpdate,
  financeGet,
  subjectsToggle,
  periodsCreate,
  periodsUpdate,
  periodsToggle,
  curriculumGet,
  curriculumSet,
  teacherSubjectGet,
  teacherSubjectSet,
} from "../controllers/schoolSettingsController.js";

const router = Router();

// --- ط¥ط¹ط¯ط§ط¯ multer ظ„ط±ظپط¹ ط§ظ„ط´ط¹ط§ط±ط§طھ ---
const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, "uploads/");
  },
  filename: (req, file, cb) => {
    const uniqueSuffix = Date.now() + "-" + Math.round(Math.random() * 1e9);
    cb(null, "school_" + uniqueSuffix + path.extname(file.originalname));
  },
});
const upload = multer({ storage });

router.use(authMiddleware);

router.get("/meta", meta);
router.get('/finance', financeGet);
// âœ… ط§ظ„ظ…ظ„ظپ ط§ظ„ط´ط®طµظٹ ظ„ظ„ظ…ط¯ط±ط³ط©
router.post("/profile", upload.single("logo"), profileUpdate);

// âœ… ط§ظ„ط¥ط¹ط¯ط§ط¯ط§طھ ط§ظ„ط£ظƒط§ط¯ظٹظ…ظٹط© (ط§ظ„ط¬ط¯ظٹط¯ط©)
router.get("/academic", academicGet);
router.post("/academic", academicUpdate);

router.post("/finance", financeUpdate);

router.get("/portals", portalsGet);
router.post("/portals", portalsUpdate);
// ط§ظ„ط³ظ†ظˆط§طھ ط§ظ„ط¯ط±ط§ط³ظٹط©
router.post("/years", yearsCreate);
router.patch("/years/:id", yearsUpdate);
router.patch("/years/:id/toggle", yearsToggle);

// ط§ظ„ظ…ط±ط§ط­ظ„
router.post("/stages", stagesCreate);
router.patch("/stages/:id", stagesUpdate);
router.patch("/stages/:id/toggle", stagesToggle);

// ط§ظ„طµظپظˆظپ
router.post("/grades", gradesCreate);
router.patch("/grades/:id", gradesUpdate);
router.patch("/grades/:id/toggle", gradesToggle);

// ط§ظ„ط´ط¹ط¨
router.post("/sections", sectionsCreate);
router.patch("/sections/:id", sectionsUpdate);
router.patch("/sections/:id/toggle", sectionsToggle);

// ط§ظ„ظ…ظˆط§ط¯
router.post("/subjects", subjectsCreate);
router.patch("/subjects/:id", subjectsUpdate);
router.patch("/subjects/:id/toggle", subjectsToggle);

// ط§ظ„ظپطھط±ط§طھ (ط§ظ„ط­طµطµ)
router.post("/periods", periodsCreate);
router.patch("/periods/:id", periodsUpdate);
router.patch("/periods/:id/toggle", periodsToggle);

// ظ…ظ†ظ‡ط¬ ط§ظ„طµظپظˆظپ
router.get("/curriculum", curriculumGet);
router.post("/curriculum", curriculumSet);

// طھط£ظ‡ظٹظ„ ط§ظ„ظ…ط¯ط±ط³ظٹظ†
router.get("/teacher-subjects", teacherSubjectGet);
router.post("/teacher-subjects", teacherSubjectSet);

export default router;

=== sectionRoutes.js ===
import { Router } from "express";
import  authMiddleware  from "../middleware/authMiddleware.js";
import  checkPermission  from "../middleware/checkPermission.js";
import { getSections } from "../controllers/sectionController.js";

const router = Router();

router.get(
  "/",
  authMiddleware,
  checkPermission("view_academic_years"),
  getSections
);

export default router;

=== staffReportsRoutes.js ===
// backend/src/routes/staffReportsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StaffReportsController } from "../controllers/staffReportsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/summary", StaffReportsController.summary);
router.get("/", StaffReportsController.list);
router.get("/:employeeId/profile", StaffReportsController.profile);

export default router;

=== stageRoutes.js ===
import { Router } from "express";
import  authMiddleware  from "../middleware/authMiddleware.js";
import  checkPermission  from "../middleware/checkPermission.js";
import { getStages } from "../controllers/stageController.js";

const router = Router();

router.get(
  "/",
  authMiddleware,
  checkPermission("view_academic_years"), // ظ…ط¤ظ‚طھط§ظ‹ ظ†ط³طھط®ط¯ظ… طµظ„ط§ط­ظٹط© ط¹ط§ظ…ط©
  getStages
);

export default router;

=== studentAttendanceRoutes.js ===
// backend/src/routes/studentAttendanceRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StudentAttendanceController } from "../controllers/studentAttendanceController.js";

const router = express.Router();
router.use(authMiddleware);

// ظ…ظ„ط®طµ ط§ظ„ظٹظˆظ… + ط±ط³ط§ظ„ط© ظ‚ظˆظٹط© ظ„ظ„ظˆط§ط¬ظ‡ط©
router.get("/today", StudentAttendanceController.today);

// ط£ط³ط¨ظˆط¹ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ط­ط§ظ„ظٹ (ط³ط¨طھ -> ط¬ظ…ط¹ط©)
router.get("/week", StudentAttendanceController.week);

// ظ†ط·ط§ظ‚ ط¹ط§ظ… (ظٹظڈط³طھط®ط¯ظ… ظ„ظ„ط´ظ‡ط±/ط§ظ„ظپطµظ„)
router.get("/range", StudentAttendanceController.range);

// ظ†ط³ط¨ط© ط­ط¶ظˆط± ط§ظ„ط¹ط§ظ… + ط¥ط­طµط§ط¦ظٹط§طھ
router.get("/stats", StudentAttendanceController.stats);

export default router;

=== studentBarcodeRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StudentBarcodeController } from "../controllers/studentBarcodeController.js";

const router = express.Router();
router.use(authMiddleware);

router.get("/", StudentBarcodeController.getMyBarcode);

export default router;

=== studentFeesPortalRoutes.js ===
// src/routes/studentFeesPortalRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { studentFeesOverview } from "../controllers/studentFeesPortalController.js";

const router = Router();

// âœ… ط§ظ„ظ…ط³ط§ط± ظ…ط­ظ…ظٹ ط¨ظ€ authMiddleware ظ„ط¶ظ…ط§ظ† طھظˆظپط± ط¨ظٹط§ظ†ط§طھ ط§ظ„ط·ط§ظ„ط¨ ظˆظ…ط¯ط±ط³طھظ‡
router.get("/overview", authMiddleware, studentFeesOverview);

export default router;

=== studentLearningRoutes.js ===
import express from "express";
import fs from "fs";
import path from "path";
import multer from "multer";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  listStudentActivities,
  getStudentActivityDetail,
  submitStudentActivity,
  listStudentGrades,
} from "../controllers/studentLearningController.js";

const router = express.Router();

const uploadDir = path.join(process.cwd(), "uploads", "submissions");
fs.mkdirSync(uploadDir, { recursive: true });

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => cb(null, uploadDir),
  filename: (_req, file, cb) => {
    const ext = path.extname(file.originalname || "");
    const safe = `sub_${Date.now()}_${Math.round(Math.random() * 1e9)}${ext}`;
    cb(null, safe);
  },
});

const upload = multer({
  storage,
  limits: {
    fileSize: 10 * 1024 * 1024, // 10MB
  },
});

router.use(authMiddleware);

router.get("/activities", listStudentActivities);
router.get("/activities/:id", getStudentActivityDetail);
router.post("/activities/:id/submit", upload.single("file"), submitStudentActivity);

router.get("/grades", listStudentGrades);

export default router;

=== studentNotificationsRoutes.js ===
// backend/src/routes/studentNotificationsRoutes.js
import express from "express";
import {
  unreadCount,
  listInbox,
  markOneRead,
  markAllRead,
  listOutbox,
  outboxRecipients,
  listMyTeachers,
  sendAdmins,
  sendTeachers,
} from "../controllers/studentNotificationsController.js";

// âœ… ط§ط³طھظٹط±ط§ط¯ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ظ„ط¶ظ…ط§ظ† ط§ط³طھط®ط±ط§ط¬ req.user.school_id
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

/**
 * ًں”’ ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ (Student Portal)
 * طھظ… طھط·ط¨ظٹظ‚ authMiddleware ظ„ط¶ظ…ط§ظ† ط£ظ† ط§ظ„ط·ط§ظ„ط¨ ظ„ط§ ظٹطµظ„ ط¥ظ„ط§ ظ„ط¨ظٹط§ظ†ط§طھ ظ…ط¯ط±ط³طھظ‡ ظپظ‚ط·.
 */
router.use(authMiddleware);

// ==========================================
// ًں“¥ طµظ†ط¯ظˆظ‚ ط§ظ„ظˆط§ط±ط¯ (Inbox)
// ==========================================

// ط¹ط¯ط¯ ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط،ط© ظ„ظ„ط·ط§ظ„ط¨ (ظ„ط¥ط¸ظ‡ط§ط± ط§ظ„طھظ†ط¨ظٹظ‡ ط§ظ„ط£ط­ظ…ط±)
router.get("/unread-count", unreadCount);

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط³طھظ„ظ…ط© (طھط­ط¯ظٹط«ط§طھ ط§ظ„ط؛ظٹط§ط¨طŒ ط§ظ„ظ†طھط§ط¦ط¬طŒ ط¥ظ„ط®)
router.get("/inbox", listInbox);

// طھط¹ظ„ظٹظ… ط¬ظ…ظٹط¹ ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ط­ط§ظ„ظٹط© ظƒظ…ظ‚ط±ظˆط،ط©
router.patch("/inbox/read-all", markAllRead);

// طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظˆط§ط­ط¯ ظ…ط­ط¯ط¯ ظƒظ…ظ‚ط±ظˆط،
router.patch("/inbox/:id/read", markOneRead);

// ==========================================
// ًں“¤ طµظ†ط¯ظˆظ‚ ط§ظ„طµط§ط¯ط± (Outbox)
// ==========================================

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„طھظٹ ط£ط±ط³ظ„ظ‡ط§ ط§ظ„ط·ط§ظ„ط¨ (ظ„ظ„ظ…ط¹ظ„ظ…ظٹظ† ط£ظˆ ط§ظ„ط¥ط¯ط§ط±ط©)
router.get("/outbox", listOutbox);

// طھظپط§طµظٹظ„ ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† ظ„ط¥ط´ط¹ط§ط± ظ…ط±ط³ظ„ (ظ„ظ…ط¹ط±ظپط© ظ‡ظ„ ظ‚ط±ط£ ط§ظ„ظ…ط¹ظ„ظ… ط§ظ„ط±ط³ط§ظ„ط©طں)
router.get("/outbox/:id/recipients", outboxRecipients);

// ==========================================
// ًں”چ ظ…ط³ط§ط¹ط¯ط§طھ ط§ظ„ط¨ط­ط« (Lookups)
// ==========================================

// ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ط§ظ„ظ…ط¹ظ„ظ…ظٹظ† ط§ظ„ط°ظٹظ† ظٹط¯ط±ط³ظˆظ† ظ‡ط°ط§ ط§ظ„ط·ط§ظ„ط¨ ط­ط§ظ„ظٹط§ظ‹ ظپظٹ ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط©
router.get("/teachers", listMyTeachers);

// ==========================================
// âœ‰ï¸ڈ ط¹ظ…ظ„ظٹط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ (Messaging)
// ==========================================

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط©/طھط¸ظ„ظ… ط¥ظ„ظ‰ ط¥ط¯ط§ط±ط© ط§ظ„ظ…ط¯ط±ط³ط©
router.post("/send/admins", sendAdmins);

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ظ…ط¹ظ„ظ… ظ…ط­ط¯ط¯ ط£ظˆ ط¬ظ…ظٹط¹ ظ…ط¹ظ„ظ…ظٹ ط§ظ„ط·ط§ظ„ط¨
router.post("/send/teachers", sendTeachers);

export default router;

=== studentPortalRoutes.js ===
// src/routes/studentPortalRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getMeta,
  getTimetable,
  getExamMeta,
  getExams,
  getStudentHeroStats
} from "../controllers/studentPortalController.js";

const router = express.Router();

// meta: days + periods + years
router.get("/meta", authMiddleware, getMeta);

// timetable published for student class
router.get("/timetable", authMiddleware, getTimetable);

// stats for top bar
router.get("/hero-stats", authMiddleware, getStudentHeroStats);

// exam metadata and list
router.get("/exams/meta", authMiddleware, getExamMeta);
router.get("/exams", authMiddleware, getExams);

export default router;

=== studentReportsRoutes.js ===
// backend/src/routes/studentReportsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StudentReportsController } from "../controllers/studentReportsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/summary", StudentReportsController.summary);
router.get("/", StudentReportsController.list);
router.get("/:studentId/profile", StudentReportsController.profile);

export default router;

=== studentRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";
import { searchStudents } from "../controllers/metaController.js";
import {
  registerStudent,
  listStudents,
  getStudentById,
  deleteStudentById,
  updateStudentById,
  getNextStudentCode, // âœ… 1. ط£ط¶ظپظ†ط§ ط§ط³طھظٹط±ط§ط¯ ط§ظ„ط¯ط§ظ„ط© ظ‡ظ†ط§
} from "../controllers/studentController.js";

const router = Router();

// POST /api/students/register
router.post(
  "/register",
  authMiddleware,
  checkPermission("admission.create_student"),
  registerStudent
);

// GET /api/students
router.get(
  "/",
  authMiddleware,
  checkPermission("admission.view_students"),
  listStudents
);

// âœ… GET /api/students/search
router.get(
  "/search",
  authMiddleware,
  checkPermission("admission.view_students"),
  searchStudents
);

// âœ… 2. ظ†ظ‚ظ„ظ†ط§ /next-code ظ„ظٹظƒظˆظ† ظ‚ط¨ظ„ /:id (طھط¬ظ†ط¨ط§ظ‹ ظ„ظپط® ط§ظ„ظ€ params)
router.get(
  '/next-code', 
  authMiddleware, 
  getNextStudentCode
);

// GET /api/students/:id
router.get(
  "/:id",
  authMiddleware,
  checkPermission("admission.view_students"),
  getStudentById
);

// âœ… PUT/PATCH /api/students/:id (طھط¹ط¯ظٹظ„)
router.put(
  "/:id",
  authMiddleware,
  checkPermission("admission.update_student"),
  updateStudentById
);

router.patch(
  "/:id",
  authMiddleware,
  checkPermission("admission.update_student"),
  updateStudentById
);

// DELETE /api/students/:id
router.delete(
  "/:id",
  authMiddleware,
  checkPermission("admission.delete_student"),
  deleteStudentById
);

export default router;

=== studentTermResultsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { getStudentTermResults } from "../controllers/studentTermResultsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getStudentTermResults);

export default router;

=== studentTransferRequestsRoutes.js ===
// src/routes/studentTransferRequestsRoutes.js
import { Router } from "express";
import { StudentTransferRequestsController } from "../controllers/studentTransferRequestsController.js";

const router = Router();

router.get("/", StudentTransferRequestsController.list);

router.post("/", StudentTransferRequestsController.create);

router.post("/:id/approve", StudentTransferRequestsController.approve);

router.post("/:id/reject", StudentTransferRequestsController.reject);

export default router;

=== teacherAssessmentsRoutes.js ===
import express from "express";
import fs from "fs";
import path from "path";
import multer from "multer";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  listAssessments,
  createAssessment,
  publishAssessment,
  closeAssessment,
  getOfficialAssessmentContext,
} from "../controllers/teacherAssessmentsController.js";

const router = express.Router();

// =========================================
// ط¥ط¹ط¯ط§ط¯ط§طھ ط±ظپط¹ ط§ظ„ظ…ط±ظپظ‚ط§طھ ط§ظ„ط®ط§طµط© ط¨ط§ظ„ظ…ط¹ظ„ظ… (Multer)
// =========================================
const uploadDir = path.join(process.cwd(), "uploads", "assessments");
fs.mkdirSync(uploadDir, { recursive: true });

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => cb(null, uploadDir),
  filename: (_req, file, cb) => {
    const ext = path.extname(file.originalname || "");
    const safe = `asm_${Date.now()}_${Math.round(Math.random() * 1e9)}${ext}`;
    cb(null, safe);
  },
});

const upload = multer({
  storage,
  limits: {
    fileSize: 10 * 1024 * 1024, // ط§ظ„ط­ط¯ ط§ظ„ط£ظ‚طµظ‰ ظ„ظ„ظ…ظ„ظپ ط§ظ„ظˆط§ط­ط¯: 10 ظ…ظٹط¬ط§ط¨ط§ظٹطھ
  },
});
// =========================================

router.use(authMiddleware);

router.get("/official-context", getOfficialAssessmentContext);
router.get("/", listAssessments);

// âœ… طھظ… ط§ظ„ط¥طµظ„ط§ط­: ط¥ط¶ط§ظپط© upload.array ظ„ظپظƒ طھط´ظپظٹط± ط§ظ„ظ€ FormData ظˆط§ط³طھظ‚ط¨ط§ظ„ ط­طھظ‰ 5 ظ…ظ„ظپط§طھ
router.post("/", upload.array("files", 5), createAssessment);

router.post("/:id/publish", publishAssessment);
router.post("/:id/close", closeAssessment);

export default router;

=== teacherAttendanceRoutes.js ===
// src/routes/teacherAttendanceRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherAttendanceController } from "../controllers/teacherAttendanceController.js";

const router = express.Router();

router.use(authMiddleware);

/* =========================
   META + SCOPES + REASONS
========================= */
router.get("/meta", TeacherAttendanceController.meta);
router.get("/scopes", TeacherAttendanceController.scopes);
router.get("/reasons", TeacherAttendanceController.reasons);

/* =========================
   SESSIONS (Today Slots + Frontend Aliases)
   ظ…ظ‡ظ…:
   ظ‡ط°ظ‡ ط§ظ„ظ…ط³ط§ط±ط§طھ ظٹط¬ط¨ ط£ظ† طھظƒظˆظ† ظ‚ط¨ظ„ /sessions/:id
========================= */

// ط§ظ„ظˆط§ط¬ظ‡ط© طھط·ظ„ط¨:
// GET /api/teacher/attendance/sessions?academicYearId=6&term=1
router.get("/sessions", TeacherAttendanceController.sessionSlots);

// ط§ظ„ط±ط§ط¨ط· ط§ظ„ظ‚ط¯ظٹظ…/ط§ظ„ط£طµظ„ظٹ
router.get("/sessions/slots", TeacherAttendanceController.sessionSlots);

// ط§ظ„ظˆط§ط¬ظ‡ط© طھط·ظ„ط¨:
// GET /api/teacher/attendance/sessions/history?academicYearId=6&term=1
router.get("/sessions/history", TeacherAttendanceController.historySearch);

// ط§ظ„ظˆط§ط¬ظ‡ط© طھط·ظ„ط¨:
// GET /api/teacher/attendance/sessions/log?academicYearId=6&term=1
router.get("/sessions/log", TeacherAttendanceController.historySearch);

/* =========================
   PERMITS (Parent requests / excuses)
   - ظ…ط³ط§ط±ط§طھ ظˆط§ط¶ط­ط© + Aliases ظ„ظ„طھظˆط§ظپظ‚
========================= */
router.get("/permits", TeacherAttendanceController.permitsList);
router.get("/permits/approved", TeacherAttendanceController.permitsApproved);
router.get("/permits/excuses", TeacherAttendanceController.permitsExcuses);
router.get("/permits/map", TeacherAttendanceController.permitsMap);

// Aliases ظ„ظˆ ط§ظ„ظپط±ظˆظ†طھ ظٹط³طھط®ط¯ظ…ظ‡ط§
router.get("/approved", TeacherAttendanceController.permitsApproved);
router.get("/excuses", TeacherAttendanceController.permitsExcuses);

/* =========================
   HISTORY + REPORTS
========================= */

// ط§ظ„ط±ط§ط¨ط· ط§ظ„ط¹ط§ظ… ط§ظ„ظ‚ط¯ظٹظ…:
// GET /api/teacher/attendance/history
router.get("/history", TeacherAttendanceController.historySearch);

router.get("/report/aggregate", TeacherAttendanceController.reportAggregate);

/* =========================
   SESSIONS (CRUD + Lock/Unlock/End)
========================= */

// Create/Open session
router.post("/sessions", TeacherAttendanceController.createSession);

// Alias: ظ„ظˆ ط§ظ„ظپط±ظˆظ†طھ ظٹط±ط³ظ„ /sessions/start
router.post("/sessions/start", TeacherAttendanceController.createSession);

// Get/Update
// ظ…ظ‡ظ… ط¬ط¯ظ‹ط§:
// ظ‡ط°ط§ ظٹط¬ط¨ ط£ظ† ظٹط¨ظ‚ظ‰ ط¨ط¹ط¯ /sessions/history ظˆ /sessions/log
// ط­طھظ‰ ظ„ط§ ظٹط¹طھط¨ط± Express ظƒظ„ظ…ط© history ط£ظˆ log ط£ظ†ظ‡ط§ id
router.get("/sessions/:id", TeacherAttendanceController.getSession);
router.patch("/sessions/:id", TeacherAttendanceController.updateSession);

// Lock/Unlock
router.patch("/sessions/:id/lock", TeacherAttendanceController.lockSession);
router.patch("/sessions/:id/unlock", TeacherAttendanceController.unlockSession);

// End
router.patch("/sessions/:id/end", TeacherAttendanceController.endSession);
router.post("/sessions/:id/end", TeacherAttendanceController.endSession);

// Scan QR to mark present inside this session
router.post("/sessions/:id/scan", TeacherAttendanceController.scanMarkPresentByToken);

/* =========================
   ENTRIES (Students + Save Attendance)
========================= */
router.get("/sessions/:id/entries", TeacherAttendanceController.listEntries);
router.put("/sessions/:id/entries", TeacherAttendanceController.saveEntries);

// Aliases ظ„ظ„طھظˆط§ظپظ‚ ظ…ط¹ ظپط±ظˆظ†طھ ظ‚ط¯ظٹظ…
router.get("/sessions/:id/students", TeacherAttendanceController.listEntries);
router.post("/sessions/:id/attendance", TeacherAttendanceController.saveEntries);

export default router;

=== teacherDashboardRoutes.js ===
import express from "express";
import requireAuth from "../middleware/authMiddleware.js";
import {
  getHero,
  getNextLesson,
} from "../controllers/teacherDashboardController.js";

const router = express.Router();

// ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„ظ‚ط¯ظٹظ…ط©
router.get("/hero", requireAuth, getHero);
router.get("/next-lesson", requireAuth, getNextLesson);

// ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„طھظٹ طھط·ظ„ط¨ظ‡ط§ ط§ظ„ظˆط§ط¬ظ‡ط© ط§ظ„ط­ط§ظ„ظٹط©
router.get("/dashboard/hero", requireAuth, getHero);
router.get("/dashboard/next-lesson", requireAuth, getNextLesson);

export default router;

=== teacherGradesRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getGradeEntry,
  saveGradeEntry,
  publishGradeEntry,
  listReopenRequests,
  createReopenRequest,
  submitMuhassala,
  getMonthlyWorkControlStatuses,
  getTermWorkControlStatus,
} from "../controllers/teacherGradesController.js";

const router = express.Router();

// ظ…ظ‡ظ… ط¬ط¯ظ‹ط§: ظƒظ„ ظ…ط³ط§ط±ط§طھ ط¯ط±ط¬ط§طھ ط§ظ„ظ…ط¹ظ„ظ… طھط­طھط§ط¬ req.user
router.use(authMiddleware);

router.get("/monthly-work-statuses", getMonthlyWorkControlStatuses);
router.get("/term-work-status", getTermWorkControlStatus);

router.get("/entry", getGradeEntry);
router.post("/entry/save", saveGradeEntry);
router.post("/entry/publish", publishGradeEntry);

router.post("/muhassala/submit", submitMuhassala);

router.get("/reopen-requests", listReopenRequests);
router.post("/reopen-requests", createReopenRequest);

export default router;

=== teacherMeRoutes.js ===
// src/routes/teacherMeRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherMeController } from "../controllers/teacherMeController.js";

const router = express.Router();

// ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ
router.use(authMiddleware);

/*
  ظ„ط£ظ† ظ‡ط°ط§ ط§ظ„ظ…ظ„ظپ ظ…ط±ظƒط¨ ظپظٹ server.js ط¹ظ„ظ‰:
  /api/teacher/me

  ط¥ط°ظ†:
  router.get("/") = /api/teacher/me
*/
router.get("/", TeacherMeController.card);

// ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„ظ…ظˆط¬ظˆط¯ط© ط³ط§ط¨ظ‚ظ‹ط§
router.get("/card", TeacherMeController.card);
router.get("/token", TeacherMeController.token);

export default router;

=== teacherNotificationsRoutes.js ===
// backend/src/routes/teacherNotificationsRoutes.js
import express from "express";
import {
  getUnreadCount,
  listInbox,
  markOneRead,
  markAllRead,
  listOutbox,
  outboxRecipients,
} from "../controllers/teacherNotificationsController.js";

// âœ… ط§ط³طھظٹط±ط§ط¯ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ط§ظ„ط®ط§طµ ط¨ط§ظ„طھظˆط«ظٹظ‚ (طھط£ظƒط¯ ظ…ظ† ط§ظ„ظ…ط³ط§ط± ط§ظ„طµط­ظٹط­ ظپظٹ ظ…ط´ط±ظˆط¹ظƒ)
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

// طھط·ط¨ظٹظ‚ ط§ظ„ط­ظ…ط§ظٹط© ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ„ط¶ظ…ط§ظ† ط§ط³طھط®ط±ط§ط¬ req.user.school_id
router.use(authMiddleware);

// ط¹ط¯ط§ط¯ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط، (ظˆط§ط±ط¯ ط§ظ„ظ…ط¹ظ„ظ…)
router.get("/unread-count", getUnreadCount);

// ط§ظ„ظˆط§ط±ط¯
router.get("/inbox", listInbox);

// طھط¹ظ„ظٹظ… ظƒظ„ ط§ظ„ظˆط§ط±ط¯ ظƒظ…ظ‚ط±ظˆط، (ظٹظڈظپط¶ظ„ ظˆط¶ط¹ظ‡ ظ‚ط¨ظ„ ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„ط¯ظٹظ†ط§ظ…ظٹظƒظٹط© :id)
router.patch("/inbox/read-all", markAllRead);

// طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظˆط§ط­ط¯ ظ…ظ‚ط±ظˆط، (ط¨ظˆط§ط³ط·ط© ط§ظ„ظ€ id)
router.patch("/inbox/:id/read", markOneRead);

// ط§ظ„طµط§ط¯ط± (ط§ظ„ط°ظٹ ط£ط±ط³ظ„ظ‡ ط§ظ„ظ…ط¹ظ„ظ…)
router.get("/outbox", listOutbox);

// طھظپط§طµظٹظ„ ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† (ظ…ظ† ظ‚ط±ط£/ظ„ظ… ظٹظ‚ط±ط£)
router.get("/outbox/:id/recipients", outboxRecipients);

export default router;

=== teacherNotificationsSendRoutes.js ===
// backend/src/routes/teacherNotificationsSendRoutes.js
import express from "express";
import {
  getTeacherScopes,
  listScopeStudents,
  listStudentGuardians,
  sendToAdmins,
  sendToStudents,
  sendToGuardians,
} from "../controllers/teacherNotificationsSendController.js";
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

// ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ ظ„ط¶ظ…ط§ظ† ط§ط³طھط®ط±ط§ط¬ school_id
router.use(authMiddleware);

// ظ†ط·ط§ظ‚ط§طھ ط§ظ„ظ…ط¹ظ„ظ…
router.get("/scopes", getTeacherScopes);

// ط·ظ„ط§ط¨ ط´ط¹ط¨ط© ظ…ط¹ظٹظ†ط© (ظ…ظ† ظ†ط·ط§ظ‚ظ‡ ظˆظ…ط¯ط±ط³طھظ‡ ظپظ‚ط·)
router.get("/students", listScopeStudents);

// ط£ظˆظ„ظٹط§ط، ط£ظ…ظˆط± ط·ط§ظ„ط¨ ظ…ط¹ظٹظ†
router.get("/students/:studentId/guardians", listStudentGuardians);

// ط¥ط±ط³ط§ظ„ ظ„ظ„ط¥ط¯ط§ط±ط©
router.post("/send/admins", sendToAdmins);

// ط¥ط±ط³ط§ظ„ ظ„ظ„ط·ظ„ط§ط¨
router.post("/send/students", sendToStudents);

// ط¥ط±ط³ط§ظ„ ظ„ظ„ط£ظˆظ„ظٹط§ط،
router.post("/send/guardians", sendToGuardians);

export default router;

=== teacherPermitsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherPermitsController } from "../controllers/teacherPermitsController.js";

const router = express.Router();
router.use(authMiddleware);

// âœ… ظ…ظ‡ظ…: ظ„ط§ط²ظ… ظ‡ط°ظ‡ ط§ظ„ط¯ط§ظ„ط© طھظƒظˆظ† ظ…ظˆط¬ظˆط¯ط© ظپظٹ ط§ظ„ظƒظ†طھط±ظˆظ„ط±
router.get("/available-slots", TeacherPermitsController.availableSlots);

router.get("/", TeacherPermitsController.listMyPermits);
router.post("/", TeacherPermitsController.createPermit);
// ظپظٹ ظ…ظ„ظپ teacherRoutes.js ط£ظˆ ظ…ط§ ط´ط§ط¨ظ‡:
router.get("/substitutions/pending", TeacherPermitsController.getPendingSubstitutions);
router.patch("/substitutions/:id/respond", TeacherPermitsController.respondToSubstitution);
export default router;

=== teacherProfileRoutes.js ===
import express from "express";
import { getTeacherJobProfile } from "../controllers/teacherProfileController.js";

const router = express.Router();

// âœ… ظ‡ظ†ط§ "/" ظ„ط£ظ†ظ†ط§ ط³ظ†ط±ظƒط¨ظ‡ ط¹ظ„ظ‰ /api/teacher/profile
router.get("/", getTeacherJobProfile);

export default router;

=== teacherReportsRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط§ط³طھط¯ط¹ط§ط، ط­ط§ط±ط³ ط§ظ„ط¨ظˆط§ط¨ط©
import {
  getTeacherReportsMeta,
  getTeacherReportScopes,
  getTeacherReportContext,
  generateTeacherReport,
} from "../controllers/teacherReportsController.js";

const router = Router();

// âœ… طھط·ط¨ظٹظ‚ ط§ظ„ط­ظ…ط§ظٹط© ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ظ…ط³ط§ط±ط§طھ ط§ظ„طھظ‚ط§ط±ظٹط± ظ„ط¶ظ…ط§ظ† ظ…ط¹ط±ظپط© ظ‡ظˆظٹط© ط§ظ„ظ…ط¹ظ„ظ… ظˆظ…ط¯ط±ط³طھظ‡
router.use(authMiddleware);

router.get("/meta", getTeacherReportsMeta);
router.get("/scopes", getTeacherReportScopes);
router.get("/context", getTeacherReportContext);
router.post("/generate", generateTeacherReport);

export default router;

=== teacherScopesRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { listTeacherScopes } from "../controllers/teacherScopesController.js";

const router = express.Router();
router.use(authMiddleware);

router.get("/", listTeacherScopes);

export default router;

=== teacherSessionsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  startSession,
  endSession,
  sessionStudents,
  saveAttendance,
} from "../controllers/teacherSessionsController.js";

const router = express.Router();

router.use(authMiddleware);

// ط¨ط¯ط، ط§ظ„ط­طµط©
router.post("/start", startSession);

// ط¥ظ†ظ‡ط§ط، ط§ظ„ط­طµط©
router.post("/:id/end", endSession);

// ط·ظ„ط§ط¨ ط§ظ„ط­طµط©
router.get("/:id/students", sessionStudents);

// ط­ظپط¸ ط§ظ„ط­ط¶ظˆط±
router.post("/:id/attendance", saveAttendance);

export default router;

=== teacherStudentsRoutes.js ===
import express from "express";
import { getMeta, getScopes, listStudents } from "../controllers/teacherStudentsController.js";

// âœ… ط؛ظٹظ‘ط± ط§ط³ظ… ط§ظ„ظ…ظٹط¯ظ„ظˆظٹط± ط­ط³ط¨ ظ…ط´ط±ظˆط¹ظƒ
import  requireAuth  from "../middleware/authMiddleware.js";

const router = express.Router();

router.get("/meta", requireAuth, getMeta);
router.get("/scopes", requireAuth, getScopes);
router.get("/list", requireAuth, listStudents);

export default router;

=== teacherTimetablesRoutes.js ===
// src/routes/teacherTimetablesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherTimetablesController } from "../controllers/teacherTimetablesController.js";

const router = express.Router();

// ط­ظ…ط§ظٹط© ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ
router.use(authMiddleware);

// ط§ظ„ط£ط³ط§ط³ظٹط©
router.get("/meta", TeacherTimetablesController.meta);
router.get("/", TeacherTimetablesController.list);
router.get("/classes", TeacherTimetablesController.classes);

// ط§ظ„ط§ط®طھط¨ط§ط±ط§طھ (ط¶ظ…ظ† ظ†ظپط³ ط§ظ„ط±ظˆطھط±)
router.get("/exams/meta", TeacherTimetablesController.examsMeta);
router.get("/exams", TeacherTimetablesController.examsList);

// âœ… ط§ظ„ط·ظ„ط§ط¨
router.get("/students/scopes", TeacherTimetablesController.studentsScopes);
router.get("/students", TeacherTimetablesController.studentsList);

export default router;

=== timetablesRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import TimetablesController from "../controllers/timetablesController.js";

const router = Router();

// =========================
// Meta / Lists
// =========================
router.get("/meta", authMiddleware, TimetablesController.meta);
router.get("/list", authMiddleware, TimetablesController.list);
router.get("/teachers", authMiddleware, TimetablesController.teachersBySubject);

// âœ… ظپط­طµ طھط¹ط§ط±ط¶ ط§ظ„ظ…ط¹ظ„ظ… ظ‚ط¨ظ„ ط§ظ„ط­ظپط¸
// ظ„ط§ط²ظ… ظٹظƒظˆظ† ظ‚ط¨ظ„ /:id
router.get(
  "/check-teacher-conflict",
  authMiddleware,
  TimetablesController.checkTeacherConflict
);

// =========================
// Create / Copy
// =========================
router.post("/get-or-create", authMiddleware, TimetablesController.getOrCreate);
router.post("/:id/copy-from", authMiddleware, TimetablesController.copyFrom);

// =========================
// Publish / Unpublish
// =========================
router.put("/:id/publish", authMiddleware, TimetablesController.publish);
router.put("/:id/unpublish", authMiddleware, TimetablesController.unpublish);

// =========================
// Weekly template entries
// =========================
router.put("/:id/entries", authMiddleware, TimetablesController.saveEntries);
router.delete("/:id/entries", authMiddleware, TimetablesController.clearEntries);

// =========================
// Overrides (ط§ط®طھط¨ط§ط±ط§طھ / ط¥ظ„ط؛ط§ط، / ط§ط³طھط«ظ†ط§ط،ط§طھ ط£ط³ط¨ظˆط¹ظٹط©)
// =========================
router.get("/:id/overrides", authMiddleware, TimetablesController.listOverrides);
router.put("/:id/overrides", authMiddleware, TimetablesController.upsertOverride);
router.delete("/:id/overrides", authMiddleware, TimetablesController.deleteOverride);

// ط¥ط°ط§ ط£ط±ط¯طھ طھط£ظƒظٹط¯/طھط­ط¯ظٹط« ط§ط³طھط«ظ†ط§ط،ط§طھ ط£ط³ط¨ظˆط¹ ظ…ط¹ظٹظ†
router.put(
  "/:id/overrides/publish-week",
  authMiddleware,
  TimetablesController.publishWeekOverrides
);

// =========================
// Delete timetable
// =========================
router.delete("/:id", authMiddleware, TimetablesController.remove);

// =========================
// Get timetable + entries
// ظ„ط§ط²ظ… ظٹظƒظˆظ† ط§ظ„ط£ط®ظٹط±
// =========================
router.get("/:id", authMiddleware, TimetablesController.getById);

export default router;

=== userRoutes.js ===
// src/routes/userRoutes.js
import { Router } from "express";
import { UserController } from "../controllers/userController.js";
import authMiddleware from "../middleware/authMiddleware.js";
import loadPermissions from "../middleware/loadPermissions.js";
import checkPermission from "../middleware/checkPermission.js";

const router = Router();

/**
 * ًں”¹ طµظ„ط§ط­ظٹط§طھ ط§ظ„ظ‚ط§ط¦ظ…ط© ظ„ظ„ظ…ط³طھط®ط¯ظ… ط§ظ„ط­ط§ظ„ظٹ
 * GET /api/users/me/menu-permissions
 * ظٹظڈط³طھط®ط¯ظ… ظپظٹ admin.js -> fetchMenuPermissions()
 */
router.get(
  "/me/menu-permissions",
  authMiddleware,
  UserController.getMyMenuPermissions
);

// ط¥ظ†ط´ط§ط، ظ…ط³طھط®ط¯ظ… ط¬ط¯ظٹط¯
router.post(
  "/",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.create
);

// ط¹ط±ط¶ ظƒظ„ ط§ظ„ظ…ط³طھط®ط¯ظ…ظٹظ†
router.get(
  "/",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.getAll
);

// ط¹ط±ط¶ ظ…ط³طھط®ط¯ظ… ظˆط§ط­ط¯
router.get(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.getOne
);

// طھط­ط¯ظٹط« ظ…ط³طھط®ط¯ظ…
router.put(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.update
);

// ط­ط°ظپ ظ…ط³طھط®ط¯ظ…
router.delete(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.delete
);

export default router;

=== academicYearRoutes.js ===
// src/routes/academicYearRoutes.js
import express from "express";

import {
  getAcademicYears,
  getActiveAcademicYear,
  createAcademicYear,
  updateAcademicYear,
  deleteAcademicYear,
  setActiveAcademicYear,
} from "../controllers/academicYearController.js";

import authMiddleware from "../middleware/authMiddleware.js";
import  checkPermission  from "../middleware/checkPermission.js";

const router = express.Router();

/**
 * ظ…ظ„ط§ط­ط¸ط§طھ ط¹ظ† ط§ظ„طµظ„ط§ط­ظٹط§طھ:
 * - ط§ط³طھط®ط¯ظ…طھ:
 *    calendar.view_years   ظ„ط¹ط±ط¶ ط§ظ„ط³ظ†ظˆط§طھ
 *    calendar.manage_years ظ„ظ„ط¥ط¶ط§ظپط© ظˆط§ظ„طھط¹ط¯ظٹظ„ ظˆط§ظ„ط­ط°ظپ ظˆط§ظ„طھظپط¹ظٹظ„
 * - ظ„ظˆ ظ…ط§ ط¹ظ†ط¯ظƒ permission ط¨ط§ط³ظ… "calendar.manage_years" ط£ط¶ظگظپظ‡ ظپظٹ ط¬ط¯ظˆظ„ permissions
 *   ظˆط§ط±ط¨ط·ظ‡ ط¨ط¯ظˆط± ط§ظ„ظ€ admin ظپظٹ ط¬ط¯ظˆظ„ role_permissions.
 */

// ط¬ظ„ط¨ ظƒظ„ ط§ظ„ط³ظ†ظˆط§طھ ط§ظ„ط¯ط±ط§ط³ظٹط©
router.get(
  "/",
  authMiddleware,
  checkPermission("calendar.view_years"),
  getAcademicYears
);

// ط¬ظ„ط¨ ط§ظ„ط³ظ†ط© ط§ظ„ط¯ط±ط§ط³ظٹط© ط§ظ„ط­ط§ظ„ظٹط©
router.get(
  "/active",
  authMiddleware,
  checkPermission("calendar.view_years"),
  getActiveAcademicYear
);

// ط¥ظ†ط´ط§ط، ط³ظ†ط© ط¯ط±ط§ط³ظٹط© ط¬ط¯ظٹط¯ط©
router.post(
  "/",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  createAcademicYear
);

// طھط¹ط¯ظٹظ„ ط³ظ†ط© ط¯ط±ط§ط³ظٹط©
router.put(
  "/:id",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  updateAcademicYear
);

// ط­ط°ظپ ط³ظ†ط© ط¯ط±ط§ط³ظٹط©
router.delete(
  "/:id",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  deleteAcademicYear
);

// طھط¹ظٹظٹظ† ط³ظ†ط© ظƒظ€ ط³ظ†ط© ط­ط§ظ„ظٹط©
router.patch(
  "/:id/activate",
  authMiddleware,
  checkPermission("calendar.manage_years"),
  setActiveAcademicYear
);

export default router;

=== activityRoutes.js ===
import express from 'express';
import { getRecentActivities } from '../controllers/activityLogController.js';

// âœ… ط§ظ„ط§ط³طھط¯ط¹ط§ط، ط§ظ„طµط­ظٹط­ ط¨ظ†ط§ط،ظ‹ ط¹ظ„ظ‰ ظ†ط¸ط§ظ…ظƒ
import authMiddleware from '../middleware/authMiddleware.js'; 

const router = express.Router();

// ظ…ط³ط§ط± ظ…ط­ظ…ظٹ ظٹط¬ظ„ط¨ ط£ط­ط¯ط« ط§ظ„ظ†ط´ط§ط·ط§طھ ظ„ظ„ظ…ط¯ط±ط³ط©
router.get('/activities/recent', authMiddleware, getRecentActivities);

export default router;

=== adminAssessmentsRoutes.js ===
import express from 'express';
import authMiddleware from "../middleware/authMiddleware.js";
import { autoActivityLogger } from '../middleware/activityLogger.js';
import { 
    getAdminAssessments, 
    getAssessmentGrades, 
    bulkOverrideGrades ,
    getSubjectsByGradeId
} from '../controllers/adminAssessmentsController.js';

const router = express.Router();

// ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ‡ظ†ط§ طھط­طھط§ط¬ ط¥ظ„ظ‰ طھط³ط¬ظٹظ„ ط¯ط®ظˆظ„
router.use(authMiddleware);

// 1. ط¬ظ„ط¨ ط§ظ„طھظ‚ظٹظٹظ…ط§طھ ط¨ظ†ط§ط،ظ‹ ط¹ظ„ظ‰ ط§ظ„ط´ط¹ط¨ط© ظˆط§ظ„ظ…ط§ط¯ط© (ظ„ظ„ظ‚ط§ط¦ظ…ط© ط§ظ„ظ…ظ†ط³ط¯ظ„ط©)
router.get('/', getAdminAssessments);
router.get('/subjects-by-grade', getSubjectsByGradeId);
// 2. ط¬ظ„ط¨ ط¯ط±ط¬ط§طھ طھظ‚ظٹظٹظ… ظ…ط¹ظٹظ† ظ„ط·ظ„ط§ط¨ ط´ط¹ط¨ط© ظ…ط¹ظٹظ†ط© (ظ„ط±ط³ظ… ط§ظ„ط¬ط¯ظˆظ„)
router.get('/:assessment_id/grades', getAssessmentGrades);

// 3. ط§ط¹طھظ…ط§ط¯ ظˆطھط¹ط¯ظٹظ„ ط§ظ„ط¯ط±ط¬ط§طھ ظ…ظ† ظ‚ط¨ظ„ ط§ظ„ط¥ط¯ط§ط±ط© (ظ…ط¹ ط§ظ„طھط³ط¬ظٹظ„ ط§ظ„ط¢ظ„ظٹ ظپظٹ ط³ط¬ظ„ ط§ظ„طھط¯ظ‚ظٹظ‚)
router.post('/:assessment_id/bulk-override', autoActivityLogger, bulkOverrideGrades);

export default router;

=== adminPermissionsRoutes.js ===
// backend/src/routes/adminPermissionsRoutes.js
import { Router } from "express";
import { listPermissions, decidePermission, overridePermission } from "../controllers/adminPermissionsController.js";

const router = Router();

router.get("/permissions", listPermissions);
router.post("/permissions/:id/decide", decidePermission);
router.post("/permissions/:id/override", overridePermission);

export default router;

=== adminTeacherAttendanceRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { AdminTeacherAttendanceController } from "../controllers/adminTeacherAttendanceController.js";

const router = express.Router();
router.use(authMiddleware);



// ===== Day Summary + Open/Lock =====
router.get("/day", AdminTeacherAttendanceController.daySummary); // ?date=YYYY-MM-DD
router.post("/day/open", AdminTeacherAttendanceController.openDay); // {date}
router.patch("/day/:id/lock", AdminTeacherAttendanceController.lockDay);
router.patch("/day/:id/unlock", AdminTeacherAttendanceController.unlockDay);

// ===== Scan + Manual =====
router.post("/scan", AdminTeacherAttendanceController.scan); // {date, code}
router.post("/entries", AdminTeacherAttendanceController.createEntry); // {date, teacher_id, status, method}
router.patch("/entries/:id", AdminTeacherAttendanceController.updateEntry); // {status, method, reason?}

export default router;

=== adminTeacherPermitsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { AdminTeacherPermitsController } from "../controllers/adminTeacherPermitsController.js";

const router = express.Router();
router.use(authMiddleware);


// list + count
// ظ†ط¸ط§ظ… ط¹ظٹظ† ط§ظ„طµظ‚ط± ظ„ظ„ط­طµطµ ط§ظ„ظ…ط±ظپظˆط¶ط©
router.get("/alerts/rejected-subs", AdminTeacherPermitsController.getRejectedSubsAlerts);
router.get("/", AdminTeacherPermitsController.list); // ?status=&from=&to=&q=&count=1
router.get("/:id", AdminTeacherPermitsController.getOne);
router.patch("/:id/decision", AdminTeacherPermitsController.decide); // {status, decision_note}

export default router;

=== adminTermResultsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getTermResults,
  calculateTermResults,
  approveTermResults,
  publishTermResults,
  unpublishTermResults,
} from "../controllers/adminTermResultsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getTermResults);
router.post("/calculate", calculateTermResults);
router.post("/approve", approveTermResults);
router.post("/publish", publishTermResults);
router.post("/unpublish", unpublishTermResults);

export default router;

=== adminTermWorksRoutes.js ===
import express from "express";
import {
  getTermWorks,
  approveTermWorks,
  returnTermWorks,
} from "../controllers/adminTermWorksController.js";
import {
  listMonthlyAssessments,
  getMonthlyWorks,
  approveMonthlyWorks,
  returnMonthlyWorks,
} from "../controllers/adminMonthlyWorksController.js";
const router = express.Router();

router.get("/monthly-works/assessments", listMonthlyAssessments);
router.get("/monthly-works", getMonthlyWorks);
router.post("/monthly-works/approve", approveMonthlyWorks);
router.post("/monthly-works/return", returnMonthlyWorks);

router.get("/term-works", getTermWorks);
router.post("/term-works/approve", approveTermWorks);
router.post("/term-works/return", returnTermWorks);

export default router;

=== aiChatRoutes.js ===
// import express from "express";
// import { aiChatController } from "../controllers/aiChatController.js";

// const router = express.Router();

// router.post("/", aiChatController.chat); // POST /api/ai-chat

// export default router;

=== assessmentReopenRequestsRoutes.js ===
// src/routes/assessmentReopenRequestsRoutes.js
import { Router } from "express";
import { AssessmentReopenRequestsController } from "../controllers/assessmentReopenRequestsController.js";

const router = Router();

router.get("/", AssessmentReopenRequestsController.list);

router.post("/:id/approve", AssessmentReopenRequestsController.approve);

router.post("/:id/reject", AssessmentReopenRequestsController.reject);

export default router;

=== assignTeachersRoutes.js ===
// src/routes/assignTeachersRoutes.js
import { Router } from "express";
import { meta, sectionView, saveSection } from "../controllers/assignTeachersController.js";

// ط¥ط°ط§ ط¹ظ†ط¯ظƒ authMiddleware/checkPermission ط§ط³طھط®ط¯ظ…ظ‡ظ… ظ‡ظ†ط§
import  authMiddleware  from "../middleware/authMiddleware.js";
// import { checkPermission } from "../middleware/checkPermission.js";

const router = Router();

router.use(authMiddleware);
// router.use(checkPermission("timetables.assign_teachers"));

router.get("/meta", meta);
router.get("/section", sectionView);
router.post("/section", saveSection);

export default router;

=== attendanceReportsRoutes.js ===
// routes/attendanceReportsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط§ط³طھط¯ط¹ط§ط، ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط±
import attendanceReportsController from "../controllers/attendanceReportsController.js";

const router = express.Router();

// âœ… طھط·ط¨ظٹظ‚ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ظ…ط³ط§ط±ط§طھ ط§ظ„طھظ‚ط§ط±ظٹط± ظ„طھظˆظپظٹط± ط¨ظٹط§ظ†ط§طھ ط§ظ„ظ…ط¯ط±ط³ط© (school_id)
router.use(authMiddleware);

// Students
router.get("/attendance/students", attendanceReportsController.getStudentsAttendanceReport);
router.get("/attendance/students/:id/details", attendanceReportsController.getStudentAttendanceDetails);

// Teachers
router.get("/attendance/teachers", attendanceReportsController.getTeachersAttendanceReport);
router.get("/attendance/teachers/:id/details", attendanceReportsController.getTeacherAttendanceDetails);

export default router;

=== authRoutes.js ===
// src/routes/authRoutes.js
import { Router } from "express";
import { AuthController } from "../controllers/authController.js";
import { rateLimiter } from "../middleware/rateLimiter.js";
import jwt from "jsonwebtoken";
import { logAudit } from "../utils/auditLogger.js";

const router = Router();

const loginLimiter = rateLimiter({
  windowMs: 15 * 60 * 1000, // 15 ط¯ظ‚ظٹظ‚ط©
  max: 10, // ط§ظ„ط­ط¯ ط§ظ„ط£ظ‚طµظ‰ 10 ظ…ط­ط§ظˆظ„ط§طھ
  message: "ظ„ظ‚ط¯ ظ‚ظ…طھ ط¨ظ…ط­ط§ظˆظ„ط§طھ طھط³ط¬ظٹظ„ ط¯ط®ظˆظ„ ظƒط«ظٹط±ط© ط¬ط¯ط§ظ‹. ظٹط±ط¬ظ‰ ط§ظ„ظ…ط­ط§ظˆظ„ط© ط¨ط¹ط¯ 15 ط¯ظ‚ظٹظ‚ط©."
});

router.post("/login", loginLimiter, AuthController.login);

router.post("/logout", async (req, res) => {
  try {
    let token = null;
    if (req.headers.cookie) {
      const cookies = {};
      req.headers.cookie.split(";").forEach((cookie) => {
        const parts = cookie.split("=");
        if (parts.length >= 2) {
          cookies[parts[0].trim()] = decodeURIComponent(parts.slice(1).join("="));
        }
      });
      token = cookies.token;
    }
    if (!token) {
      const authHeader = req.headers.authorization || "";
      const match = authHeader.match(/^Bearer\s+(.+)$/i);
      token = match ? match[1]?.trim() : null;
    }
    if (token && process.env.JWT_SECRET) {
      const decoded = jwt.verify(token, process.env.JWT_SECRET);
      if (decoded && decoded.id) {
        await logAudit({
          action: "LOGOUT",
          actionLabel: "طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬",
          module: "Security",
          tableName: "users",
          recordId: decoded.id,
          description: `ظ‚ط§ظ… ط§ظ„ظ…ط³طھط®ط¯ظ… ط¨طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬ ظ…ظ† ط§ظ„ظ†ط¸ط§ظ…`,
          schoolIdFallback: decoded.school_id,
          userIdFallback: decoded.id,
          userNameFallback: decoded.role || "ظ…ط³طھط®ط¯ظ…",
          userRoleFallback: decoded.role || "system"
        });
      }
    }
  } catch (err) {
    // Ignore error, we still want to log out
  }
  res.clearCookie("token");
  return res.json({ success: true, message: "طھظ… طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬ ط¨ظ†ط¬ط§ط­" });
});

export default router;

=== backupRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";
import multer from "multer";
import {
  getSettings,
  updateSettings,
  getLogs,
  runManualBackup,
  downloadBackup,
  deleteBackup,
  browseDirectories,
  restoreBackup,
  getGoogleDriveStatus,      // ًںں¢ ط¥ط¶ط§ظپط© ط§ط³طھظٹط±ط§ط¯ ط¯ط§ظ„ط© ط§ظ„ط­ط§ظ„ط©
  disconnectGoogleDrive      // ًںں¢ ط¥ط¶ط§ظپط© ط§ط³طھظٹط±ط§ط¯ ط¯ط§ظ„ط© ظپطµظ„ ط§ظ„ط­ط³ط§ط¨
} from "../controllers/backupController.js";
const upload = multer({ dest: "uploads/temp/" });
const router = Router();

// طھط£ظ…ظٹظ† ظƒط§ظپط© ظ…ط³ط§ط±ط§طھ ط§ظ„ظ†ط³ط® ط§ظ„ط§ط­طھظٹط§ط·ظٹ ط¨طµظ„ط§ط­ظٹط§طھ ط¥ط¯ط§ط±ظٹط© ظƒط§ظ…ظ„ط© ظ„ظ„ظ…ط¯ط±ط³ط©
router.use(authMiddleware);

router.get("/settings", getSettings);
router.post("/settings", updateSettings);
// ًںں¢ ظ…ط³ط§ط±ط§طھ ط¥ط¯ط§ط±ط© ط§ظ„طھط²ط§ظ…ظ† ط§ظ„ط³ط­ط§ط¨ظٹ ظ…ط¹ ظ‚ظˆظ‚ظ„ ط¯ط±ط§ظٹظپ (BYOD)
router.get("/google-drive-status", getGoogleDriveStatus);
router.post("/google-drive-disconnect", disconnectGoogleDrive);
router.get("/logs", getLogs);
router.post("/run-manual", runManualBackup);
router.get("/download/:id", downloadBackup);
router.get("/browse-directories", browseDirectories);
router.post("/restore", upload.single("backupFile"), restoreBackup);
router.delete("/:id", deleteBackup);

export default router;


=== certificatesRoutes.js ===
// backend/src/routes/certificatesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { CertificatesController } from "../controllers/certificatesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/meta", CertificatesController.meta);

router.get("/:type/students", CertificatesController.students);
router.get("/:type", CertificatesController.list);

router.post("/:type", CertificatesController.create);
router.post("/:type/:id/printed", CertificatesController.markPrinted);

router.delete("/:type/:id", CertificatesController.remove);

export default router;

=== continuingRoutes.js ===
import express from "express";
import {
  getContinuingMeta,
  listContinuingStudents,
  registerContinuingStudents,
} from "../controllers/continuingStudentsController.js";

const router = express.Router();

router.get("/continuing-students/meta", getContinuingMeta);
router.get("/continuing-students/students", listContinuingStudents);
router.post("/continuing-students/register", registerContinuingStudents);

export default router;

=== dashboardRoutes.js ===
// src/routes/dashboardRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";

import {
  getDashboardStats,
  getAdminHomeDashboard,
} from "../controllers/dashboardController.js";

const router = Router();

// ط§ظ„ط¥ط­طµط§ط¦ظٹط§طھ ط§ظ„ظ‚ط¯ظٹظ…ط© ط§ظ„ظ…ظˆط¬ظˆط¯ط© ط­ط§ظ„ظٹظ‹ط§
router.get("/stats", authMiddleware, getDashboardStats);

// ط§ظ„ط¯ط§ط´ط¨ظˆط±ط¯ ط§ظ„ط¬ط¯ظٹط¯ط© ط§ظ„ظƒط§ظ…ظ„ط©
router.get("/admin-home", authMiddleware, getAdminHomeDashboard);

export default router;

=== employeesRoutes.js ===
// src/routes/employeesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  employeesMeta,
  employeesList,
  employeeGet,
  employeeCreate,
  employeeUpdate,
  employeeDelete,
  employeeSetActive,
} from "../controllers/employeesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/meta", employeesMeta);
router.get("/", employeesList);
router.get("/:id", employeeGet);
router.post("/", employeeCreate);
router.put("/:id", employeeUpdate);
router.patch("/:id/active", employeeSetActive);
router.delete("/:id", employeeDelete);

export default router;

=== examTimetablesRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import ExamTimetablesController from "../controllers/examTimetablesController.js";

const router = Router();

// âœ… Meta (dropdowns)
router.get("/meta", authMiddleware, ExamTimetablesController.meta);

// âœ… List (ظ„ط§ط²ظ… ظ‚ط¨ظ„ /:id)
router.get("/list", authMiddleware, ExamTimetablesController.list);

// Get or create timetable header
router.post("/get-or-create", authMiddleware, ExamTimetablesController.getOrCreate);

// Copy from another exam timetable
router.post("/:id/copy-from", authMiddleware, ExamTimetablesController.copyFrom);

// Publish / Unpublish
router.put("/:id/publish", authMiddleware, ExamTimetablesController.publish);
router.put("/:id/unpublish", authMiddleware, ExamTimetablesController.unpublish);

// Save draft entries (bulk replace)
router.put("/:id/entries", authMiddleware, ExamTimetablesController.saveEntries);

// Clear entries
router.delete("/:id/entries", authMiddleware, ExamTimetablesController.clearEntries);

// Delete timetable (draft only)
router.delete("/:id", authMiddleware, ExamTimetablesController.remove);

// Get timetable + entries
router.get("/:id", authMiddleware, ExamTimetablesController.getById);

export default router;

=== feeAdjustmentRequestsRoutes.js ===
// src/routes/feeAdjustmentRequestsRoutes.js
import { Router } from "express";
import { FeeAdjustmentRequestsController } from "../controllers/feeAdjustmentRequestsController.js";

const router = Router();

router.get("/", FeeAdjustmentRequestsController.list);

router.post("/", FeeAdjustmentRequestsController.create);

router.post("/:id/approve", FeeAdjustmentRequestsController.approve);

router.post("/:id/reject", FeeAdjustmentRequestsController.reject);

export default router;

=== feeRulesRoutes.js ===
// src/routes/feeRulesRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";

import {
  listFeeRules,
  createFeeRule,
  updateFeeRule,
  deleteFeeRule,
  listStudentsFeesView,
} from "../controllers/feeRulesController.js";

const router = Router();

// âڑ ï¸ڈ ط¹ط¯ظ‘ظ„ ط§ط³ظ… ط§ظ„طµظ„ط§ط­ظٹط© ط­ط³ط¨ ط¬ط¯ظˆظ„ permissions ط¹ظ†ط¯ظƒ

router.get(
  "/",
  authMiddleware,
  listFeeRules
);

router.get(
  "/students",
  authMiddleware,
  listStudentsFeesView
);

router.post(
  "/",
  authMiddleware,
  createFeeRule
);

router.put(
  "/:id",
  authMiddleware,
  updateFeeRule
);

router.delete(
  "/:id",
  authMiddleware,
  deleteFeeRule
);

export default router;

=== feesRequestsRoutes.js ===
// import express from "express";
// import authMiddleware from "../middleware/authMiddleware.js";
// import { uploadFeesRequestImages } from "../middleware/uploadFeesRequests.js";
// import { FeesRequestsController } from "../controllers/feesRequestsController.js";

// const router = express.Router();

// // ط¥ظ†ط´ط§ط، ط·ظ„ط¨ ط¯ظپط¹ (multipart/form-data) + طµظˆط± ظ…طھط¹ط¯ط¯ط©
// router.post("/fees/requests", authMiddleware, (req, res, next) => {
//   uploadFeesRequestImages(req, res, (err) => {
//     if (err) return res.status(400).json({ message: err.message });
//     next();
//   });
// }, FeesRequestsController.create);

// router.get("/fees/requests", authMiddleware, FeesRequestsController.list);
// router.get("/fees/requests/:id", authMiddleware, FeesRequestsController.read);

// router.post("/fees/requests/:id/approve", authMiddleware, FeesRequestsController.approve);
// router.post("/fees/requests/:id/reject", authMiddleware, FeesRequestsController.reject);

// export default router;

=== feesRoutes.js ===
import { Router } from "express";
import uploadFeesRequests from "../middleware/uploadFeesRequests.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط£ط¶ظپظ†ط§ ظ‡ط°ط§

import {
  getContract,
  createContract,
  updateContract,     // âœ… ط§ط³طھط¯ط¹ط§ط، ط¯ط§ظ„ط© ط§ظ„طھط¹ط¯ظٹظ„ ط§ظ„ط¬ط¯ظٹط¯ط©
  getInstallments,
  getPayments,
  createPayment,
  reportCollections,
  reportOutstanding,
  confirmPayment, 
} from "../controllers/feesController.js";

const router = Router();

// ==========================================
// ًں”’ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ…ط­ظ…ظٹط© ط¨ظ€ authMiddleware
// ==========================================

// ط¥ط¯ط§ط±ط© ط§ظ„ط¹ظ‚ظˆط¯ ظˆط§ظ„ط£ظ‚ط³ط§ط·
router.get("/fees/contract", authMiddleware, getContract);
router.post("/fees/contracts", authMiddleware, createContract);
router.put("/fees/contracts/:id", authMiddleware, updateContract); // âœ… ظ…ط³ط§ط± ط§ظ„طھط¹ط¯ظٹظ„ ظˆط¥ط¹ط§ط¯ط© ط§ظ„ط¬ط¯ظˆظ„ط©

router.get("/fees/installments", authMiddleware, getInstallments);

// ط¥ط¯ط§ط±ط© ط§ظ„ط¯ظپط¹ط§طھ
router.get("/fees/payments", authMiddleware, getPayments);
router.post("/fees/payments", authMiddleware, uploadFeesRequests, createPayment);

// ط§ط¹طھظ…ط§ط¯ ط¯ظپط¹ط© (ظ‚ظٹط¯ ط§ظ„ظ…ط±ط§ط¬ط¹ط© -> ظ…ط¤ظƒط¯)
router.patch("/fees/payments/:id/confirm", authMiddleware, confirmPayment);

// ط§ظ„طھظ‚ط§ط±ظٹط±
router.get("/fees/reports/collections", authMiddleware, reportCollections);
router.get("/fees/reports/outstanding", authMiddleware, reportOutstanding);

export default router;

=== googleAuthRouter.js ===
import { Router } from 'express';
import { google } from 'googleapis';
import jwt from 'jsonwebtoken';
import { pool } from '../config/db.js';

const router = Router();

// ط¯ط§ظ„ط© ظ…ط³ط§ط¹ط¯ط© ظ„طھظ‡ظٹط¦ط© ط§ظ„ط¹ظ…ظٹظ„
function getOAuth2Client() {
  return new google.auth.OAuth2(
    process.env.GOOGLE_CLIENT_ID,
    process.env.GOOGLE_CLIENT_SECRET,
    process.env.GOOGLE_REDIRECT_URI
  );
}

// ًںں¢ 1. ظ…ط³ط§ط± ط§ظ„طھظˆط¬ظٹظ‡ ط¥ظ„ظ‰ طµظپط­ط© ط¬ظˆط¬ظ„ ط§ظ„ط±ط³ظ…ظٹط© ظ„ظ…ظ†ط­ ط§ظ„طµظ„ط§ط­ظٹط§طھ
router.get('/google', async (req, res) => {
  try {
    const token = req.query.token;
    if (!token) return res.status(401).send('ط؛ظٹط± ظ…طµط±ط­: ط§ظ„طھظˆظƒظ† ظ…ظپظ‚ظˆط¯');

    // ظپظƒ طھط´ظپظٹط± ط§ظ„طھظˆظƒظ† ظ„ظ…ط¹ط±ظپط© ظ…ظ† ظ‡ظٹ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„طھظٹ طھط·ظ„ط¨ ط§ظ„ط±ط¨ط· ط§ظ„ط¢ظ†
    // (ط§ط³طھط¨ط¯ظ„ JWT_SECRET ط¨ظ…طھط؛ظٹط± ط§ظ„ط¨ظٹط¦ط© ط§ظ„ط®ط§طµ ط¨ظƒ ظپظٹ ظ…ط´ط±ظˆط¹ظƒ)
    const decoded = jwt.verify(token, process.env.JWT_SECRET || 'secret');
    const schoolId = decoded.school_id; 

    const oauth2Client = getOAuth2Client();

    // طھظˆظ„ظٹط¯ ط±ط§ط¨ط· ط§ظ„طھط­ظ‚ظ‚ ظ…ط¹ ط·ظ„ط¨ طµظ„ط§ط­ظٹط© ط§ظ„ظˆطµظˆظ„ ظ„ظ„ظ…ظ„ظپط§طھ ظˆط·ظ„ط¨ ط§ظ„طھظˆظƒظ† ط§ظ„ط¯ط§ط¦ظ… (offline)
    const authUrl = oauth2Client.generateAuthUrl({
      access_type: 'offline', // ط¥ظ„ط²ط§ظ…ظٹ ظ„ظ„ط­طµظˆظ„ ط¹ظ„ظ‰ ط§ظ„ظ€ Refresh Token ط§ظ„ط«ط§ط¨طھ ظ„ظ„ط±ظپط¹ ط§ظ„طھظ„ظ‚ط§ط¦ظٹ
      prompt: 'consent',     // ط¥ط¬ط¨ط§ط± ظ‚ظˆظ‚ظ„ ط¹ظ„ظ‰ ط¥ط¸ظ‡ط§ط± ط´ط§ط´ط© ط§ظ„ظ…ظˆط§ظپظ‚ط© ظ„طھظˆظ„ظٹط¯ ط§ظ„طھظˆظƒظ†
      scope: ['https://www.googleapis.com/auth/drive.file'], // ط§ظ„ظˆطµظˆظ„ ظپظ‚ط· ظ„ظ„ظ…ظ„ظپط§طھ ط§ظ„طھظٹ ظٹظ†ط´ط¦ظ‡ط§ ط³ظٹط³طھظ…ظƒ ظ„ط­ظ…ط§ظٹط© ط®طµظˆطµظٹطھظ‡ظ…
      state: String(schoolId) // طھظ…ط±ظٹط± ط±ظ‚ظ… ط§ظ„ظ…ط¯ط±ط³ط© ظƒظ…ط¹ط§ظ…ظ„ ط£ظ…ط§ظ† ظ„ظژظ‚ط· ط§ظ„ط­ط§ظ„ط© ط¹ظ†ط¯ ط§ظ„ط¹ظˆط¯ط©
    });

    return res.redirect(authUrl);
  } catch (err) {
    console.error('Google Auth Init Error:', err.message);
    return res.status(500).send('ط­ط¯ط« ط®ط·ط£ ط£ط«ظ†ط§ط، ط¨ط¯ط، ط§ظ„ط§طھطµط§ظ„ ط¨ط³ظٹط±ظپط±ط§طھ ط¬ظˆط¬ظ„');
  }
});

// ًںں¢ 2. ظ…ط³ط§ط± ط§ظ„ط§ط³طھظ‚ط¨ط§ظ„ (Callback URL) ط§ظ„ط°ظٹ ظٹط¹ظˆط¯ ط¥ظ„ظٹظ‡ ط§ظ„ظ…طھطµظپط­ ط¨ط¹ط¯ ظ…ظˆط§ظپظ‚ط© ط§ظ„ط¹ظ…ظٹظ„
router.get('/google/callback', async (req, res) => {
  const { code, state } = req.query; // code = ط´ظپط±ط© ط¬ظˆط¬ظ„طŒ state = ط±ظ‚ظ… ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ظ…ظ…ط±ط± ط³ط§ط¨ظ‚ط§ظ‹
  const schoolId = parseInt(state, 10);

  if (!code || !schoolId) {
    return res.status(400).send('ط¨ظٹط§ظ†ط§طھ ط§ظ„طھط­ظ‚ظ‚ ظ…ظ† ط¬ظˆط¬ظ„ ط؛ظٹط± ظ…ظƒطھظ…ظ„ط©');
  }

  try {
    const oauth2Client = getOAuth2Client();
    
    // طھط¨ط§ط¯ظ„ ط§ظ„ط´ظپط±ط© ط§ظ„ظ…ط¤ظ‚طھط© ط¨ط§ظ„طھظˆظƒظ†ط§طھ ط§ظ„ط¯ط§ط¦ظ…ط© ظ…ظ† ط¬ظˆط¬ظ„
    const { tokens } = await oauth2Client.getToken(code);
    
    if (!tokens.refresh_token) {
      throw new Error('ظ„ظ… ظٹطھظ… ط¥ط±ط¬ط§ط¹ Refresh Token. ظٹط±ط¬ظ‰ ط¥ظ„ط؛ط§ط، ط§ظ„ط±ط¨ط· ظˆط¥ط¹ط§ط¯ط© ط§ظ„ظ…ط­ط§ظˆظ„ط© ظ…ط¬ط¯ط¯ط§ظ‹.');
    }

    // طھظ‡ظٹط¦ط© ط¹ظ…ظٹظ„ ط§ظ„ظ€ Drive ظ„ط¥ظ†ط´ط§ط، ظ…ط¬ظ„ط¯ ظ…ط¹ط²ظˆظ„ ظ„ظ„ظ…ط¯ط±ط³ط© ظپظˆط±ط§ظ‹
    oauth2Client.setCredentials(tokens);
    const drive = google.drive({ version: 'v3', auth: oauth2Client });

    // ط¬ظ„ط¨ ط§ط³ظ… ط§ظ„ظ…ط¯ط±ط³ط© ظ…ظ† ط§ظ„ط¯ط§طھط§ط¨ظٹط² ظ„طھط³ظ…ظٹط© ط§ظ„ظ…ط¬ظ„ط¯ ط¨ط§ط³ظ… ظ…ظ†ط´ط£طھظ‡ظ… ط¨ط´ظƒظ„ ط§ط­طھط±ط§ظپظٹ
    const schoolRes = await pool.query('SELECT name_ar FROM schools WHERE id = $1', [schoolId]);
    const schoolName = schoolRes.rows[0]?.name_ar || `School_${schoolId}`;

    // ط¥ط¹ط¯ط§ط¯ ط¨ظٹط§ظ†ط§طھ ط§ظ„ظ…ط¬ظ„ط¯ ط§ظ„ظپط±ظٹط¯ ظ„ظ„ظ…ط³طھط£ط¬ط±
    const folderMetadata = {
      name: `ظ…ظ†طµطھظٹ ط§ظ„طھط¹ظ„ظٹظ…ظٹط© - ظ†ط³ط® ط§ط­طھظٹط§ط·ظٹ (${schoolName})`,
      mimeType: 'application/vnd.google-apps.folder'
    };

    const folder = await drive.files.create({
      requestBody: folderMetadata,
      fields: 'id'
    });

    const folderId = folder.data.id; // ظ…ط¹ط±ظ‘ظپ ط§ظ„ظ…ط¬ظ„ط¯ ط§ظ„ظپط±ظٹط¯ ط§ظ„ظ…ط´ظپط± ظ…ظ† ط¬ظˆط¬ظ„

    // ط­ظپط¸ ط§ظ„طھظˆظƒظ† ظˆظ…ط¹ط±ظ‘ظپ ط§ظ„ظ…ط¬ظ„ط¯ ظپظٹ ط¬ط¯ظˆظ„ ط§ظ„ط¥ط¹ط¯ط§ط¯ط§طھ ط§ظ„ط®ط§طµ ط¨ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط©
    await pool.query(
      `INSERT INTO backup_settings (school_id, google_drive_refresh_token, google_drive_folder_id, updated_at)
       VALUES ($1, $2, $3, NOW())
       ON CONFLICT (school_id) DO UPDATE 
       SET google_drive_refresh_token = EXCLUDED.google_drive_refresh_token,
           google_drive_folder_id = EXCLUDED.google_drive_folder_id,
           updated_at = NOW()`,
      [schoolId, tokens.refresh_token, folderId]
    );

    // ًںژ¯ ط¥ط¹ط§ط¯ط© طھظˆط¬ظٹظ‡ ط§ظ„ظ…ط³طھط®ط¯ظ… ظ„طµظپط­ط© ط§ظ„ط¥ط¹ط¯ط§ط¯ط§طھ ظ„ظˆط­ط© ط§ظ„طھط­ظƒظ… ظپظٹ ط§ظ„ظپط±ظˆظ†طھ ط¥ظ†ط¯ ط¨ظ†ط¬ط§ط­
    // (ظ‚ظ… ط¨طھط­ط¯ظٹط« ط§ظ„ظ…ط³ط§ط± ط§ظ„ظ†طµظٹ ظ„ظٹطھط·ط§ط¨ظ‚ ظ…ط¹ ظ…ط¬ظ„ط¯ط§طھ ط§ظ„ط¹ط±ط¶ ظ„ط¯ظٹظƒ)
    return res.redirect('/frontend/settings/settings.html?tab=backups&status=success');

  } catch (err) {
    console.error('Google OAuth Callback Error:', err.message);
    return res.redirect(`/frontend/settings/settings.html?tab=backups&status=error&msg=${encodeURIComponent(err.message)}`);
  }
});

export default router;

=== gradeRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import loadPermissions from "../middleware/loadPermissions.js";
import checkPermission from "../middleware/checkPermission.js";
import { getGrades } from "../controllers/gradeController.js";

const router = Router();

router.get(
  "/",
  authMiddleware,
  loadPermissions,
  // checkPermission("grades.view"),
  getGrades
);

export default router;

=== metaRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getAcademicYears,
  getGrades,
  getClasses,
  searchStudents,
} from "../controllers/metaController.js";

const router = Router();

router.use(authMiddleware);

router.get("/academic-years", getAcademicYears);
router.get("/grades", getGrades);
router.get("/classes", getClasses);
router.get("/students/search", searchStudents);

export default router;

=== moduleRoutes.js ===
import express from "express";
import {
  addModule,
  fetchModules,
  fetchModuleById,
  editModule,
  removeModule,
} from "../controllers/moduleController.js";
import authMiddleware from "../middleware/authMiddleware.js";
import loadPermissions from "../middleware/loadPermissions.js";
import checkPermission from "../middleware/checkPermission.js";

const router = express.Router();

/**
 * ظ…ظ„ط§ط­ط¸ط© ظ…ظ‡ظ…ط©:
 * طµظپط­ط© "ط§ظ„ط£ط¯ظˆط§ط±" ظˆ "ط§ظ„طµظ„ط§ط­ظٹط§طھ" طھط­طھط§ط¬ ظپظ‚ط· ط£ظ† طھظ‚ط±ط£ ظ‚ط§ط¦ظ…ط© ط§ظ„ظˆط­ط¯ط§طھطŒ
 * ط¹ط´ط§ظ† طھط¹ط¨ظٹ ط§ظ„ظ€ select ظپظٹ ط§ظ„ظˆط§ط¬ظ‡ط©طŒ ظ…ظˆ ط¨ط§ظ„ط¶ط±ظˆط±ط© ظٹط¹ط¯ظ‘ظ„ ط§ظ„ظˆط­ط¯ط§طھ.
 *
 * ظ„ط°ظ„ظƒ:
 *  - GET /modules: ظ†ط³ظ…ط­ ط¨ظ‡ ظ„ط£ظٹ ط¯ظˆط± ط¹ظ†ط¯ظ‡ ظˆط§ط­ط¯ط© ظ…ظ† طµظ„ط§ط­ظٹط§طھ RBAC.
 *  - POST/PUT/DELETE: ظ†ط±ط¨ط·ظ‡ط§ ط¨طµظ„ط§ط­ظٹط© rbac.manage_modules ظپظ‚ط·.
 */

// ظ…ط³ط§ط¹ط¯ط©: ط¯ط§ظ„ط© ظ„ظپط­طµ ط£ظٹ طµظ„ط§ط­ظٹط© ظ…ظ† ظ…ط¬ظ…ظˆط¹ط©
import PermissionRoleModel from "../modules/permissionRoleModel.js";

function checkAnyPermission(codes = []) {
  return async function (req, res, next) {
    try {
      const user = req.user;
      if (!user) {
        return res.status(401).json({ message: "ط؛ظٹط± ظ…طµط±ط­" });
      }

      const roleName = String(user.role || user.role_name || "").toLowerCase();

      // ط§ظ„ط£ط¯ظ…ظ€ظ† ط§ظ„ظƒط§ظ…ظ„ ظٹطھط®ط·ظ‰ ط§ظ„ظپط­طµ
      if (
        roleName === "admin" ||
        roleName === "superadmin"
      ) {
        return next();
      }

      // ظ„ظˆ ط¹ظ†ط¯ظ‡ user.permissions ط¬ط±ط¨ ظ†ط¨ط­ط« ظپظٹظ‡ط§
      if (Array.isArray(user.permissions)) {
        const hasFromArray = codes.some((c) =>
          user.permissions.includes(c)
        );
        if (hasFromArray) return next();
      }

      // ظپط­طµ ظ…ظ† ظ‚ط§ط¹ط¯ط© ط§ظ„ط¨ظٹط§ظ†ط§طھ: ظ‡ظ„ ط§ظ„ط¯ظˆط± ظٹظ…ظ„ظƒ ط£ظٹ ظ…ظ† ظ‡ط°ظ‡ ط§ظ„طµظ„ط§ط­ظٹط§طھطں
      const hasAny = await PermissionRoleModel.roleHasAnyPermission(
        user.role_id,
        codes
      );
      if (!hasAny) {
        return res
          .status(403)
          .json({ message: "ظ„ظٹط³ ظ„ط¯ظٹظƒ طµظ„ط§ط­ظٹط© ظ„طھظ†ظپظٹط° ظ‡ط°ط§ ط§ظ„ط¥ط¬ط±ط§ط،" });
      }

      return next();
    } catch (err) {
      console.error("checkAnyPermission error:", err);
      return res
        .status(500)
        .json({ message: "ط®ط·ط£ ظپظٹ ط§ظ„طھط­ظ‚ظ‚ ظ…ظ† طµظ„ط§ط­ظٹط§طھ ط§ظ„ظ…ط³طھط®ط¯ظ…" });
    }
  };
}

// ======================= ط§ظ„ط±ط§ظˆطھط§طھ =======================

// ط¹ط±ط¶ ظƒظ„ ط§ظ„ظˆط­ط¯ط§طھ (ظ‚ط±ط§ط،ط© ظپظ‚ط·)
// ظٹظƒظپظٹ ط£ظ† ظٹظƒظˆظ† ط¹ظ†ط¯ظ‡ ط£ظٹ طµظ„ط§ط­ظٹط© ظ…ظ† طµظ„ط§ط­ظٹط§طھ RBAC
router.get(
  "/",
  authMiddleware,
  loadPermissions,
  checkAnyPermission([
    "rbac.manage_modules",
    "rbac.manage_permissions",
    "rbac.manage_roles",
    "rbac.manage_users",
  ]),
  fetchModules
);

// ط¥ظ†ط´ط§ط، ظˆط­ط¯ط©
router.post(
  "/",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  addModule
);

// ط¬ظ„ط¨ ظˆط­ط¯ط© ظˆط§ط­ط¯ط©
router.get(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  fetchModuleById
);

// طھط¹ط¯ظٹظ„ ظˆط­ط¯ط©
router.put(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  editModule
);

// ط­ط°ظپ ظˆط­ط¯ط©
router.delete(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_modules"),
  removeModule
);

export default router;

=== monthlyCertificatesRoutes.js ===
// backend/src/routes/monthlyCertificatesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { MonthlyCertificatesController } from "../controllers/monthlyCertificatesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/meta", MonthlyCertificatesController.meta);
router.get("/students", MonthlyCertificatesController.students);
router.get("/", MonthlyCertificatesController.list);

router.post("/", MonthlyCertificatesController.create);
router.post("/:id/printed", MonthlyCertificatesController.markPrinted);

router.delete("/:id", MonthlyCertificatesController.remove);

export default router;

=== notificationsAdminRoutes.js ===
// backend/src/routes/notificationsAdminRoutes.js
import express from "express";
import {
  previewRecipients,
  sendManual,
  listSentLog,
  sentLogDetails,
  lookupStagesHandler,
  lookupGradesHandler,
  lookupSectionsHandler,
  lookupStudentsHandler,
  lookupTeachersHandler,
  lookupGuardiansHandler,
} from "../controllers/notificationsAdminController.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط¶ط±ظˆط±ظٹ ط¬ط¯ط§ظ‹ ظ„ط§ط³طھط®ط±ط§ط¬ school_id
import checkPermission from "../middleware/checkPermission.js"; // âœ… ط§ط®طھظٹط§ط±ظٹ ط­ط³ط¨ ظ†ط¸ط§ظ…ظƒ
import multer from "multer";
import fs from "fs";
import path from "path";
import crypto from "crypto";

const router = express.Router();

// --- ط¥ط¹ط¯ط§ط¯ط§طھ ط±ظپط¹ ط§ظ„ظ…ظ„ظپط§طھ (ط§ظ„ظ…ط±ظپظ‚ط§طھ) ---
const UPLOAD_DIR = path.join(process.cwd(), "uploads", "notifications");
if (!fs.existsSync(UPLOAD_DIR)) {
  fs.mkdirSync(UPLOAD_DIR, { recursive: true });
}

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => cb(null, UPLOAD_DIR),
  filename: (_req, file, cb) => {
    const ext = path.extname(file.originalname || "");
    // طھط³ظ…ظٹط© ظپط±ظٹط¯ط© ظ„ظ…ظ†ط¹ طھط¯ط§ط®ظ„ ط§ظ„ظ…ظ„ظپط§طھ
    cb(null, `${Date.now()}-${crypto.randomBytes(6).toString("hex")}${ext}`);
  },
});

const upload = multer({
  storage,
  limits: { files: 10, fileSize: 25 * 1024 * 1024 }, // ط­ط¯ ط£ظ‚طµظ‰ 25 ظ…ظٹط¬ط§
});

/**
 * ًں”’ ط­ظ…ط§ظٹط© ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ:
 * ط¬ظ…ظٹط¹ ظ‡ط°ظ‡ ط§ظ„ط¹ظ…ظ„ظٹط§طھ طھطھط·ظ„ط¨ ط£ظ† ظٹظƒظˆظ† ط§ظ„ظ…ط³طھط®ط¯ظ… ظ…ط³ط¬ظ„ ط¯ط®ظˆظ„
 * ظ„ظƒظٹ ظٹطھظ…ظƒظ† ط§ظ„ظ†ط¸ط§ظ… ظ…ظ† ظ…ط¹ط±ظپط© ظ…ط¯ط±ط³ط© ط§ظ„ظ…ط³طھط®ط¯ظ… (req.user.school_id)
 */

// ===== Lookups ظ„ظ„ظˆط§ط¬ظ‡ط© (ط§ظ„ط¨ط­ط« ط§ظ„ظ…ط®طµطµ ظ„ظ„ظ…ط¯ط±ط³ط©) =====
router.get("/lookups/stages", authMiddleware, lookupStagesHandler);
router.get("/lookups/grades", authMiddleware, lookupGradesHandler);
router.get("/lookups/sections", authMiddleware, lookupSectionsHandler);
router.get("/lookups/students", authMiddleware, lookupStudentsHandler);
router.get("/lookups/teachers", authMiddleware, lookupTeachersHandler);
router.get("/lookups/guardians", authMiddleware, lookupGuardiansHandler);

// ===== ط¹ظ…ظ„ظٹط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ ظˆط§ظ„ظ…ط¹ط§ظٹظ†ط© =====

// ظ…ط¹ط§ظٹظ†ط© ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† (ظٹطھط·ظ„ط¨ طµظ„ط§ط­ظٹط© ط§ظ„ط¥ط±ط³ط§ظ„)
router.post(
  "/preview-recipients", 
  authMiddleware, 
  // checkPermission?.("notifications.send"), // ط§ط®طھظٹط§ط±ظٹ: طھط£ظƒط¯ ط£ظ† ط§ظ„ط¯ط§ظ„ط© ظ…ط¯ط¹ظˆظ…ط© ط¹ظ†ط¯ظƒ
  previewRecipients
);

// ط¥ط±ط³ط§ظ„ ط¥ط´ط¹ط§ط± ظٹط¯ظˆظٹ ظ…ط¹ ط§ظ„ظ…ط±ظپظ‚ط§طھ
router.post(
  "/send", 
  authMiddleware, 
  // checkPermission?.("notifications.send"), 
  upload.array("files", 10), // ط§ط³طھظ‚ط¨ط§ظ„ ط­طھظ‰ 10 ظ…ظ„ظپط§طھ
  sendManual
);

// ===== ط³ط¬ظ„ط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ (Sent Log) =====

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط±ط³ظ„ط© ظ…ظ† ظ‚ط¨ظ„ ط§ظ„ظ…ط¯ط±ط³ط©
router.get(
  "/sent-log", 
  authMiddleware, 
  // checkPermission?.("notifications.sent_log.view"), 
  listSentLog
);

// طھظپط§طµظٹظ„ ط¥ط´ط¹ط§ط± ظ…ط±ط³ظ„ ظ…ط­ط¯ط¯ ظ…ط¹ ط­ط§ظ„ط© ط§ظ„ظ‚ط±ط§ط،ط©
router.get(
  "/sent-log/:id", 
  authMiddleware, 
  // checkPermission?.("notifications.sent_log.view"), 
  sentLogDetails
);

export default router;

=== notificationsInboxRoutes.js ===
// backend/src/routes/notificationsInboxRoutes.js
import { Router } from "express";
import {
  listInbox,
  unreadCount,
  markRead,
  markAllRead,
} from "../controllers/notificationsInboxController.js";

const router = Router();

/**
 * ًں”’ طµظ†ط¯ظˆظ‚ ط§ظ„ظˆط§ط±ط¯ ظ„ظ„ظ…ط³طھط®ط¯ظ…ظٹظ† (Inbox)
 * ظ…ظ„ط§ط­ط¸ط©: authMiddleware ظ…ط¶ط§ظپ ظپظٹ app.js ط¹ظ†ط¯ ط§ظ„ط±ط¨ط·طŒ 
 * ظˆظ‡ظˆ ط§ظ„ظ…ط³ط¤ظˆظ„ ط¹ظ† ط­ظ‚ظ† req.user.school_id ظ„ط¶ظ…ط§ظ† ط¹ط²ظ„ ط§ظ„ط¨ظٹط§ظ†ط§طھ ط¨ظٹظ† ط§ظ„ظ…ط¯ط§ط±ط³.
 */

// 1ï¸ڈâƒ£ ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ (ظ…ط¹ ط§ظ„ظپظ„طھط±ط© ظˆط§ظ„ط¨ط­ط« ظˆط§ظ„ظ…ط±ظپظ‚ط§طھ)
// GET /api/notifications/inbox?filter=unread&q=search_term
router.get("/inbox", listInbox);

// 2ï¸ڈâƒ£ ط¬ظ„ط¨ ط¹ط¯ط¯ ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط،ط© ظپظ‚ط· (ظ„ط¥ط¸ظ‡ط§ط± ط§ظ„طھظ†ط¨ظٹظ‡ط§طھ ظپظٹ ط§ظ„ظˆط§ط¬ظ‡ط©)
// GET /api/notifications/inbox/unread-count
router.get("/inbox/unread-count", unreadCount);

// 3ï¸ڈâƒ£ طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظ…ط­ط¯ط¯ ظƒظ…ظ‚ط±ظˆط، (ط¨ط§ط³طھط®ط¯ط§ظ… ظ…ط¹ط±ظپ ط³ط¬ظ„ ط§ظ„ط§ط³طھظ„ط§ظ…)
// PATCH /api/notifications/inbox/123/read
router.patch("/inbox/:recipientRowId/read", markRead);

// 4ï¸ڈâƒ£ طھط¹ظ„ظٹظ… ط¬ظ…ظٹط¹ ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط³طھط®ط¯ظ… ظپظٹ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ط­ط§ظ„ظٹط© ظƒظ…ظ‚ط±ظˆط،ط©
// PATCH /api/notifications/inbox/read-all
router.patch("/inbox/read-all", markAllRead);

export default router;

=== parentAttendanceRoutes.js ===
// backend/src/routes/parentAttendanceRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { ParentAttendanceController } from "../controllers/parentAttendanceController.js";

const router = express.Router();
router.use(authMiddleware);

// /api/parent/attendance/today?studentId=..&date=YYYY-MM-DD&term=1&yearId=1
router.get("/today", ParentAttendanceController.today);

// /api/parent/attendance/week?studentId=..&end=YYYY-MM-DD&term=1&yearId=1
router.get("/week", ParentAttendanceController.week);
router.get("/range", ParentAttendanceController.range);

export default router;

=== parentFeesPortalRoutes.js ===
import { Router } from "express";
import uploadFeesRequests from "../middleware/uploadFeesRequests.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط§ط³طھط¯ط¹ط§ط، ط§ظ„ظ…ظٹط¯ظ„ظˆظٹط±
import { parentFeesOverview, parentPaymentRequest } from "../controllers/parentFeesPortalController.js";

const router = Router();

// âœ… طھط·ط¨ظٹظ‚ ط§ظ„ط­ظ…ط§ظٹط© (authMiddleware) ط¹ظ„ظ‰ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ„ط¶ظ…ط§ظ† ظˆط¬ظˆط¯ ظ…ط¹ط±ظپ ط§ظ„ظ…ط³طھط®ط¯ظ… ظˆط§ظ„ظ…ط¯ط±ط³ط©
router.get("/parent/fees/overview", authMiddleware, parentFeesOverview);
router.post("/parent/fees/payment-request", authMiddleware, uploadFeesRequests, parentPaymentRequest);

export default router;

=== parentGradesRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { getParentChildAssessmentGrades } from "../controllers/parentGradesController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getParentChildAssessmentGrades);

export default router;

=== parentNotificationsRoutes.js ===
// backend/src/routes/parentNotificationsRoutes.js
import express from "express";
import {
  listChildren,
  childTeachers,
  getUnreadCount,
  listInbox,
  markOneRead,
  markAllRead,
  listOutbox,
  outboxRecipients,
  sendAdmins,
  sendChildren,
  sendTeachers,
} from "../controllers/parentNotificationsController.js";

// âœ… ط§ط³طھظٹط±ط§ط¯ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ط§ظ„ط®ط§طµ ط¨ط§ظ„طھظˆط«ظٹظ‚ ظ„ط¶ظ…ط§ظ† ظˆط¬ظˆط¯ req.user.school_id
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

/**
 * ًں”’ ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ
 * طھط·ط¨ظٹظ‚ authMiddleware ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„طھط§ظ„ظٹط© ظ„ط¶ظ…ط§ظ† ط¹ط²ظ„ ط§ظ„ط¨ظٹط§ظ†ط§طھ
 * ط¨ظ†ط§ط،ظ‹ ط¹ظ„ظ‰ ط±ظ‚ظ… ط§ظ„ظ…ط¯ط±ط³ط© (school_id) ط§ظ„ظ…ظˆط¬ظˆط¯ ظپظٹ ط§ظ„طھظˆظƒظ†.
 */
router.use(authMiddleware);

// ==========================================
// ًں”چ ظ…ط³ط§ط¹ط¯ط§طھ ط§ظ„ظˆط§ط¬ظ‡ط© (Lookups)
// ==========================================

// ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ط§ظ„ط£ط¨ظ†ط§ط، ط§ظ„ظ…ط±طھط¨ط·ظٹظ† ط¨ظˆظ„ظٹ ط§ظ„ط£ظ…ط± (ظپظٹ ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط© ظپظ‚ط·)
router.get("/children", listChildren);

// ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ظ…ط¹ظ„ظ…ظٹظ† ط§ط¨ظ† ظ…ط­ط¯ط¯ (ظ„ظ„ط³ظ†ط© ط§ظ„ط¯ط±ط§ط³ظٹط© ط§ظ„ط­ط§ظ„ظٹط© ظپظٹ ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط©)
router.get("/children/:studentId/teachers", childTeachers);

// ==========================================
// ًں“¥ طµظ†ط¯ظˆظ‚ ط§ظ„ظˆط§ط±ط¯ (Inbox)
// ==========================================

// ط¹ط¯ط¯ ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط،ط© ظ„ظˆظ„ظٹ ط§ظ„ط£ظ…ط±
router.get("/unread-count", getUnreadCount);

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط³طھظ„ظ…ط© (ظ…ط¹ ط¯ط¹ظ… ط§ظ„ظپظ„طھط±ط© ظˆط§ظ„ط¨ط­ط«)
router.get("/inbox", listInbox);

// طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظˆط§ط­ط¯ ظƒظ…ظ‚ط±ظˆط،
router.patch("/inbox/:id/read", markOneRead);

// طھط¹ظ„ظٹظ… ط¬ظ…ظٹط¹ ط¥ط´ط¹ط§ط±ط§طھ ظ…ط¯ط±ط³ط© ظˆظ„ظٹ ط§ظ„ط£ظ…ط± ظƒظ…ظ‚ط±ظˆط،ط©
router.patch("/inbox/read-all", markAllRead);

// ==========================================
// ًں“¤ طµظ†ط¯ظˆظ‚ ط§ظ„طµط§ط¯ط± (Outbox)
// ==========================================

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„طھظٹ ط£ط±ط³ظ„ظ‡ط§ ظˆظ„ظٹ ط§ظ„ط£ظ…ط±
router.get("/outbox", listOutbox);

// طھظپط§طµظٹظ„ ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† ظ„ط¥ط´ط¹ط§ط± ظ…ط±ط³ظ„ (ظ„ظ…ط¹ط±ظپط© ظ…ظ† ظ‚ط±ط£ ط§ظ„ط±ط³ط§ظ„ط©)
router.get("/outbox/:id/recipients", outboxRecipients);

// ==========================================
// âœ‰ï¸ڈ ط¹ظ…ظ„ظٹط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ (Send)
// ==========================================

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ط¥ط¯ط§ط±ط© ط§ظ„ظ…ط¯ط±ط³ط©
router.post("/send/admins", sendAdmins);

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ط§ظ„ط£ط¨ظ†ط§ط، (ظ„ط­ط³ط§ط¨ط§طھ ط§ظ„ط·ظ„ط§ط¨)
router.post("/send/children", sendChildren);

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ظ…ط¹ظ„ظ…ظٹ ط§ظ„ط£ط¨ظ†ط§ط،
router.post("/send/teachers", sendTeachers);

export default router;

=== parentPermissionsRoutes.js ===
// backend/src/routes/parentPermissionsRoutes.js
import { Router } from "express";
import {
  createPermission,
  getPermissionForDay,
} from "../controllers/parentPermissionsController.js";

const router = Router();

// /api/parent/permissions
router.get("/permissions", getPermissionForDay);
router.post("/permissions", createPermission);

export default router;

=== parentPortalRoutes.js ===
// src/routes/parentPortalRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getParentMe,
  getParentMeta,
  getChildTimetable,
  getParentExamsMeta,
  getChildExams,
} from "../controllers/parentPortalController.js";

const router = express.Router();

router.get("/me", authMiddleware, getParentMe);
router.get("/meta", authMiddleware, getParentMeta);
router.get("/timetable", authMiddleware, getChildTimetable);

// âœ… Exams for parent
router.get("/exams/meta", authMiddleware, getParentExamsMeta);
router.get("/exams", authMiddleware, getChildExams);

export default router;

=== parentRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { ParentsController } from "../controllers/parentsController.js";
import { getChildActivities } from "../controllers/parentLearningController.js"; 

const router = express.Router();

// ًں”’ ط­ظ…ط§ظٹط© ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ (ظٹط¬ط¨ ط£ظ† ظٹظƒظˆظ† ط§ظ„ظ…ط³طھط®ط¯ظ… ظ…ط³ط¬ظ„ط§ظ‹)
router.use(authMiddleware);

// ==========================================
// âœ… ظ…ط³ط§ط±ط§طھ ط§ظ„ط¥ط¯ط§ط±ط© (ظ„ظ„ط¨ط­ط« ط¹ظ† ط£ظˆظ„ظٹط§ط، ط§ظ„ط£ظ…ظˆط±)
// ==========================================
router.get("/search", ParentsController.search);
router.get("/", ParentsController.list);
router.get("/:id", ParentsController.getById);

// ==========================================
// âœ… ظ…ط³ط§ط±ط§طھ ط¨ظˆط§ط¨ط© ظˆظ„ظٹ ط§ظ„ط£ظ…ط± (Parent Portal)
// ==========================================
// ط§ظ„ظ…ط³ط§ط± ط§ظ„ط®ط§طµ ط¨ظ…ظ‡ط§ظ… ظˆط£ظ†ط´ط·ط© ط§ظ„ط§ط¨ظ†
router.get("/children/:childId/activities", getChildActivities);

export default router;

=== parentsRoutes.js ===
// src/routes/parentsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import ParentsController from "../controllers/parentsController.js";

const router = express.Router();

// âœ… endpoints ظˆط§ط¶ط­ط©
router.get("/search", authMiddleware, ParentsController.search);
router.get("/", authMiddleware, ParentsController.list);

// âœ… Route ط°ظƒظٹ: ظ„ظˆ ط±ظ‚ظ… => getById طŒ ظ„ظˆ ظ†طµ => search ط¨ط¯ظ„ ط®ط·ط£ Invalid id
router.get("/:id", authMiddleware, (req, res) => {
  const raw = String(req.params.id || "").trim();

  // ط±ظ‚ظ…طں ط±ظˆط­ getById
  if (/^\d+$/.test(raw)) {
    return ParentsController.getById(req, res);
  }

  // ظ†طµطں ط§ط¹طھط¨ط±ظ‡ ط¨ط­ط« (ط­طھظ‰ ظ„ظˆ ط¬ط§ط، ط¨ط§ظ„ط؛ظ„ط· ظ…ظ† ط£ظٹ ط³ظƒط±ط¨طھ)
  req.query.q = raw; // ظٹط¯ط¹ظ… search() ظ…ط¨ط§ط´ط±ط©
  return ParentsController.search(req, res);
});

export default router;

=== parentTermResultsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { getParentChildTermResults } from "../controllers/parentTermResultsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getParentChildTermResults);

export default router;

=== periodsRoutes.js ===
// src/routes/periodsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { listPeriods, createPeriod, updatePeriod, deletePeriod } from "../controllers/periodsController.js";

const router = express.Router();

router.get("/", authMiddleware, listPeriods);
router.post("/", authMiddleware, createPeriod);
router.put("/:id", authMiddleware, updatePeriod);
router.delete("/:id", authMiddleware, deletePeriod);

export default router;

=== permissionRoleRoutes.js ===
import { Router } from "express";
import { PermissionRoleController } from "../controllers/permissionRoleController.js";
import adminOnly from "../middleware/adminOnly.js";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط£ط¶ظپظ†ط§ ط§ط³طھظٹط±ط§ط¯ ظ…طµط§ط¯ظ‚ط© ط§ظ„ط¯ط®ظˆظ„

const router = Router();

// ط§ط³طھط®ط¯ط§ظ… authMiddleware ظ„ظ„طھط£ظƒط¯ ظ…ظ† ظ‡ظˆظٹط© ط§ظ„ظ…ط¯ط±ط³ط©
router.use(authMiddleware);

// ط¥ظ†ط´ط§ط، ط¹ظ„ط§ظ‚ط©
router.post("/assign", PermissionRoleController.assign);

// ظƒظ„ ط§ظ„ط¹ظ„ط§ظ‚ط§طھ
router.get("/", PermissionRoleController.getAll);

// ط­ط³ط¨ ط§ظ„ط¯ظˆط±
router.get("/:role_id", PermissionRoleController.getByRole);

// ط­ط°ظپ ط¹ظ„ط§ظ‚ط©
router.delete("/:id", PermissionRoleController.delete);

export default router;

=== permissionRoutes.js ===
import { Router } from "express";
import { PermissionController } from "../controllers/permissionController.js";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";

const router = Router();

// ًں”’ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظٹط¬ط¨ ط£ظ† طھظƒظˆظ† ظ…ط­ظ…ظٹط© ط¨طھط³ط¬ظٹظ„ ط§ظ„ط¯ط®ظˆظ„
router.use(authMiddleware);

// ًں‘پï¸ڈ ط¹ط±ط¶ ط§ظ„طµظ„ط§ط­ظٹط§طھ (ظ…ط³ظ…ظˆط­ ظ„ط£ظٹ ط´ط®طµ ظٹظ…ظ„ظƒ طµظ„ط§ط­ظٹط© ط¥ط¯ط§ط±ط© ط§ظ„ط£ط¯ظˆط§ط± ظ„ظٹطھظ…ظƒظ† ظ…ظ† ط±ط¤ظٹط© ط§ظ„ظ‚ط§ط¦ظ…ط©)
router.get("/", PermissionController.getPermissions);
router.get("/:id", PermissionController.getPermission);

// âڑ ï¸ڈ ط¥ظ†ط´ط§ط،طŒ طھط¹ط¯ظٹظ„طŒ ظˆط­ط°ظپ طµظ„ط§ط­ظٹط© (ط¹ظ…ظ„ظٹط§طھ ط­ط³ط§ط³ط© ظˆظ…ط­ظƒظˆظ…ط© ط¨طµظ„ط§ط­ظٹط© ط¥ط¯ط§ط±ط© ط§ظ„طµظ„ط§ط­ظٹط§طھ)
router.post("/", checkPermission("rbac.manage_permissions"), PermissionController.createPermission);
router.put("/:id", checkPermission("rbac.manage_permissions"), PermissionController.updatePermission);
router.delete("/:id", checkPermission("rbac.manage_permissions"), PermissionController.deletePermission);

export default router;

=== platformAuthRoutes.js ===
import express from "express";
import {
  loginPlatformAdmin,
  getPlatformMe,
} from "../controllers/platformAuthController.js";
import platformAuthMiddleware from "../middleware/platformAuthMiddleware.js";
import { rateLimiter } from "../middleware/rateLimiter.js";

const router = express.Router();

const platformLoginLimiter = rateLimiter({
  windowMs: 15 * 60 * 1000, // 15 ط¯ظ‚ظٹظ‚ط©
  max: 10, // ط§ظ„ط­ط¯ ط§ظ„ط£ظ‚طµظ‰ 10 ظ…ط­ط§ظˆظ„ط§طھ
  message: "ظ„ظ‚ط¯ ظ‚ظ…طھ ط¨ظ…ط­ط§ظˆظ„ط§طھ طھط³ط¬ظٹظ„ ط¯ط®ظˆظ„ ظƒط«ظٹط±ط© ط¬ط¯ط§ظ‹. ظٹط±ط¬ظ‰ ط§ظ„ظ…ط­ط§ظˆظ„ط© ط¨ط¹ط¯ 15 ط¯ظ‚ظٹظ‚ط©."
});

router.post("/login", platformLoginLimiter, loginPlatformAdmin);
router.get("/me", platformAuthMiddleware, getPlatformMe);

router.post("/logout", (req, res) => {
  res.clearCookie("token");
  return res.json({ success: true, message: "طھظ… طھط³ط¬ظٹظ„ ط§ظ„ط®ط±ظˆط¬ ط¨ظ†ط¬ط§ط­" });
});

export default router;

=== platformSchoolsRoutes.js ===
import express from "express";
import {
  getPlatformDashboard,
  listSchools,
  getSchoolDetails,
  activateTrial,
  activateSubscription,
  suspendSchool,
  impersonateSchoolAdmin,
  reactivateSchool,
  getSchoolFullDetails,
listPlatformActivityLogs,
getSubscriptionAlerts,
} from "../controllers/platformSchoolsController.js";

const router = express.Router();
router.get("/dashboard", getPlatformDashboard);
router.get("/activity-logs", listPlatformActivityLogs);
router.get("/subscription-alerts", getSubscriptionAlerts);

router.get("/schools", listSchools);
router.get("/schools/:id/full-details", getSchoolFullDetails);
router.get("/schools/:id", getSchoolDetails);

router.post("/schools/:id/trial", activateTrial);
router.post("/schools/:id/subscription", activateSubscription);
router.post("/schools/:id/suspend", suspendSchool);
router.post("/schools/:id/reactivate", reactivateSchool);
router.post("/schools/:id/impersonate", impersonateSchoolAdmin);

export default router;

=== profileRoutes.js ===
// src/routes/profileRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getMyProfile,
  changePassword,
  changeEmail,
} from "../controllers/profileController.js";

const router = Router();

router.use(authMiddleware);

// GET /api/profile/me
router.get("/me", getMyProfile);

// PUT /api/profile/password
router.put("/password", changePassword);

// PUT /api/profile/email
router.put("/email", changeEmail);

export default router;

=== public.routes.js ===
import { Router } from 'express';
import multer from 'multer';
import path from 'path';
import { registerSchool } from '../controllers/public.controller.js';

const router = Router();

// ط¥ط¹ط¯ط§ط¯ ظ…ظƒط§ظ† ظˆط§ط³ظ… ط­ظپط¸ ط§ظ„ط´ط¹ط§ط± (ط§ظ„طµظˆط±)
const storage = multer.diskStorage({
  destination: function (req, file, cb) {
    // âڑ ï¸ڈ طھط£ظƒط¯ ط£ظ† ظ„ط¯ظٹظƒ ظ…ط¬ظ„ط¯ ط¨ط§ط³ظ… "uploads" ظپظٹ ط§ظ„ظ…ط³ط§ط± ط§ظ„ط±ط¦ظٹط³ظٹ ظ„ظ…ط´ط±ظˆط¹ظƒ (ط¨ط¬ط§ظ†ط¨ src)
    cb(null, 'uploads/'); 
  },
  filename: function (req, file, cb) {
    // ط¥ط¹ط·ط§ط، ط§ظ„طµظˆط±ط© ط§ط³ظ…ط§ظ‹ ظپط±ظٹط¯ط§ظ‹: logo-16987654321-random.jpg
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
    cb(null, 'logo-' + uniqueSuffix + path.extname(file.originalname));
  }
});

const upload = multer({ storage: storage });

// âœ… ط¥ط¶ط§ظپط© upload.single('logo') ظ„ظƒظٹ ظٹطھظ…ظƒظ† ط§ظ„ط³ظٹط±ظپط± ظ…ظ† ظ‚ط±ط§ط،ط© ط§ظ„طµظˆط±ط©
// ظƒظ„ظ…ط© 'logo' ظٹط¬ط¨ ط£ظ† طھط·ط§ط¨ظ‚ ظ†ظپط³ ط§ظ„ط§ط³ظ… ط§ظ„ط°ظٹ ط£ط±ط³ظ„ظ†ط§ظ‡ ظ…ظ† ط§ظ„ظ€ FormData ظپظٹ ط§ظ„ظ…طھطµظپط­
router.post('/register-school', upload.single('logo'), registerSchool);

export default router;

=== roleRoutes.js ===
// src/routes/roleRoutes.js
import express from "express";
import {
  createRole,
  getRoles,
  getRole,
  updateRole,
  deleteRole,
  getRolePermissions,
  updateRolePermissions,
  grantAllPermissions, // âœ… ط§ظ„ط¥ط¶ط§ظپط© ط§ظ„ط¬ط¯ظٹط¯ط©
} from "../controllers/roleController.js";

import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";

const router = express.Router();

/* ======================
   ًں”’ ط¥ط¯ط§ط±ط© ط§ظ„ط£ط¯ظˆط§ط±
====================== */

// ط¥ظ†ط´ط§ط، ط¯ظˆط± ط¬ط¯ظٹط¯
router.post(
  "/",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  createRole
);

// ط¬ظ„ط¨ ظƒظ„ ط§ظ„ط£ط¯ظˆط§ط±
router.get(
  "/",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  getRoles
);

// ط¬ظ„ط¨ ط¯ظˆط± ظˆط§ط­ط¯
router.get(
  "/:id",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  getRole
);

// طھط­ط¯ظٹط« ط¯ظˆط±
router.put(
  "/:id",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  updateRole
);

// ط­ط°ظپ ط¯ظˆط±
router.delete(
  "/:id",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  deleteRole
);

/* ======================
   ًں”‘ طµظ„ط§ط­ظٹط§طھ ط§ظ„ط¯ظˆط±
====================== */

// ط¬ظ„ط¨ طµظ„ط§ط­ظٹط§طھ ط¯ظˆط± ظ…ط¹ظٹظ†
router.get(
  "/:id/permissions",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  getRolePermissions
);

// طھط­ط¯ظٹط« طµظ„ط§ط­ظٹط§طھ ط¯ظˆط± (checkboxes)
router.post(
  "/:id/permissions",
  authMiddleware,
  checkPermission("rbac.manage_roles"),
  updateRolePermissions
);

/* ======================
   âœ… ظ…ظٹط²ط© ط§ط­طھط±ط§ظپظٹط©
   ًں”گ ظ…ظ†ط­ ظƒظ„ ط§ظ„طµظ„ط§ط­ظٹط§طھ
====================== */

// router.post(
//   "/:id/grant-all-permissions",
//   authMiddleware,
//   checkPermission("rbac.manage_roles"),
//   grantAllPermissions
// );

export default router;

=== schoolSettingsRoutes.js ===
import { Router } from "express";
import multer from "multer";
import path from "path";
import authMiddleware from "../middleware/authMiddleware.js";

import {
  meta,
  profileUpdate,
  academicGet, // âœ… ط£ط¶ظپ ظ‡ط°ط§ ط§ظ„ط§ط³طھظٹط±ط§ط¯
  academicUpdate, // âœ… ط£ط¶ظپ ظ‡ط°ط§ ط§ظ„ط§ط³طھظٹط±ط§ط¯
  financeUpdate,
  portalsGet,
  portalsUpdate,
  yearsCreate,
  yearsUpdate,
  yearsToggle,
  stagesCreate,
  stagesUpdate,
  stagesToggle,
  gradesCreate,
  gradesUpdate,
  gradesToggle,
  sectionsCreate,
  sectionsUpdate,
  sectionsToggle,
  subjectsCreate,
  subjectsUpdate,
  financeGet,
  subjectsToggle,
  periodsCreate,
  periodsUpdate,
  periodsToggle,
  curriculumGet,
  curriculumSet,
  teacherSubjectGet,
  teacherSubjectSet,
} from "../controllers/schoolSettingsController.js";

const router = Router();

// --- ط¥ط¹ط¯ط§ط¯ multer ظ„ط±ظپط¹ ط§ظ„ط´ط¹ط§ط±ط§طھ ---
const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, "uploads/");
  },
  filename: (req, file, cb) => {
    const uniqueSuffix = Date.now() + "-" + Math.round(Math.random() * 1e9);
    cb(null, "school_" + uniqueSuffix + path.extname(file.originalname));
  },
});
const upload = multer({ storage });

router.use(authMiddleware);

router.get("/meta", meta);
router.get('/finance', financeGet);
// âœ… ط§ظ„ظ…ظ„ظپ ط§ظ„ط´ط®طµظٹ ظ„ظ„ظ…ط¯ط±ط³ط©
router.post("/profile", upload.single("logo"), profileUpdate);

// âœ… ط§ظ„ط¥ط¹ط¯ط§ط¯ط§طھ ط§ظ„ط£ظƒط§ط¯ظٹظ…ظٹط© (ط§ظ„ط¬ط¯ظٹط¯ط©)
router.get("/academic", academicGet);
router.post("/academic", academicUpdate);

router.post("/finance", financeUpdate);

router.get("/portals", portalsGet);
router.post("/portals", portalsUpdate);
// ط§ظ„ط³ظ†ظˆط§طھ ط§ظ„ط¯ط±ط§ط³ظٹط©
router.post("/years", yearsCreate);
router.patch("/years/:id", yearsUpdate);
router.patch("/years/:id/toggle", yearsToggle);

// ط§ظ„ظ…ط±ط§ط­ظ„
router.post("/stages", stagesCreate);
router.patch("/stages/:id", stagesUpdate);
router.patch("/stages/:id/toggle", stagesToggle);

// ط§ظ„طµظپظˆظپ
router.post("/grades", gradesCreate);
router.patch("/grades/:id", gradesUpdate);
router.patch("/grades/:id/toggle", gradesToggle);

// ط§ظ„ط´ط¹ط¨
router.post("/sections", sectionsCreate);
router.patch("/sections/:id", sectionsUpdate);
router.patch("/sections/:id/toggle", sectionsToggle);

// ط§ظ„ظ…ظˆط§ط¯
router.post("/subjects", subjectsCreate);
router.patch("/subjects/:id", subjectsUpdate);
router.patch("/subjects/:id/toggle", subjectsToggle);

// ط§ظ„ظپطھط±ط§طھ (ط§ظ„ط­طµطµ)
router.post("/periods", periodsCreate);
router.patch("/periods/:id", periodsUpdate);
router.patch("/periods/:id/toggle", periodsToggle);

// ظ…ظ†ظ‡ط¬ ط§ظ„طµظپظˆظپ
router.get("/curriculum", curriculumGet);
router.post("/curriculum", curriculumSet);

// طھط£ظ‡ظٹظ„ ط§ظ„ظ…ط¯ط±ط³ظٹظ†
router.get("/teacher-subjects", teacherSubjectGet);
router.post("/teacher-subjects", teacherSubjectSet);

export default router;

=== sectionRoutes.js ===
import { Router } from "express";
import  authMiddleware  from "../middleware/authMiddleware.js";
import  checkPermission  from "../middleware/checkPermission.js";
import { getSections } from "../controllers/sectionController.js";

const router = Router();

router.get(
  "/",
  authMiddleware,
  checkPermission("view_academic_years"),
  getSections
);

export default router;

=== staffReportsRoutes.js ===
// backend/src/routes/staffReportsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StaffReportsController } from "../controllers/staffReportsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/summary", StaffReportsController.summary);
router.get("/", StaffReportsController.list);
router.get("/:employeeId/profile", StaffReportsController.profile);

export default router;

=== stageRoutes.js ===
import { Router } from "express";
import  authMiddleware  from "../middleware/authMiddleware.js";
import  checkPermission  from "../middleware/checkPermission.js";
import { getStages } from "../controllers/stageController.js";

const router = Router();

router.get(
  "/",
  authMiddleware,
  checkPermission("view_academic_years"), // ظ…ط¤ظ‚طھط§ظ‹ ظ†ط³طھط®ط¯ظ… طµظ„ط§ط­ظٹط© ط¹ط§ظ…ط©
  getStages
);

export default router;

=== studentAttendanceRoutes.js ===
// backend/src/routes/studentAttendanceRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StudentAttendanceController } from "../controllers/studentAttendanceController.js";

const router = express.Router();
router.use(authMiddleware);

// ظ…ظ„ط®طµ ط§ظ„ظٹظˆظ… + ط±ط³ط§ظ„ط© ظ‚ظˆظٹط© ظ„ظ„ظˆط§ط¬ظ‡ط©
router.get("/today", StudentAttendanceController.today);

// ط£ط³ط¨ظˆط¹ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ط­ط§ظ„ظٹ (ط³ط¨طھ -> ط¬ظ…ط¹ط©)
router.get("/week", StudentAttendanceController.week);

// ظ†ط·ط§ظ‚ ط¹ط§ظ… (ظٹظڈط³طھط®ط¯ظ… ظ„ظ„ط´ظ‡ط±/ط§ظ„ظپطµظ„)
router.get("/range", StudentAttendanceController.range);

// ظ†ط³ط¨ط© ط­ط¶ظˆط± ط§ظ„ط¹ط§ظ… + ط¥ط­طµط§ط¦ظٹط§طھ
router.get("/stats", StudentAttendanceController.stats);

export default router;

=== studentBarcodeRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StudentBarcodeController } from "../controllers/studentBarcodeController.js";

const router = express.Router();
router.use(authMiddleware);

router.get("/", StudentBarcodeController.getMyBarcode);

export default router;

=== studentFeesPortalRoutes.js ===
// src/routes/studentFeesPortalRoutes.js
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { studentFeesOverview } from "../controllers/studentFeesPortalController.js";

const router = Router();

// âœ… ط§ظ„ظ…ط³ط§ط± ظ…ط­ظ…ظٹ ط¨ظ€ authMiddleware ظ„ط¶ظ…ط§ظ† طھظˆظپط± ط¨ظٹط§ظ†ط§طھ ط§ظ„ط·ط§ظ„ط¨ ظˆظ…ط¯ط±ط³طھظ‡
router.get("/overview", authMiddleware, studentFeesOverview);

export default router;

=== studentLearningRoutes.js ===
import express from "express";
import fs from "fs";
import path from "path";
import multer from "multer";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  listStudentActivities,
  getStudentActivityDetail,
  submitStudentActivity,
  listStudentGrades,
} from "../controllers/studentLearningController.js";

const router = express.Router();

const uploadDir = path.join(process.cwd(), "uploads", "submissions");
fs.mkdirSync(uploadDir, { recursive: true });

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => cb(null, uploadDir),
  filename: (_req, file, cb) => {
    const ext = path.extname(file.originalname || "");
    const safe = `sub_${Date.now()}_${Math.round(Math.random() * 1e9)}${ext}`;
    cb(null, safe);
  },
});

const upload = multer({
  storage,
  limits: {
    fileSize: 10 * 1024 * 1024, // 10MB
  },
});

router.use(authMiddleware);

router.get("/activities", listStudentActivities);
router.get("/activities/:id", getStudentActivityDetail);
router.post("/activities/:id/submit", upload.single("file"), submitStudentActivity);

router.get("/grades", listStudentGrades);

export default router;

=== studentNotificationsRoutes.js ===
// backend/src/routes/studentNotificationsRoutes.js
import express from "express";
import {
  unreadCount,
  listInbox,
  markOneRead,
  markAllRead,
  listOutbox,
  outboxRecipients,
  listMyTeachers,
  sendAdmins,
  sendTeachers,
} from "../controllers/studentNotificationsController.js";

// âœ… ط§ط³طھظٹط±ط§ط¯ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ظ„ط¶ظ…ط§ظ† ط§ط³طھط®ط±ط§ط¬ req.user.school_id
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

/**
 * ًں”’ ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ (Student Portal)
 * طھظ… طھط·ط¨ظٹظ‚ authMiddleware ظ„ط¶ظ…ط§ظ† ط£ظ† ط§ظ„ط·ط§ظ„ط¨ ظ„ط§ ظٹطµظ„ ط¥ظ„ط§ ظ„ط¨ظٹط§ظ†ط§طھ ظ…ط¯ط±ط³طھظ‡ ظپظ‚ط·.
 */
router.use(authMiddleware);

// ==========================================
// ًں“¥ طµظ†ط¯ظˆظ‚ ط§ظ„ظˆط§ط±ط¯ (Inbox)
// ==========================================

// ط¹ط¯ط¯ ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط،ط© ظ„ظ„ط·ط§ظ„ط¨ (ظ„ط¥ط¸ظ‡ط§ط± ط§ظ„طھظ†ط¨ظٹظ‡ ط§ظ„ط£ط­ظ…ط±)
router.get("/unread-count", unreadCount);

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط³طھظ„ظ…ط© (طھط­ط¯ظٹط«ط§طھ ط§ظ„ط؛ظٹط§ط¨طŒ ط§ظ„ظ†طھط§ط¦ط¬طŒ ط¥ظ„ط®)
router.get("/inbox", listInbox);

// طھط¹ظ„ظٹظ… ط¬ظ…ظٹط¹ ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„ظ…ط¯ط±ط³ط© ط§ظ„ط­ط§ظ„ظٹط© ظƒظ…ظ‚ط±ظˆط،ط©
router.patch("/inbox/read-all", markAllRead);

// طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظˆط§ط­ط¯ ظ…ط­ط¯ط¯ ظƒظ…ظ‚ط±ظˆط،
router.patch("/inbox/:id/read", markOneRead);

// ==========================================
// ًں“¤ طµظ†ط¯ظˆظ‚ ط§ظ„طµط§ط¯ط± (Outbox)
// ==========================================

// ظ‚ط§ط¦ظ…ط© ط§ظ„ط¥ط´ط¹ط§ط±ط§طھ ط§ظ„طھظٹ ط£ط±ط³ظ„ظ‡ط§ ط§ظ„ط·ط§ظ„ط¨ (ظ„ظ„ظ…ط¹ظ„ظ…ظٹظ† ط£ظˆ ط§ظ„ط¥ط¯ط§ط±ط©)
router.get("/outbox", listOutbox);

// طھظپط§طµظٹظ„ ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† ظ„ط¥ط´ط¹ط§ط± ظ…ط±ط³ظ„ (ظ„ظ…ط¹ط±ظپط© ظ‡ظ„ ظ‚ط±ط£ ط§ظ„ظ…ط¹ظ„ظ… ط§ظ„ط±ط³ط§ظ„ط©طں)
router.get("/outbox/:id/recipients", outboxRecipients);

// ==========================================
// ًں”چ ظ…ط³ط§ط¹ط¯ط§طھ ط§ظ„ط¨ط­ط« (Lookups)
// ==========================================

// ط¬ظ„ط¨ ظ‚ط§ط¦ظ…ط© ط§ظ„ظ…ط¹ظ„ظ…ظٹظ† ط§ظ„ط°ظٹظ† ظٹط¯ط±ط³ظˆظ† ظ‡ط°ط§ ط§ظ„ط·ط§ظ„ط¨ ط­ط§ظ„ظٹط§ظ‹ ظپظٹ ظ‡ط°ظ‡ ط§ظ„ظ…ط¯ط±ط³ط©
router.get("/teachers", listMyTeachers);

// ==========================================
// âœ‰ï¸ڈ ط¹ظ…ظ„ظٹط§طھ ط§ظ„ط¥ط±ط³ط§ظ„ (Messaging)
// ==========================================

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط©/طھط¸ظ„ظ… ط¥ظ„ظ‰ ط¥ط¯ط§ط±ط© ط§ظ„ظ…ط¯ط±ط³ط©
router.post("/send/admins", sendAdmins);

// ط¥ط±ط³ط§ظ„ ط±ط³ط§ظ„ط© ط¥ظ„ظ‰ ظ…ط¹ظ„ظ… ظ…ط­ط¯ط¯ ط£ظˆ ط¬ظ…ظٹط¹ ظ…ط¹ظ„ظ…ظٹ ط§ظ„ط·ط§ظ„ط¨
router.post("/send/teachers", sendTeachers);

export default router;

=== studentPortalRoutes.js ===
// src/routes/studentPortalRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getMeta,
  getTimetable,
  getExamMeta,
  getExams,
  getStudentHeroStats
} from "../controllers/studentPortalController.js";

const router = express.Router();

// meta: days + periods + years
router.get("/meta", authMiddleware, getMeta);

// timetable published for student class
router.get("/timetable", authMiddleware, getTimetable);

// stats for top bar
router.get("/hero-stats", authMiddleware, getStudentHeroStats);

// exam metadata and list
router.get("/exams/meta", authMiddleware, getExamMeta);
router.get("/exams", authMiddleware, getExams);

export default router;

=== studentReportsRoutes.js ===
// backend/src/routes/studentReportsRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { StudentReportsController } from "../controllers/studentReportsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/summary", StudentReportsController.summary);
router.get("/", StudentReportsController.list);
router.get("/:studentId/profile", StudentReportsController.profile);

export default router;

=== studentRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import checkPermission from "../middleware/checkPermission.js";
import { searchStudents } from "../controllers/metaController.js";
import {
  registerStudent,
  listStudents,
  getStudentById,
  deleteStudentById,
  updateStudentById,
  getNextStudentCode, // âœ… 1. ط£ط¶ظپظ†ط§ ط§ط³طھظٹط±ط§ط¯ ط§ظ„ط¯ط§ظ„ط© ظ‡ظ†ط§
} from "../controllers/studentController.js";

const router = Router();

// POST /api/students/register
router.post(
  "/register",
  authMiddleware,
  checkPermission("admission.create_student"),
  registerStudent
);

// GET /api/students
router.get(
  "/",
  authMiddleware,
  checkPermission("admission.view_students"),
  listStudents
);

// âœ… GET /api/students/search
router.get(
  "/search",
  authMiddleware,
  checkPermission("admission.view_students"),
  searchStudents
);

// âœ… 2. ظ†ظ‚ظ„ظ†ط§ /next-code ظ„ظٹظƒظˆظ† ظ‚ط¨ظ„ /:id (طھط¬ظ†ط¨ط§ظ‹ ظ„ظپط® ط§ظ„ظ€ params)
router.get(
  '/next-code', 
  authMiddleware, 
  getNextStudentCode
);

// GET /api/students/:id
router.get(
  "/:id",
  authMiddleware,
  checkPermission("admission.view_students"),
  getStudentById
);

// âœ… PUT/PATCH /api/students/:id (طھط¹ط¯ظٹظ„)
router.put(
  "/:id",
  authMiddleware,
  checkPermission("admission.update_student"),
  updateStudentById
);

router.patch(
  "/:id",
  authMiddleware,
  checkPermission("admission.update_student"),
  updateStudentById
);

// DELETE /api/students/:id
router.delete(
  "/:id",
  authMiddleware,
  checkPermission("admission.delete_student"),
  deleteStudentById
);

export default router;

=== studentTermResultsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { getStudentTermResults } from "../controllers/studentTermResultsController.js";

const router = express.Router();

router.use(authMiddleware);

router.get("/", getStudentTermResults);

export default router;

=== studentTransferRequestsRoutes.js ===
// src/routes/studentTransferRequestsRoutes.js
import { Router } from "express";
import { StudentTransferRequestsController } from "../controllers/studentTransferRequestsController.js";

const router = Router();

router.get("/", StudentTransferRequestsController.list);

router.post("/", StudentTransferRequestsController.create);

router.post("/:id/approve", StudentTransferRequestsController.approve);

router.post("/:id/reject", StudentTransferRequestsController.reject);

export default router;

=== teacherAssessmentsRoutes.js ===
import express from "express";
import fs from "fs";
import path from "path";
import multer from "multer";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  listAssessments,
  createAssessment,
  publishAssessment,
  closeAssessment,
  getOfficialAssessmentContext,
} from "../controllers/teacherAssessmentsController.js";

const router = express.Router();

// =========================================
// ط¥ط¹ط¯ط§ط¯ط§طھ ط±ظپط¹ ط§ظ„ظ…ط±ظپظ‚ط§طھ ط§ظ„ط®ط§طµط© ط¨ط§ظ„ظ…ط¹ظ„ظ… (Multer)
// =========================================
const uploadDir = path.join(process.cwd(), "uploads", "assessments");
fs.mkdirSync(uploadDir, { recursive: true });

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => cb(null, uploadDir),
  filename: (_req, file, cb) => {
    const ext = path.extname(file.originalname || "");
    const safe = `asm_${Date.now()}_${Math.round(Math.random() * 1e9)}${ext}`;
    cb(null, safe);
  },
});

const upload = multer({
  storage,
  limits: {
    fileSize: 10 * 1024 * 1024, // ط§ظ„ط­ط¯ ط§ظ„ط£ظ‚طµظ‰ ظ„ظ„ظ…ظ„ظپ ط§ظ„ظˆط§ط­ط¯: 10 ظ…ظٹط¬ط§ط¨ط§ظٹطھ
  },
});
// =========================================

router.use(authMiddleware);

router.get("/official-context", getOfficialAssessmentContext);
router.get("/", listAssessments);

// âœ… طھظ… ط§ظ„ط¥طµظ„ط§ط­: ط¥ط¶ط§ظپط© upload.array ظ„ظپظƒ طھط´ظپظٹط± ط§ظ„ظ€ FormData ظˆط§ط³طھظ‚ط¨ط§ظ„ ط­طھظ‰ 5 ظ…ظ„ظپط§طھ
router.post("/", upload.array("files", 5), createAssessment);

router.post("/:id/publish", publishAssessment);
router.post("/:id/close", closeAssessment);

export default router;

=== teacherAttendanceRoutes.js ===
// src/routes/teacherAttendanceRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherAttendanceController } from "../controllers/teacherAttendanceController.js";

const router = express.Router();

router.use(authMiddleware);

/* =========================
   META + SCOPES + REASONS
========================= */
router.get("/meta", TeacherAttendanceController.meta);
router.get("/scopes", TeacherAttendanceController.scopes);
router.get("/reasons", TeacherAttendanceController.reasons);

/* =========================
   SESSIONS (Today Slots + Frontend Aliases)
   ظ…ظ‡ظ…:
   ظ‡ط°ظ‡ ط§ظ„ظ…ط³ط§ط±ط§طھ ظٹط¬ط¨ ط£ظ† طھظƒظˆظ† ظ‚ط¨ظ„ /sessions/:id
========================= */

// ط§ظ„ظˆط§ط¬ظ‡ط© طھط·ظ„ط¨:
// GET /api/teacher/attendance/sessions?academicYearId=6&term=1
router.get("/sessions", TeacherAttendanceController.sessionSlots);

// ط§ظ„ط±ط§ط¨ط· ط§ظ„ظ‚ط¯ظٹظ…/ط§ظ„ط£طµظ„ظٹ
router.get("/sessions/slots", TeacherAttendanceController.sessionSlots);

// ط§ظ„ظˆط§ط¬ظ‡ط© طھط·ظ„ط¨:
// GET /api/teacher/attendance/sessions/history?academicYearId=6&term=1
router.get("/sessions/history", TeacherAttendanceController.historySearch);

// ط§ظ„ظˆط§ط¬ظ‡ط© طھط·ظ„ط¨:
// GET /api/teacher/attendance/sessions/log?academicYearId=6&term=1
router.get("/sessions/log", TeacherAttendanceController.historySearch);

/* =========================
   PERMITS (Parent requests / excuses)
   - ظ…ط³ط§ط±ط§طھ ظˆط§ط¶ط­ط© + Aliases ظ„ظ„طھظˆط§ظپظ‚
========================= */
router.get("/permits", TeacherAttendanceController.permitsList);
router.get("/permits/approved", TeacherAttendanceController.permitsApproved);
router.get("/permits/excuses", TeacherAttendanceController.permitsExcuses);
router.get("/permits/map", TeacherAttendanceController.permitsMap);

// Aliases ظ„ظˆ ط§ظ„ظپط±ظˆظ†طھ ظٹط³طھط®ط¯ظ…ظ‡ط§
router.get("/approved", TeacherAttendanceController.permitsApproved);
router.get("/excuses", TeacherAttendanceController.permitsExcuses);

/* =========================
   HISTORY + REPORTS
========================= */

// ط§ظ„ط±ط§ط¨ط· ط§ظ„ط¹ط§ظ… ط§ظ„ظ‚ط¯ظٹظ…:
// GET /api/teacher/attendance/history
router.get("/history", TeacherAttendanceController.historySearch);

router.get("/report/aggregate", TeacherAttendanceController.reportAggregate);

/* =========================
   SESSIONS (CRUD + Lock/Unlock/End)
========================= */

// Create/Open session
router.post("/sessions", TeacherAttendanceController.createSession);

// Alias: ظ„ظˆ ط§ظ„ظپط±ظˆظ†طھ ظٹط±ط³ظ„ /sessions/start
router.post("/sessions/start", TeacherAttendanceController.createSession);

// Get/Update
// ظ…ظ‡ظ… ط¬ط¯ظ‹ط§:
// ظ‡ط°ط§ ظٹط¬ط¨ ط£ظ† ظٹط¨ظ‚ظ‰ ط¨ط¹ط¯ /sessions/history ظˆ /sessions/log
// ط­طھظ‰ ظ„ط§ ظٹط¹طھط¨ط± Express ظƒظ„ظ…ط© history ط£ظˆ log ط£ظ†ظ‡ط§ id
router.get("/sessions/:id", TeacherAttendanceController.getSession);
router.patch("/sessions/:id", TeacherAttendanceController.updateSession);

// Lock/Unlock
router.patch("/sessions/:id/lock", TeacherAttendanceController.lockSession);
router.patch("/sessions/:id/unlock", TeacherAttendanceController.unlockSession);

// End
router.patch("/sessions/:id/end", TeacherAttendanceController.endSession);
router.post("/sessions/:id/end", TeacherAttendanceController.endSession);

// Scan QR to mark present inside this session
router.post("/sessions/:id/scan", TeacherAttendanceController.scanMarkPresentByToken);

/* =========================
   ENTRIES (Students + Save Attendance)
========================= */
router.get("/sessions/:id/entries", TeacherAttendanceController.listEntries);
router.put("/sessions/:id/entries", TeacherAttendanceController.saveEntries);

// Aliases ظ„ظ„طھظˆط§ظپظ‚ ظ…ط¹ ظپط±ظˆظ†طھ ظ‚ط¯ظٹظ…
router.get("/sessions/:id/students", TeacherAttendanceController.listEntries);
router.post("/sessions/:id/attendance", TeacherAttendanceController.saveEntries);

export default router;

=== teacherDashboardRoutes.js ===
import express from "express";
import requireAuth from "../middleware/authMiddleware.js";
import {
  getHero,
  getNextLesson,
} from "../controllers/teacherDashboardController.js";

const router = express.Router();

// ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„ظ‚ط¯ظٹظ…ط©
router.get("/hero", requireAuth, getHero);
router.get("/next-lesson", requireAuth, getNextLesson);

// ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„طھظٹ طھط·ظ„ط¨ظ‡ط§ ط§ظ„ظˆط§ط¬ظ‡ط© ط§ظ„ط­ط§ظ„ظٹط©
router.get("/dashboard/hero", requireAuth, getHero);
router.get("/dashboard/next-lesson", requireAuth, getNextLesson);

export default router;

=== teacherGradesRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  getGradeEntry,
  saveGradeEntry,
  publishGradeEntry,
  listReopenRequests,
  createReopenRequest,
  submitMuhassala,
  getMonthlyWorkControlStatuses,
  getTermWorkControlStatus,
} from "../controllers/teacherGradesController.js";

const router = express.Router();

// ظ…ظ‡ظ… ط¬ط¯ظ‹ط§: ظƒظ„ ظ…ط³ط§ط±ط§طھ ط¯ط±ط¬ط§طھ ط§ظ„ظ…ط¹ظ„ظ… طھط­طھط§ط¬ req.user
router.use(authMiddleware);

router.get("/monthly-work-statuses", getMonthlyWorkControlStatuses);
router.get("/term-work-status", getTermWorkControlStatus);

router.get("/entry", getGradeEntry);
router.post("/entry/save", saveGradeEntry);
router.post("/entry/publish", publishGradeEntry);

router.post("/muhassala/submit", submitMuhassala);

router.get("/reopen-requests", listReopenRequests);
router.post("/reopen-requests", createReopenRequest);

export default router;

=== teacherMeRoutes.js ===
// src/routes/teacherMeRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherMeController } from "../controllers/teacherMeController.js";

const router = express.Router();

// ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ
router.use(authMiddleware);

/*
  ظ„ط£ظ† ظ‡ط°ط§ ط§ظ„ظ…ظ„ظپ ظ…ط±ظƒط¨ ظپظٹ server.js ط¹ظ„ظ‰:
  /api/teacher/me

  ط¥ط°ظ†:
  router.get("/") = /api/teacher/me
*/
router.get("/", TeacherMeController.card);

// ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„ظ…ظˆط¬ظˆط¯ط© ط³ط§ط¨ظ‚ظ‹ط§
router.get("/card", TeacherMeController.card);
router.get("/token", TeacherMeController.token);

export default router;

=== teacherNotificationsRoutes.js ===
// backend/src/routes/teacherNotificationsRoutes.js
import express from "express";
import {
  getUnreadCount,
  listInbox,
  markOneRead,
  markAllRead,
  listOutbox,
  outboxRecipients,
} from "../controllers/teacherNotificationsController.js";

// âœ… ط§ط³طھظٹط±ط§ط¯ ط§ظ„ظ…ظٹط¯ظ„ ظˆظٹط± ط§ظ„ط®ط§طµ ط¨ط§ظ„طھظˆط«ظٹظ‚ (طھط£ظƒط¯ ظ…ظ† ط§ظ„ظ…ط³ط§ط± ط§ظ„طµط­ظٹط­ ظپظٹ ظ…ط´ط±ظˆط¹ظƒ)
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

// طھط·ط¨ظٹظ‚ ط§ظ„ط­ظ…ط§ظٹط© ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ ظ„ط¶ظ…ط§ظ† ط§ط³طھط®ط±ط§ط¬ req.user.school_id
router.use(authMiddleware);

// ط¹ط¯ط§ط¯ ط؛ظٹط± ط§ظ„ظ…ظ‚ط±ظˆط، (ظˆط§ط±ط¯ ط§ظ„ظ…ط¹ظ„ظ…)
router.get("/unread-count", getUnreadCount);

// ط§ظ„ظˆط§ط±ط¯
router.get("/inbox", listInbox);

// طھط¹ظ„ظٹظ… ظƒظ„ ط§ظ„ظˆط§ط±ط¯ ظƒظ…ظ‚ط±ظˆط، (ظٹظڈظپط¶ظ„ ظˆط¶ط¹ظ‡ ظ‚ط¨ظ„ ط§ظ„ظ…ط³ط§ط±ط§طھ ط§ظ„ط¯ظٹظ†ط§ظ…ظٹظƒظٹط© :id)
router.patch("/inbox/read-all", markAllRead);

// طھط¹ظ„ظٹظ… ط¥ط´ط¹ط§ط± ظˆط§ط­ط¯ ظ…ظ‚ط±ظˆط، (ط¨ظˆط§ط³ط·ط© ط§ظ„ظ€ id)
router.patch("/inbox/:id/read", markOneRead);

// ط§ظ„طµط§ط¯ط± (ط§ظ„ط°ظٹ ط£ط±ط³ظ„ظ‡ ط§ظ„ظ…ط¹ظ„ظ…)
router.get("/outbox", listOutbox);

// طھظپط§طµظٹظ„ ط§ظ„ظ…ط³طھظ„ظ…ظٹظ† (ظ…ظ† ظ‚ط±ط£/ظ„ظ… ظٹظ‚ط±ط£)
router.get("/outbox/:id/recipients", outboxRecipients);

export default router;

=== teacherNotificationsSendRoutes.js ===
// backend/src/routes/teacherNotificationsSendRoutes.js
import express from "express";
import {
  getTeacherScopes,
  listScopeStudents,
  listStudentGuardians,
  sendToAdmins,
  sendToStudents,
  sendToGuardians,
} from "../controllers/teacherNotificationsSendController.js";
import authMiddleware from "../middleware/authMiddleware.js";

const router = express.Router();

// ط­ظ…ط§ظٹط© ط§ظ„ظ…ط³ط§ط±ط§طھ ظ„ط¶ظ…ط§ظ† ط§ط³طھط®ط±ط§ط¬ school_id
router.use(authMiddleware);

// ظ†ط·ط§ظ‚ط§طھ ط§ظ„ظ…ط¹ظ„ظ…
router.get("/scopes", getTeacherScopes);

// ط·ظ„ط§ط¨ ط´ط¹ط¨ط© ظ…ط¹ظٹظ†ط© (ظ…ظ† ظ†ط·ط§ظ‚ظ‡ ظˆظ…ط¯ط±ط³طھظ‡ ظپظ‚ط·)
router.get("/students", listScopeStudents);

// ط£ظˆظ„ظٹط§ط، ط£ظ…ظˆط± ط·ط§ظ„ط¨ ظ…ط¹ظٹظ†
router.get("/students/:studentId/guardians", listStudentGuardians);

// ط¥ط±ط³ط§ظ„ ظ„ظ„ط¥ط¯ط§ط±ط©
router.post("/send/admins", sendToAdmins);

// ط¥ط±ط³ط§ظ„ ظ„ظ„ط·ظ„ط§ط¨
router.post("/send/students", sendToStudents);

// ط¥ط±ط³ط§ظ„ ظ„ظ„ط£ظˆظ„ظٹط§ط،
router.post("/send/guardians", sendToGuardians);

export default router;

=== teacherPermitsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherPermitsController } from "../controllers/teacherPermitsController.js";

const router = express.Router();
router.use(authMiddleware);

// âœ… ظ…ظ‡ظ…: ظ„ط§ط²ظ… ظ‡ط°ظ‡ ط§ظ„ط¯ط§ظ„ط© طھظƒظˆظ† ظ…ظˆط¬ظˆط¯ط© ظپظٹ ط§ظ„ظƒظ†طھط±ظˆظ„ط±
router.get("/available-slots", TeacherPermitsController.availableSlots);

router.get("/", TeacherPermitsController.listMyPermits);
router.post("/", TeacherPermitsController.createPermit);
// ظپظٹ ظ…ظ„ظپ teacherRoutes.js ط£ظˆ ظ…ط§ ط´ط§ط¨ظ‡:
router.get("/substitutions/pending", TeacherPermitsController.getPendingSubstitutions);
router.patch("/substitutions/:id/respond", TeacherPermitsController.respondToSubstitution);
export default router;

=== teacherProfileRoutes.js ===
import express from "express";
import { getTeacherJobProfile } from "../controllers/teacherProfileController.js";

const router = express.Router();

// âœ… ظ‡ظ†ط§ "/" ظ„ط£ظ†ظ†ط§ ط³ظ†ط±ظƒط¨ظ‡ ط¹ظ„ظ‰ /api/teacher/profile
router.get("/", getTeacherJobProfile);

export default router;

=== teacherReportsRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js"; // âœ… ط§ط³طھط¯ط¹ط§ط، ط­ط§ط±ط³ ط§ظ„ط¨ظˆط§ط¨ط©
import {
  getTeacherReportsMeta,
  getTeacherReportScopes,
  getTeacherReportContext,
  generateTeacherReport,
} from "../controllers/teacherReportsController.js";

const router = Router();

// âœ… طھط·ط¨ظٹظ‚ ط§ظ„ط­ظ…ط§ظٹط© ط¹ظ„ظ‰ ط¬ظ…ظٹط¹ ظ…ط³ط§ط±ط§طھ ط§ظ„طھظ‚ط§ط±ظٹط± ظ„ط¶ظ…ط§ظ† ظ…ط¹ط±ظپط© ظ‡ظˆظٹط© ط§ظ„ظ…ط¹ظ„ظ… ظˆظ…ط¯ط±ط³طھظ‡
router.use(authMiddleware);

router.get("/meta", getTeacherReportsMeta);
router.get("/scopes", getTeacherReportScopes);
router.get("/context", getTeacherReportContext);
router.post("/generate", generateTeacherReport);

export default router;

=== teacherScopesRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { listTeacherScopes } from "../controllers/teacherScopesController.js";

const router = express.Router();
router.use(authMiddleware);

router.get("/", listTeacherScopes);

export default router;

=== teacherSessionsRoutes.js ===
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import {
  startSession,
  endSession,
  sessionStudents,
  saveAttendance,
} from "../controllers/teacherSessionsController.js";

const router = express.Router();

router.use(authMiddleware);

// ط¨ط¯ط، ط§ظ„ط­طµط©
router.post("/start", startSession);

// ط¥ظ†ظ‡ط§ط، ط§ظ„ط­طµط©
router.post("/:id/end", endSession);

// ط·ظ„ط§ط¨ ط§ظ„ط­طµط©
router.get("/:id/students", sessionStudents);

// ط­ظپط¸ ط§ظ„ط­ط¶ظˆط±
router.post("/:id/attendance", saveAttendance);

export default router;

=== teacherStudentsRoutes.js ===
import express from "express";
import { getMeta, getScopes, listStudents } from "../controllers/teacherStudentsController.js";

// âœ… ط؛ظٹظ‘ط± ط§ط³ظ… ط§ظ„ظ…ظٹط¯ظ„ظˆظٹط± ط­ط³ط¨ ظ…ط´ط±ظˆط¹ظƒ
import  requireAuth  from "../middleware/authMiddleware.js";

const router = express.Router();

router.get("/meta", requireAuth, getMeta);
router.get("/scopes", requireAuth, getScopes);
router.get("/list", requireAuth, listStudents);

export default router;

=== teacherTimetablesRoutes.js ===
// src/routes/teacherTimetablesRoutes.js
import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import { TeacherTimetablesController } from "../controllers/teacherTimetablesController.js";

const router = express.Router();

// ط­ظ…ط§ظٹط© ط¬ظ…ظٹط¹ ط§ظ„ظ…ط³ط§ط±ط§طھ
router.use(authMiddleware);

// ط§ظ„ط£ط³ط§ط³ظٹط©
router.get("/meta", TeacherTimetablesController.meta);
router.get("/", TeacherTimetablesController.list);
router.get("/classes", TeacherTimetablesController.classes);

// ط§ظ„ط§ط®طھط¨ط§ط±ط§طھ (ط¶ظ…ظ† ظ†ظپط³ ط§ظ„ط±ظˆطھط±)
router.get("/exams/meta", TeacherTimetablesController.examsMeta);
router.get("/exams", TeacherTimetablesController.examsList);

// âœ… ط§ظ„ط·ظ„ط§ط¨
router.get("/students/scopes", TeacherTimetablesController.studentsScopes);
router.get("/students", TeacherTimetablesController.studentsList);

export default router;

=== timetablesRoutes.js ===
import { Router } from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import TimetablesController from "../controllers/timetablesController.js";

const router = Router();

// =========================
// Meta / Lists
// =========================
router.get("/meta", authMiddleware, TimetablesController.meta);
router.get("/list", authMiddleware, TimetablesController.list);
router.get("/teachers", authMiddleware, TimetablesController.teachersBySubject);

// âœ… ظپط­طµ طھط¹ط§ط±ط¶ ط§ظ„ظ…ط¹ظ„ظ… ظ‚ط¨ظ„ ط§ظ„ط­ظپط¸
// ظ„ط§ط²ظ… ظٹظƒظˆظ† ظ‚ط¨ظ„ /:id
router.get(
  "/check-teacher-conflict",
  authMiddleware,
  TimetablesController.checkTeacherConflict
);

// =========================
// Create / Copy
// =========================
router.post("/get-or-create", authMiddleware, TimetablesController.getOrCreate);
router.post("/:id/copy-from", authMiddleware, TimetablesController.copyFrom);

// =========================
// Publish / Unpublish
// =========================
router.put("/:id/publish", authMiddleware, TimetablesController.publish);
router.put("/:id/unpublish", authMiddleware, TimetablesController.unpublish);

// =========================
// Weekly template entries
// =========================
router.put("/:id/entries", authMiddleware, TimetablesController.saveEntries);
router.delete("/:id/entries", authMiddleware, TimetablesController.clearEntries);

// =========================
// Overrides (ط§ط®طھط¨ط§ط±ط§طھ / ط¥ظ„ط؛ط§ط، / ط§ط³طھط«ظ†ط§ط،ط§طھ ط£ط³ط¨ظˆط¹ظٹط©)
// =========================
router.get("/:id/overrides", authMiddleware, TimetablesController.listOverrides);
router.put("/:id/overrides", authMiddleware, TimetablesController.upsertOverride);
router.delete("/:id/overrides", authMiddleware, TimetablesController.deleteOverride);

// ط¥ط°ط§ ط£ط±ط¯طھ طھط£ظƒظٹط¯/طھط­ط¯ظٹط« ط§ط³طھط«ظ†ط§ط،ط§طھ ط£ط³ط¨ظˆط¹ ظ…ط¹ظٹظ†
router.put(
  "/:id/overrides/publish-week",
  authMiddleware,
  TimetablesController.publishWeekOverrides
);

// =========================
// Delete timetable
// =========================
router.delete("/:id", authMiddleware, TimetablesController.remove);

// =========================
// Get timetable + entries
// ظ„ط§ط²ظ… ظٹظƒظˆظ† ط§ظ„ط£ط®ظٹط±
// =========================
router.get("/:id", authMiddleware, TimetablesController.getById);

export default router;

=== userRoutes.js ===
// src/routes/userRoutes.js
import { Router } from "express";
import { UserController } from "../controllers/userController.js";
import authMiddleware from "../middleware/authMiddleware.js";
import loadPermissions from "../middleware/loadPermissions.js";
import checkPermission from "../middleware/checkPermission.js";

const router = Router();

/**
 * ًں”¹ طµظ„ط§ط­ظٹط§طھ ط§ظ„ظ‚ط§ط¦ظ…ط© ظ„ظ„ظ…ط³طھط®ط¯ظ… ط§ظ„ط­ط§ظ„ظٹ
 * GET /api/users/me/menu-permissions
 * ظٹظڈط³طھط®ط¯ظ… ظپظٹ admin.js -> fetchMenuPermissions()
 */
router.get(
  "/me/menu-permissions",
  authMiddleware,
  UserController.getMyMenuPermissions
);

// ط¥ظ†ط´ط§ط، ظ…ط³طھط®ط¯ظ… ط¬ط¯ظٹط¯
router.post(
  "/",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.create
);

// ط¹ط±ط¶ ظƒظ„ ط§ظ„ظ…ط³طھط®ط¯ظ…ظٹظ†
router.get(
  "/",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.getAll
);

// ط¹ط±ط¶ ظ…ط³طھط®ط¯ظ… ظˆط§ط­ط¯
router.get(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.getOne
);

// طھط­ط¯ظٹط« ظ…ط³طھط®ط¯ظ…
router.put(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.update
);

// ط­ط°ظپ ظ…ط³طھط®ط¯ظ…
router.delete(
  "/:id",
  authMiddleware,
  loadPermissions,
  checkPermission("rbac.manage_users"),
  UserController.delete
);

export default router;

